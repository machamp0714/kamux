# kamux-lane/SKILL.md の実例・実測集

`kamux-lane/SKILL.md` の規則を裏付けた実例と実測。規則の射程を判断するときや、規則を消してよいか迷ったときに読む。
節名は SKILL.md 側の見出しに対応する。台帳 = `.claude/AGENT-LESSONS.md`。

## dispatch の前に、契約 73 章 5 節の該当フェーズの行を読む

- 2026-08-14、§73.5 の M2-4 欄を読みながら 🔴 が付いた 2 章（§119 / §122）だけを拾い、印の無い §63 を素通りした（台帳 #177。索引の空振り 4 回目）。
  §63.7 は逐語で「`resume_session` が既存の起動経路を通る」を M2-4 の dispatch 時 worklist として持ち、契約 §123 の最強の根拠だった。
- 「dispatch 時に適用する」worklist（38 行・7 フェーズ）は 2026-08-05 に 3 回連続で落とした。3 回とも形は「契約に書いてあったが読まれなかった」である。

## 呼び出しの形

- ハーネスの隔離 worktree の自動削除条件に、コミットが 1 つも無い初回タスクの最中は必ず当たり、M1-2 と M2-1 で実際に worktree が消えた。
- 実セッションで dispatch が 42k 文字に膨れ、99% が貼り付けた履歴だった事例がある。

## 起動後の監督（監督表）

- `SendMessage` で再開させようとして待った 2026-08-05 は、4 回・計 3 時間半以上を失った。
- 監督表の「1 時間以上応答が無い」の行だけを読んで通知待ちで構えた結果が 2 時間 14 分である（次節の実例）。
- レビューへの修正で便を交差させた PR #75 では 3 回続けて交差し、毎回「判定は既に送信済み」の往復が 1 つ増えた（台帳 #174。2026-08-14）。
  依頼に測定時 HEAD を要求する規律は既に効いており、それで毎回どちらが古いかを切り分けられた。

## 事象が起きないことを検知する規則は、能動的な起床機構を同時に置く

- 実例（2026-08-15。PR #80 / `lane-h2`）: レーンは `e70dad1`（13:09:59）を最後に停止した。team-lead が気づいたのは 15:24 で、ユーザーに指摘されてからである —— 2 時間 14 分。
  監督表の「1 時間以上応答が無い → 停止を疑う」は当時も在り、規則は在った。
  13:11 にレーンの idle 通知を受けて実測し「CI 待ちだろう」と判断して終わり、次に自分が動く条件を何も設定していなかった。結果、タイマー役がユーザーになった。
- 本節を書いた便で実際に踏んだ形（台帳 #230）: `task-reviewer` の待ちに `Monitor` を張ったが、報告ファイルの mtime 1 点しか見ておらず、報告を書き始める前の 10 分で `STALL:` が誤爆した。
  レビュアーは健全で、同じ scratchpad に検証用フィクスチャを 2 本書いていた。
  `lane-controller.md`「対処 1」の「レポートファイルの存在を条件にしない」は存在の側だけを警告しており、mtime の側が抜けていた。
- 判定基準を 2 箇所へ写した 2 版が実際に食い違い、同じ入力に別の評決を出す形になった（PR #88 の r3 レビューが検出）。
- この機構は `lane-controller.md`「対処 1: dispatch と同じターンで監視ループを張る」として既に在った（implementer 待ちについて）。team-lead 側だけが持っていなかった。
  昇格（2026-08-15。群 ξ）は新しい機構の発明ではなく、片方のロールにしか無かった機構に名前を付けて両方へ置いたものである。
- 発生当日の事後分析と `Monitor` の 4 条件の設計は台帳 #227 に在る。

## 言明の一部だけを測って、残りを前提として扱わない

stage4 で 4 回 + stage5 で 1 回。表の各行の実測:

- 理由: 「`Cmd+Alt+J` が発火するのは合成イベントのテスト由来」→ そのテストは 1 本も存在しない。結論（発火しない）は正しかったが理由が偽（台帳 #82）。
  「push すれば `ci.yml` が走る」を追認 → `ci.yml` は `branches: [main]` にしか反応せず、結果は永久に来ない。実在しない待機を 1 つ作った。
- 観測経路: 「rust-implementer が変異を打たなかった」→ レーンが 2 条件を dispatch へ入れ忘れていた。
  「報告に変異検証が無い」→ レポート `:83` に実在し、要約の書式に出ないだけだった（台帳 #102）。2 回とも「観測経路が届いていない」側だった。
- すぐ確かめられる証拠: 「brief の describe は射程外」→ ファイルを開けば 6 箇所すべてを覆っていた。
  「brief の数え方に系統誤差」→ 系列の頭（Task 1・2）は完全一致で、原因は別だった（台帳 #103）。
- 原因 / 分類の根拠: 並列違反の原因を推定して再発防止を書いた → 実際の原因は「マーカで起床して次を出した」だった。
  推定に当てた手当ては、当事者が「守っていた」と言える形になり、穴が開いたまま閉じたように見える。
- 比較の片側: コード側の符号位置は測ったが、契約側は測らず「U+00B7 のはずだ」と前提した（contract-owner が測って一致を確認）。
- 記録した実測の鮮度: 申し送り（`TEAM-LEAD-RULINGS.md`）の「`assignPane` は `focusedSessionId` を更新しない」は書いた時点では真だったが `c2dddf4` では偽のまま渡された（台帳 #100）。
- 「契約は 3 箇所で同じ誤った理由を書いている」と契約変更要求に添えたが、実際に当たるのは 1 箇所だけだった（台帳 #171。他の 2 箇所は別の理由で書かれており、そちらは真だった）。
- difftastic: team-lead が `git diff <range>` の `^diff --git` 0 行を根拠に「新しい doc の断定は増えていない」と誤判定した（台帳 #159）。
- 入口と出口: stage4 で 2 回とも入口（規則の書き方）を疑い、1 回は出口（規則を満たした後の行き先）だった。

## 中継の言葉は、対象ごとに書き分ける

- 「実装完了」の中継に「次のタスクへ進んでよい」と書き、レビューゲートを免除したように読める指示を 2 レーンへ出した（台帳 #105）。
  M2-3 が解釈を確認してきて発覚した。止めなければ 2 レーンでレビューが飛んでいた。
- 「Task 9 を出す前にブランチを戻せ」という時間依存の指示には「その worktree で稼働中のエージェントが 0 体であること」という実行条件が要った。

## 人間ゲートの手順は、渡す前に実装で検算する

- 5 つの失敗形のうち 3 つは 2026-08-05、2 つは 2026-08-06〜07 の M3-1 で実測した。この 5 行は積み上がって書き直された規則である（台帳 #22 / #24 の 2 件 + M3-1 の 3 件）。
- 粒度: 1 文字ずつ打てば現れる現象を「文字列を入力する」と書いた。
- 向き: M3-1 の項目 22 は `Cmd+J`/`Cmd+K` が editor 画面で効くと書いていたが、契約 §11.4.2 は逆を定めていた。
- 不合格の形: M3-1 の項目 7 は「nvim が開く」までしか書いておらず、PTY が 80×24 のまま画面の一部しか埋めない状態でも合格として通った（ユーザーの目視で偶然見つかった）。
- 台帳 #23 の帰属: かつて表の 2 行目（実在しない観測を指している）に寄せられていたが、機構が違う（2 行目は「出力経路が無い」、#23 は「出力経路はあるが前提条件を作れない」）。検査 4 へ移した。
  結果（手順が空振りする）が同じなので同じ行に寄せたのが、規則の側の誤りだった。
- 検査 5 の実測（2026-08-14。レーンが §124.7.1 の形を §6.1〜§6.7 に当てて 2 件見つけた）:
  §6.1 の目玉（「私の名前は何？」）は `--resume` と `--continue` を区別せず、`--resume` が黙って落ちても緑になった。
  §6.4 の逆ケース（「トーストが出ない」）は、文言を描く `InterruptedOverlay` が未マウントなので恒真だった。
- 台帳 #193: hooks の疎通を測るはずの観測欄が `BelDetected` / `SilenceTimeout` 由来の 🟡 / ⚪ に到達でき、hooks が死んでいても合格した（`runtime_state.rs:149-150`）。
- 検査 5 の宛先の書き直し（2026-08-15。台帳 #230）: 宛先を「変異 / 陽性の対照 / スモークの観測欄」と書いていたため待ち条件・起床条件が射程外に落ちた。
  7 行が射程外に落ちたのと同じ形が、宛先を変えてもう一度起きた形である。
- 検査 4 の実測（2026-08-10 の stage4 ゲートで、台帳 #23 を 3 検査に逐語で通した結果）: 3 つとも合格するのに、手順は必ず空振りする。
  `PATH` から nvim を外す手順は実施者の対話シェルの `PATH` を変えるだけで、アプリはログインシェルから組み直した `PATH` に 4 つのフォールバックを無条件に足していた（`launch_env.rs:34-35,150-155`）。
  macOS で nvim が入っているのはまさに `/opt/homebrew/bin` である。
  検査 1 で grep すると「ガイド文の経路はある」と出て合格になる。出力経路は在り、作れないのは入力のほうだった。

## 規則を書いた便には、その規則をその便自身へ当てる

stage5 で 5 回（台帳 #124 / #125 / #128 / #142 / #144）。

- 節の見出しを 2 度改名した PR が、同じ PR で足した `SKILL.md` の行を旧名で引用したまま残した（台帳 #124）。引用の解決は、改名した本人の便の中で切れた。
- 観測点を 3 箇所に書いた PR が、`SKILL.md` §6 が正典と定めた棚卸し表だけ 1 行落とした（台帳 #125）。正典を自分で定めた便が、その正典を満たさなかった。
- 「配置先の全ロール」を §6 へ足した PR が、直前の件数宣言を「次の 2 つ」のまま残した（2026-08-14。自分で当てて捕まえた 1 件目）。
- 引用の向きの検査は 2026-08-14 に 3 ラウンド連続で毎回 1〜2 件ずつ出た。
- 正規化: 見出しが `断定は、**宛先を問わず**、…` で引用が `断定は、宛先を問わず…` だと素の `grep` は 0 を返す。この形をその PR 自身が 3 回作った。
- code span: `**` のパリティ検査が `` `**Task 11 相当: complete` `` の中の `**` を数えて偽陽性を出し、team-lead 側とレビュアー側の 2 系統の独立検査が同じ盲点を持って互いを検算できなかった（2026-08-14。台帳 #172）。
- 警告文だけの手当ての再発（2026-08-14。台帳 #196 / #201）: 規則は 5 ファイル全部に在ったのに両側で同日再発した。#196 は実装者が自分の報告 `:144` に書いた罠を次の便で踏んだ形で、その行自身が「警告文では閉じない。手当てはコマンドの形でなければならない」と書いていた。
  #201 は team-lead が `TEAM-LEAD-RULINGS.md` へ「`cd -` で書かない」と手順を直した直後に同じ形を打ち、3 回目に `git -C <絶対パス>` へ変えて初めて止まった。

## 証拠に借りたコマンドの出力が、2 状態を区別するか

- パイプ: 2026-08-15 のゲートで team-lead 自身が `cargo test --workspace 2>&1 | tail -20` を踏み、`cargo test` が 8 件しか見えなかった（実際は 783）。clippy が落ちていても緑と報告する形だった。
- `git push -q origin main`（台帳 #180）/ `rm -rf` の glob（#114。実物は `rm -rf $TMPDIR/kamux-boot-from-*` で、zsh の no-match が先行パターンの削除ごと中断した）/ `gh pr merge --delete-branch`（#118 / #191。worktree から打つと `fatal: 'main' is already used by worktree` が出るが、リモートのマージは成功している）/ `git ls-files -s`（#169）/ `git status --porcelain`（#113）/ `--no-fail-fast` 無し（#40）/ アサーション反転とテスト本数（#28）。
- CI / ローカルの緑（#115）: `externalBin` 化の後、ローカルは worktree に残ったバイナリで全緑、CI だけが `resource path … doesn't exist` で落ちた。PR 単位レビューも変異検証つきで APPROVED を出していた。
- 無視パターンが `src-tauri/target/` を焼き込んで死んだ件（#54）は、CI が `target/` を持たないため CI から永久に観測できない。

## 検査と是正の順序

- 規則 1 の実測（2026-08-14。台帳 #176）: `APPROVED` は `df5d06f` に対して出たが、マージされたのは `0934abf` で `df5d06f..95fd6fe`（3 ファイル / +8 行)を含んでいた。
  PR 本文に「承認後に 1 コミット積んだ」と明記したが、それは口頭補填だった。同型の 1 件目は #16（全差分レビューのパッケージ生成後にコミットが積まれた）。
- 「フェーズ完了時は必ず非空になる」は 2026-08-15 の PR #78 レビューが検出した。
- 但し書き（101 章 4 節は逐語では governs しない）は 2026-08-15 のレビュー r2 が検出した。`lane-controller.md` の手順 3 には同じ但し書きが在り、本ファイルだけ落ちていた（群 K の形）。
  §101.4 の見出しは「判別条件は『コンフリクト解決が発生したか』である」、正典ブロックの前置きは「P1 を成立させるために `git merge origin/main` を入れたとき」（`00-contracts.md:12785` / `:12787`。逐語で引いた）。
- 根拠の差し替え: レビュアーが「自分はこの依存を未検証だ」と明記したので team-lead が §32 条件 3 の逐語を引き、根拠を §101.4 へ差し替えた（規則を自分の便へ当てた結果である）。
- 規則 2 の実測（台帳 #211）: 旧手順の順序により、M2-4 では Critical 1 件（L-1）が手当ての経路を持たないまま main に着地し、契約 §127 が単独 hotfix を裁定して初めて宛先を得た。
- 観測点の書き直し: 初版の「ファイル全体 grep」は、初出が `gh pr merge` が先で順序条件が偽になる形を 2026-08-15 のレビューが実際に検出した。合格と不合格を区別しない観測点だった。

## 同じ作業に実体は 1 体だけ

- #7: 繰り越し項目の受け手を名指ししたが、既に着手していた contract-owner が居るかを確認せず、受け手が 2 人になりうる状態を作った。
- #185: 降りたレーンへ `SendMessage` を 2 通送って再稼働させ、同時に後継も起動して lane-controller が 2 体になった。
  先行レーンは既に implementer を dispatch しており、後継が着手前に BLOCKED で止めなければ `cli_args.rs` に implementer が 2 体乗るところだった。
- #205: 「未適用」として起動指示に書いた計画 4 箇所が dispatch 直後に前任レーンによって適用され、後継が二重適用しうる状態になった。修正 3 は全面差し替えなので二重に当てると内容が壊れる。

## 稼働中の他者が使っている状態へ書かない

- 2026-08-14、team-lead が稼働中レーンと同じチェックアウトで `git commit` を打ち、コミット 2 本がレーンの作業ブランチへ落ちた（台帳 #179）。stage5 は隔離不要でチェックアウトを共有していた。
- 同型の 2 件目は #112: レビュアーの変異が、稼働中の実アプリが絶対パスで参照する `target/debug/kamux-relay` を上書きした。
- 手当てとして作った常設 worktree（main を占有）が、今度はレーンの `gh pr merge --delete-branch` を失敗させた（#191）。

## 裁定は SendMessage を待たず、契約ファイルの末尾を読む

- §123 / §124 / §125 の 3 回とも、team-lead の裁定中継とレーンの引き継ぎが行き違った（台帳 #203）。#147 / #165 も同じ機構である。

## 工程を定義したら、産物の書き先・書き手・時点を同じ場所に書く

- #33: ステージ完了ゲートの実施記録を書く場所がどこにも定義されておらず、確認 1 が「本 PR の後」という予告のまま恒久記録に残った。
- #162: 観測を手動スモークから自動テストへ移す作業で、手動チェックリスト側からの項目削除が同じ差分に入るのか別作業なのかが定義されていなかった。
- #188: 持ち越し `minor (deferred)` の最終判定者と判定タイミングが定義に無く、PR 単位レビューの dispatch が個別に指示して初めて 10 件が判定された。
- 観測点の書き直し: 2026-08-15 のレビューが初版を「ファイルごとの個別カウントが返り判定できない」「glob に guard が無い」として差し戻し、2 版目も worktree で打つと zsh の no-match で中断した。
  本節を書いた同じ便が昇格させた #114 を、書いた本人が 2 版続けて踏み、3 版目で `find` に替えて閉じた。

## 原本が在る値を手で写さない

- #4: 二重の正典を 1 ファイルから削った同じコミットで、同じ閾値が別ファイルに新設された。
- #110: 人間ゲートの手順に置いた長い `printf` が `build_hook_settings()`（`session/cli_args.rs:149`）の出力を人間が書き写した第 2 の正典であり、`HOOK_EVENTS` / `HOOK_TIMEOUT_SECS` / `shell_single_quote` の 3 つを手で複製していた。
  4 検査は 4 つとも合格しており、壊れたのは手順の「転送」だけだった —— 貼り付け時に折り返されて JSON 文字列に生の改行が入り、`jq` が `control characters from U+0000 through U+001F must be escaped` を返した。

## 列挙・件数は機械で導出して突き合わせる

3 件とも 2026-08-14。

- #197: モジュールツリーの欠落を目で数えて 17 とし 1 件を落とした。`comm -23` で取り直して 18。要求側の team-lead は 2 件しか挙げていなかった。
- #199: 契約 §124.10 の見出しが「8 行」と宣言しているのに表は 7 行だった。
- #200: 台帳へ足した行の列数を 2 回落とした。行数検算は 2 回とも合格を返し、捕まえたのは列数検算だけである。

## 「この規則は本件には当たらない」と書くとき

- 2026-08-09、1 行の中に 3 回。§98.10 が「§97.9 の理由は本件に転移しない」と逐語で書いていた（台帳 #68）。
- 前例として、行内の複数回で昇格させたのは #59 = 群 D（4 回）と #48 = 群 P（3 回）である。

## 数値には単位・時点・母数の定義を添える

- 旧規律は `REPORT_WRITTEN_AT` と「測定時 HEAD」という形で報告ファイルにしか掛かっていなかった。
  実測（`74f9a73`）: `grep -c REPORT_WRITTEN_AT` は task-reviewer 2 / rust 2 / web 1 に対し lane-controller 0 / SKILL.md 0 / contract-owner 0。
- #122: PR 本文の差分規模を `+46/-15` と書き、実測は `+72/-0`。
- #186: 起動指示の数値に単位を書かず、受け手が別の量を測った（データ行数 184 / ファイル総行数 299）。
- #189: production の doc に `750 passed`、実測 767。#190: その手当ての引用 sha `424d8a2` が squash で到達不能だった。

## 本ファイル内の 2 箇所が食い違ったら、それは定義の穴である

- 旧規律（群 V）は「dispatch ⇔ 定義ファイル」の 1 軸しか持たなかった。実測（`74f9a73`）: `grep -c '群 V'` は lane-controller 3 / task-reviewer 1 / rust 1 / web 1 に対し SKILL.md 0 / contract-owner 0。
- 規則 a の実測（台帳 #126）: 2026-08-14、本ファイルの 🔴 行と判定行が食い違い、レーンがどちらに従っても違反する状態になった。
- 規則 b の実測（#87）: 計画 6 箇所 vs 契約 4 箇所の衝突で、producer 側の「該当 0 件」の実測が決め手になった。その形の証拠を裁定要求に添える手順がどこにも無かった。

## 規則を当てる工程を、渡す前に 1 回入れる

- 契約 §105.5（名詞 / 操作 / 観測の 3 欄に一次証拠を 1 つずつ挙げる）は 2026-08-09 21:00 に成立した。その当日 21:14 に、それが捕まえるはずの 6 件目を素通りさせた。
- Dock バッジの例: 名詞（Dock の kamux アイコン）には `lsappinfo … ApplicationType="Foreground"` を挙げたが、観測（バッジが見える）に `defaults read com.apple.dock autohide` = 0 を挙げなかった。この機体は `1`（自動的に隠れる）だった。
  バッジは 3 回とも正しく出ていた。Dock が画面に存在しなかっただけである。窓を 3 秒 → 20 秒に広げても、合図を `tail -f` にしても、この原因には一切効かない。
- 実施者が 6 回止まっている（台帳 #77 / 本件）。

## 測定を指示するときは、測る対象を名指しする

台帳 #78。同じ穴が 2 回。

- 測定 2 で「どのスレッドから測るか」を落とした（メインから測ると `is_waiting` はほぼ常に false になり、別の量を測る）。
- 修正ラウンド 1 で「`wait_for_response` をどのハンドルに対して行うか」を落とし、項目 3〜6 の観測が全部「捨てる経路」にぶら下がった。
- 平均値（台帳 #80）: `is_waiting` の `ratio` が 0.73 の回で試行 A が通り、0.82 の回で落ちた。1 回しか回していなければ「A で十分」と結論していた。

## 裁定と実施記録は、リポジトリに残る形で渡す

- M3-1 では team-lead の裁定を task-reviewer が `TEAM-LEAD-RULINGS.md` に grep しても見つけられず、「承認条件が満たされていない可能性」として検証不能扱いになった（台帳 #12）。
- PR 24 では契約 §32 条件 3（PR 単位レビュー）が ledger に「Task 8 のレビュー後・PR 24 作成前に実施する。いま dispatch しない」と予告だけ書かれ、実施されないままレーンが終了した（台帳 #26）。
  マージ直前に team-lead が気づかなければそのまま入っていた。
- フェーズ完了報告そのものが出ず、契約 §26.5-3 の仕分け記録が残らなかった事例もある（台帳 #30）。
- 恒久引用の実例: `TEAM-LEAD-RULINGS.md`（統合前の書き先）を `SKILL.md` の起床機構の節が経緯の恒久的な引用先として引用してしまい、他の worktree から読めない状態を作った。修正後は台帳 #227 を引いている。

## マージ許可

- 2026-08-08、許可を出したあと 3 通に分けて条件を足し、3 通が届いた時点で PR は既にマージ済みだった。
- 番号外 PR: 2026-08-09、`[PR H2]`（#62）について「CI 緑になり次第マージしてよい」と 2 度書いた。中身は `task-reviewer` が一度も見ておらず、`pty/surface.rs` の production 挙動を変える修正だった。M2-2 レーンが止めた。

## ステージ完了ゲート

- 確認 1 の「空」（2026-08-14。台帳 #170）: 検査 17 ② は単一行パターンで、対象が行に割れていたため実際に空振りし、契約に偽の断定を書かせた。
- 検査の向き（台帳 #210）: 契約 §25.4 は「誰も Create しないまま Modify される」しか見ず、逆向き（Create は在るが誰も使わない＝装着工程が無い）は 12 計画 171 タスクで一度も鳴らなかった。
  `InterruptedOverlay.tsx` が未マウントのまま main へ着地した。
- 別の綴り（#62）: ポーリング禁止を `set_nonblocking` / `sleep` の grep で判定し、`Incoming` の無待機スピンが掛からなかった。
- 確認 3 の空振り: 索引を読みながら印の無い章を素通りした「索引の 4 回目の空振り」が在る（台帳 #177）。

## やってはいけないこと

- implementer の並列起動: 2026-08-05、M2-1 の Task 4 と Task 5 の並行編集で作業が実際に消失した（復旧に一時 worktree + `git update-ref` を要した）。
- 定義ファイルの無レビュー変更: 「コードを直す」としか書かれていなかったため定義ファイルが該当するか読み取れず、PR #52 / #54 / #57 と 3 度続いた。
  台帳 #2 は `—` のまま据え置かれ、閾値 2 回が発火しなかった（台帳 #34）。
