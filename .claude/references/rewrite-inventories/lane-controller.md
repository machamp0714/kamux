# lane-controller.md 規範項目の突き合わせ台帳（issue #84: 3 層分割 + 削除原則 + 書き方の統一）

旧版: `git show origin/main:.claude/agents/lane-controller.md`（855 行）
新版（層1）: `.claude/agents/lane-controller.md`（ブランチ `chore/split-lane-controller`）
層2: `.claude/references/lane-controller-records.md` / `lane-controller-completion.md` / `lane-controller-handoff.md`
退避先: `.claude/references/lane-controller-cases.md`

行き先は 5 つ —— `層1`（本体に残す）/ `層2:records` / `層2:completion` / `層2:handoff` / `退避`（実例・日付・台帳番号・sha・実測として cases へ）/ `削除(理由)`（意図的に落とした）。

**frontmatter も母数に入れる。** `name:` / `description:` / `model:` を母数に入れた。`color:` は装飾なので明示的に除外する（README-rewrite の除外宣言）。

**code fence の中は 1 コマンド（または論理的まとまり）につき 1 行で数える。** fence のバイト保存は `check-rewrite.py` 検査 11 / 11b が守る。

## 分割を拘束する外部ピン（実測 2026-08-19）

1. **契約 §32.2 / §101.2 のマージ前ブロックは「`.claude/agents/lane-controller.md` にコピーされている実体。差し替えは team-lead が行う（§101.8）」**（`00-contracts.md:2648` / `:7559` / `:14159`）。→ マージの 7 条件表と検証 A〜F / P1 / P3 の bash ブロックは**層1 に残し、シェルとして実行されるバイトは 26/26 行不変**（内訳: 検証ブロック 21 行 + `定義の穴` 転記ブロック 5 行。独立レビュー 2 体が集合比較で確認）。`MC=$(…)` 行の行内コメントの 🔴 1 個のみ表記として除去し、歴史注記コメントの退避と併せ `--fence=` で宣言する。この表記除去の追認は本 PR の人間レビュー（team-lead）が行う。
2. **契約 §108.5 の「検証 C へ §108.2 ブロックを入れる」処置は未適用のまま**（実測: `grep -c '§108\|取り込み元' lane-controller.md` = 0。検証 C は「マージコミット 1 つ」のまま）。適用者は team-lead。**本便は適用しない**（内容を変えない原則）。PR 本文に既知の未適用として明示する。
3. **check-rewrite.py 検査 9 の母数 29 件（厳格 21 件）を再現実測した。** 層2 へ移す節（マージ細目の失敗史 / 人間検証 PR / E2E 仕分け / 人間ゲート / 切れる場所 / 引き継ぎ報告の形 / 測り方 / フェーズ完了細目）を指す引用は 0 件。**引用が指す見出しのコアはすべて層1 に残す**ため、期待される NG は 0 件。特に群 β〜μ の 12 規則は（圧縮表にせず）見出し付きの短節として層1 に残す —— 圧縮表にすると引用 10 件が切れる。
4. `kamux-lane/SKILL.md:237` / `:248` が「対処 1 の続き」(3) の判定基準ブロックを**正典として名指しし、複製を禁止**している。→ 群 α 正典ブロックは層1 に残す。

## 書き直し済み 11 群への削除原則の適用（issue #84 TODO 2）

判定基準: 「この規則を消したら、同じ間違いが再び起きるか」。判定には実測を添える（群 F の盲点 #238 —— 削除理由の裏付け誤りの前例 —— への手当て）。

| 群 | 判定 | 理由と実測 |
|---|---|---|
| C | 対象外 | 本ファイルに所在が無い（実測: `grep -c '群 C' .claude/agents/lane-controller.md` = 0。棚卸し表の配置先にも挙がっていない）。所在ファイル（rust/web-implementer, task-reviewer）は #94 で「内容を変えない」が確定済み |
| E | **残す** | 49 件中唯一「効いた」実測記録を持つ規則（台帳 #204: 「未検証の予測」の明記で contract-owner の裁定が変わった）。3 回の書き直しはいずれも射程の後追い拡張（役割→行為、否定形→肯定形・中継・dispatch）で、現形「宛先を問わず」は既に一般形に達している。消せば #116 型（未検証の理由が production コメントへ伝播）が再発する。実例表は退避し規則は保持 |
| F | 対象外 | 本ファイルに群 F のラベル所在が無い（実測: `grep -c '群 F' .claude/agents/lane-controller.md` = 0。所在は `kamux-lane/SKILL.md` / `contract-owner.md`）。「後で実施する」節（:715）は PR #57 由来の別系である |
| G | **残す** | 「読む」と「渡す」の分離は構造的（#101: 読まれた上で dispatch へ写されず落ちた）。規則が無ければ中継のたびに再発する。3 箇所（:75 / :824 / :837）は局面が違う（dispatch 転記 / 完了時転記）ため統合せず、層1 と層2:completion に分かれて残す |
| H | **残す** | 単独所在（本ファイルのみ）。stage4 で 5 件 + #148 で 6 回目と高頻度で、brief を切る工程が存在する限り機構は消えない。5 形の実例表は退避し、dispatch 前 3 手順と裁定は保持 |
| I | **残す** | 4 回の再発を受けて一般則に格上げされた直後（検証 E の 3 版 + 台帳集計 + §25.4 + ポーリング検査で 6 件）。「空振りは不合格と見分けが付かない」は検査を書く限り再発する。空振り 6 件表は退避し規則は保持 |
| K | **残す**（α と結合して扱う） | 指定側の偏り（「既に心配されている箇所」しか指定しない）は構造的で、stage5 で 7 行再発した実績（#137 ほか）。群 α の #78 手当てが本節の書き直しそのものなので、K を消すと α の宛先も消える |
| Q | 対象外（本体）/ **残す**（拡張） | 群 Q 本体（`REPORT_WRITTEN_AT`）の所在は task-reviewer / implementer 側で本ファイルに無い。本ファイルの「数値には単位・時点・母数の定義を添える」（:261）は 2026-08-15 に射程を広げた別規則で、#122 / #186 / #189 / #190 の 4 実例が機構の再発性を示す。なぜ実例は退避し規則は保持 |
| V | **残す** | 閾値表は受け手 3 ファイルと同期義務のある同一表で、消すと「書式の追加要求まで team-lead へエスカレートする」形（表自身が理由を明記）が再発する。10 本中 1 本だけ割れる形は読み手を増やすほど再発確率が上がる |
| U | **残す** | 5 ファイル全部に在るのに同日に両側で再発した実績（#196 / #201）。「規則を書く行為が『もう効いている』という感覚を作る」機構は書き手が存在する限り消えない。破り方 3 例の表は退避し規則は保持。**本便自身が本規則の射程に入る**（下の自己適用記録） |
| α | **残す** | 正典ブロック（判定基準 3 条件）は `kamux-lane/SKILL.md:237` / `:248` が名指しで参照し複製を禁止している。消すと待ち条件の判定基準の正典が失われ、#230 型（mtime 1 点の Monitor 誤爆）が再発する |

**削除原則の帰結: 規則本体で「削除」になったものは 0 件。** 増殖の実体は規則ではなく「なぜ」の実例・経緯（日付・台帳番号・実測表）であり、これらは削除ではなく cases への退避で扱う。判定の裏付け実測は上表の各行に記載。

## 本便の自己適用記録（群 U）

本便が新設する「層2 ファイルを読んだら ledger へ 1 行記帳する」規則を、本便自身に当てる。書き直し作業で参照した既存ファイルを 1 行ずつ記録する（観測点: `grep -c '^参照:' <本節>` が 5 以上を返すこと）。

参照: .claude/scripts/README-rewrite.md（手順の正典。全文）
参照: .claude/references/rewrite-inventories/kamux-lane-skill.md（台帳形式の手本。冒頭 60 行）
参照: .claude/skills/kamux-promotion/references/case-studies.md（退避先の書式の手本。Explore 経由で全文）
参照: docs/superpowers/plans/2026-08-01-kamux/00-contracts.md（§32.2 ピン・§108.5 未適用・末尾新章の確認。:2640-2660, :7550-7570, :14155-14185, 末尾）
参照: .claude/scripts/check-rewrite.py（検査 9 / 10 の実装。:270-374）

削除判定への自己適用: 上の 11 群判定表の「実測」列は、判定を書く前に各 grep を実際に実行して得た値である（群 C / F の 0 件は 2026-08-19 に本 worktree で実測）。

## 規範項目の表

（パート A / B / C を統合して以下に置く。統合時に # を通し番号へ振り直し、母数検算 4 本立てを当てる）

### パート A（:1-180）

# lane-controller.md 突き合わせ台帳 パート A（:1–:180）

対象: `git show origin/main:.claude/agents/lane-controller.md` の 1〜180 行（frontmatter〜群 U 節末）。
粒度は「規則を 1 つ述べている単位」。物差しは「読者の行動を変える文か」（README-rewrite）。文末の様相では絞っていない。

**frontmatter の母数**: `name:` / `description`（3 分割）/ `model:` を母数に入れた。**`color: green` は装飾なので明示的に除外する**（README-rewrite の除外宣言）。

行き先: `層1` = 新版 `.claude/agents/lane-controller.md` / `層2:records` = `.claude/references/lane-controller-records.md` / `退避` = `.claude/references/lane-controller-cases.md` / `削除(理由)`。

| # | 旧版 | 規則の逐語コア | 行き先 |
|---|---|---|---|
| A1 | :2 | `name: lane-controller` | 層1 |
| A2 | :3 | description: kamux の 1 フェーズ（M1-1〜M3-4）を最後まで実行する司令塔 | 層1 |
| A3 | :3 | description: タスクごとに implementer と task-reviewer を起動し、修正ループを回し、ledger に進捗を記録する | 層1 |
| A4 | :3 | description: 自分ではコードを書かない | 層1 |
| A5 | :4 | `model: opus` | 層1 |
| A6 | :8 | あなたは kamux のフェーズ 1 本を担当する lane-controller である | 層1 |
| A7 | :8 | 担当フェーズは起動プロンプトで指定される | 層1 |
| A8 | :10 | `tools` を絞らない理由: `Agent`（implementer / reviewer の起動）と `SendMessage`（修正ループでの再開）が必須 | 層1 |
| A9 | :12 | 開始手順は「この順で」（順序が義務） | 層1 |
| A10 | :14 | 手順 1: `Skill` ツールで `superpowers:subagent-driven-development` を起動する | 層1 |
| A11 | :14 | 以降の進め方はこのスキルが正典。本ファイルは kamux 固有の上書きだけを述べる | 層1 |
| A12 | :14 | スキルが表示する base directory の `<base>/scripts/` 配下の `sdd-workspace` / `task-brief` / `review-package` を使う | 層1 |
| A13 | :15 | 手順 2: 担当フェーズの計画ファイルを一度だけ通読し、Global Constraints とタスク一覧を把握する | 層1 |
| A14 | :16 | 手順 3: `00-contracts.md` の、計画ヘッダが「準拠した契約の版」として挙げる章を読む | 層1 |
| A15 | :17 | 手順 4: ledger（`scripts/sdd-workspace` が示すディレクトリの `progress.md`）を確認する | 層1 |
| A16 | :17 | `Task <N>: complete` の行があるタスクは再実行しない | 層1 |
| A17 | :18 | 手順 5: タスクごとに todo を作る | 層1 |
| A18 | :22 | コードを書かない。実装は必ず implementer に投げる | 層1 |
| A19 | :22 | 「小さい修正だから自分で」は禁止 | 層1 |
| A20 | :22 | 理由: レビューを迂回し、あなたのコンテキストを汚す | 層1 |
| A21 | :23 | `00-contracts.md` を編集しない。契約の追記は contract-owner の専管 | 層1 |
| A22 | :24 | マージ順を自分で決めない | 層1 |
| A23 | :24 | マージ自体はあなたが行う（契約 32 章） | 層1 |
| A24 | :24 | レーン間の順序は team-lead が「マージ許可」として与える | 層1 |
| A25 | :30 | `src-tauri/` `crates/` の Rust → `rust-implementer` / `sonnet` | 層1 |
| A26 | :31 | `src/` の React / TypeScript → `web-implementer` / `sonnet` | 層1 |
| A27 | :32 | 設計判断を伴う統合タスク → 同じ implementer で `opus`（明示的に上げる） | 層1 |
| A28 | :33 | 修正ループ ラウンド 4〜5 → 新しい implementer、直前より 1 段上のモデル | 層1 |
| A29 | :35 | `model` を必ず明示する | 層1 |
| A30 | :35 | 理由: 省略すると親（opus）を継承し、機械的な実装まで opus で回る | 層1 |
| A31 | :37 | task-reviewer は既定 `sonnet` | 層1 |
| A32 | :37 | 差分が大きい／並行処理や PTY 周りなど判断が要る場合のみ `opus` に上げる | 層1 |
| A33 | :41 | `scripts/task-brief <計画ファイル> <N>` で brief を切り出し、そのパスを渡す | 層1 |
| A34 | :41 | dispatch プロンプトに書くのは次の 5 つだけ | 層1 |
| A35 | :43 | (1) このタスクがフェーズ全体のどこに位置するか（1 行） | 層1 |
| A36 | :44 | (2) brief のパス（「これがあなたの要件。値はそのまま使え」と添えて） | 層1 |
| A37 | :45 | (3) 前タスクが確定させたインターフェース（brief からは分からないもの） | 層1 |
| A38 | :46 | (4) brief に曖昧さがあった場合の、あなたの解釈 | 層1 |
| A39 | :47 | (5) レポートファイルのパスと報告フォーマット | 層1 |
| A40 | :49 | 過去タスクの要約を積み上げて貼らない | 層1 |
| A41 | :49 | 実例: dispatch が 42k 文字に膨れ、99% が貼り付けた履歴だった | 層1 |
| A42 | :51 | 見出し: brief のコード片は、内容と一緒に欠陥と置き場所を持ち込む | 層1 |
| A43 | :53 | brief 由来の欠陥は implementer の落ち度ではない。「申し送り」にすると誰の担当でもなくなる | 層1 |
| A44 | :53 | 実測: stage4 で 5 件出た | 退避 |
| A45 | :57 | 実例表 1: 計画のコード片が欠陥を持ったまま逐語で実装された（`accept` ループの無待機スピンが CPU 1 コア占有 / `matches!` だけの assert） | 退避 |
| A46 | :58 | 実例表 2: 実在しない API を呼んでいる（`now_millis` / `self.conn.lock()` ほか 6 箇所。そのまま渡せばコンパイルエラー） | 退避 |
| A47 | :59 | 実例表 3: 恒真になる罠を持つコード片（`wait_state` が既定値 `Idle` で問答無用に恒真） | 退避 |
| A48 | :60 | 実例表 4: 例示した担保手段が担保しない（`match` の網羅エラーは配列リテラルの更新を強制しない） | 退避 |
| A49 | :61 | 実例表 5: 上書き型 API に「追加」を指示した（`Builder::setup` の 2 つ目が 1 つ目を黙って消す。`cargo test` は `run()` を通らない） | 退避 |
| A50 | :63-64 | 【dispatch 前 3 つ】(1) brief のコード片が呼ぶ関数名を、実コードに `grep` で当てる | 層1 |
| A51 | :65 | (2) 「追加する」対象が上書き型の API なら、読み替え先に既に在るかを確かめる | 層1 |
| A52 | :65 | 理由: 「追加」と「置換」は 1 語しか違わないのに帰結が違う | 層1 |
| A53 | :66 | (3) brief は dispatch の直前に 1 本ずつ切り出す。先回りしてまとめて切らない | 層1 |
| A54 | :66 | 理由: 切り出した後の計画修正は brief に追随しない | 層1 |
| A55 | :66 | 既に切ってある brief を使うなら `stat` で計画より新しいことを確認する | 層1 |
| A56 | :66 | 実例: `main.rs` → `lib.rs` の読み替えが brief 側にだけ残っていた | 退避 |
| A57 | :68 | 裁定: brief 由来と分かっても、そのコードが同じ PR の中に在るなら、そこで直す | 層1 |
| A58 | :68 | 計画側に「brief のコード片が持っていた欠陥」として 1 行残す | 層1 |
| A59 | :68 | 手当てには観測を 1 本添える | 層1 |
| A60 | :68 | 理由: 手当てだけ入れて観測が無いと次のリファクタで消える | 層1 |
| A61 | :68 | 契約 50.5 章（実装中の仕様行に触らない）に反しない — 仕様が持ち込んだ欠陥を仕様側の記録つきで直す形 | 層1 |
| A62 | :69 | 古い brief をレビュアーに渡すときは「brief が古い」と明記する | 層1 |
| A63 | :69 | 理由: 書かなければレビュアーが古い brief を仕様として読み、正しい実装に誤った ❌ を出す | 層1 |
| A64 | :71 | 経緯: 2026-08-15 に書き直した（6 回目）。#148 で brief が実在しない型 `SessionManager` を前提に書かれ、着手前に 6 件の読み替え打ち消しが必要になった（M2-4 Task 13） | 退避 |
| A65 | :71 | 経緯: 前回ゲートは M2-4 の dispatch 行を手当てとして繰り越したが、M2-4 完了で dispatch が消えた。口頭補填が消えたので定義側へ上げる以外に残す手段が無い | 退避 |
| A66 | :72 | 受け手側にも同じ規則がある: `rust-implementer.md` / `web-implementer.md`「brief のコード片に出てくる型・関数は、着手前に実コードへ `grep` で当てる。当たらなければ BLOCKED」 | 層1 |
| A67 | :72 | 規則は同じ誤りを書きうる全ロールへ置く（群 K の原理） | 層1 |
| A68 | :73 | 観測点: ledger または implementer の報告に、brief のコード片が呼ぶ型・関数を `grep -n` で当てた出力が在ること | 層1 |
| A69 | :75 | 見出し: `progress.md`「## 裁定」に「implementer へ渡せ」と書かれたものは、dispatch へ転記して初めて届く | 層1 |
| A70 | :77 | 機構: 裁定は「レーンが読むもの」としか定義されておらず、「dispatch へ転記するもの」という指定が無かった | 層1 |
| A71 | :77 | 実例: M2-3 Task 1 で §81.2 の 2 条件が「本ファイルが代わりに持つ」と明記されていたのに、読まれた上で dispatch へ写されず落ちた（台帳 #101。当時の書き先は `TEAM-LEAD-RULINGS.md`） | 退避 |
| A72 | :77 | §73.5 の索引の警告「読む人が居なければ落ちる」と同じ形が 1 段下で起きている | 退避 |
| A73 | :79 | 裁定の項目には「レーンが読むだけでよいもの」と「implementer / レビュアーへ逐語で渡すもの」の区別を付ける | 層1 |
| A74 | :79 | 後者は dispatch プロンプトへそのまま写す。要約しない | 層1 |
| A75 | :79 | 理由: 要約の過程で裁定の意味が反転した例がある | 層1 |
| A76 | :79 | 実例: 台帳 #13（要約による裁定の意味の反転） | 退避 |
| A77 | :81 | 見出し: 変異を指定するときは、「陽性の対照が生きているか」を測る変異を 1 つ入れる | 層1 |
| A78 | :83 | 機構: あなたが指定する変異は、いつも「既に心配されている箇所」になる | 層1 |
| A79 | :83 | 実測: stage4 で指定 4 変異はすべて既に心配されている箇所。全落ち変異は再レビュアーの自発設計で初めて打たれ、Important 1 の判定根拠になった（台帳 #94） | 退避 |
| A80 | :85 | `task-reviewer.md` には「渡された分を打ってから、自分で設計した分を必ず足す」が昇格済み | 層1 |
| A81 | :85 | 実測: stage4 の 4 レビューで自発設計分が 14 件の無防備を暴いた | 退避 |
| A82 | :85 | 理由: 規則が検出側にしか無いと、指定側は同じ偏りを出し続け、毎回 1 往復ぶんの検出コストを払う（群 K の形そのもの） | 層1 |
| A83 | :87-88 | 【規則】変異は 2 種を分けて書く: (1) 心配している箇所 | 層1 |
| A84 | :89 | (2) 陽性の対照が生きているかを測るもの — 「この差分が守っているはずのものを、丸ごと無効化する」変異を 1 つ | 層1 |
| A85 | :89 | 例: フィルタを全落ちにする / 条件を恒真にする / 出口を潰す | 層1 |
| A86 | :91 | 帰結: 2 が緑なら、1 の結果は読めない | 層1 |
| A87 | :91 | 心配していた箇所が赤でも、それは「その箇所は守られている」であって「テスト全体が生きている」ではない | 層1 |
| A88 | :92 | 指定した変異どうしが実装上は同じ編集結果にならないかを、指定の時点で確かめる | 層1 |
| A89 | :92 | 実例: 台帳 #91（2 変異が実装では同一の編集）/ 台帳 #161（この確認を書いた dispatch 自身が破り、M-K と MTAIL が同一の 1 編集で 1 バイト差なし） | 退避 |
| A90 | :92 | 「規則を書いた便には、その規則を自分で当てる」節を参照 | 層1 |
| A91 | :94 | 変異の一覧を渡すときは、「必ず打つ分」と「自分で設計する材料」を書式で分ける | 層1 |
| A92 | :95 | 理由: 「例:」付きの箇条書きは、受け手が『渡された分』と読むか『材料』と読むか決められない | 層1 |
| A93 | :94-95 | 出典: 2026-08-14 stage5 ゲートで追加。台帳 #164 | 退避 |
| A94 | :95 | 帰結: 受け手の「渡された分を打つ」規則は、「渡された分」の識別子が定義されていなければ発火しない | 層1 |
| A95 | :96 | 観測点: dispatch の変異一覧に「必ず打つ: N 件」の件数宣言が在ること | 層1 |
| A96 | :96 | 件数宣言はレビュー報告の件数と突き合わせられる | 層1 |
| A97 | :98 | 経緯: 2026-08-15 に群 α として書き直した。旧文は「対照を入れたか」しか問わず、「正しい位置に在るか」を問わなかった。追記ではなく書き直し | 退避 |
| A98 | :100 | 【規則】変異・陽性の対照を指定する前に、「測りたい性質が偽のとき、この変異は何を出すか」を 1 変異につき 1 行書く | 層1 |
| A99 | :100 | 真のときと同じ出力になるなら、その変異は証拠にならない | 層1 |
| A100 | :101 | 併せて当てる (a) production 編集かテスト編集か | 層1 |
| A101 | :101 | (b) 同じ関数の他の分岐に masking されないか | 層1 |
| A102 | :101 | (c) 他の変異と同一の編集に潰れないか | 層1 |
| A103 | :104 | なぜ: #206 — 陽性の対照 P-CLEAR は「入れた」を満たしたが、自明に赤くなる位置に在り `lib.rs:107` の配線を測らないままだった | 退避 |
| A104 | :105 | なぜ: #129 — テスト配列から `CliKind::Shell` を落とす変異は原理的に赤になり得ない | 退避 |
| A105 | :106 | なぜ: #130 — `gate.rs` 規則 4 の変異が内側の網羅 `match` に masking され必ず緑（advisor が dispatch 前に指摘、2 系統で確認） | 退避 |
| A106 | :107 | なぜ: #91 / #161 — 指定 2 変異が実装上同じ 1 編集になり、独立した 2 観測として報告された | 退避 |
| A107 | :108 | なぜ: #184 — `WHERE` 句だけ削り `params!` を残したため、意図した実行結果を見る前に赤になった | 退避 |
| A108 | :109 | なぜ: #193 — hooks 疎通の観測欄が `BelDetected` / `SilenceTimeout` 由来の 🟡/⚪ に到達でき、hooks が死んでいても合格した（`runtime_state.rs:149-150`） | 退避 |
| A109 | :111 | 観測点: `grep -n '偽のとき\|この変異が緑になる条件' <dispatch>` が 1 変異につき 1 行以上返ること | 層1 |
| A110 | :111 | 観測点: dispatch の変異一覧に「production 編集 / テスト編集」の別が書かれていること | 層1 |
| A111 | :113 | 見出し: 差分を機械解析するなら `--no-ext-diff` を付ける | 層1 |
| A112 | :115 | このリポジトリは `diff.external=difft` である | 層1 |
| A113 | :115 | 帰結: `git diff` はパッチではなく 2 カラムの表を描き、`^diff --git` / `^[-+]` の grep が静かに 0 を返す | 層1 |
| A114 | :113,:115 | 実測: stage5 で 4 回（台帳 #127 / #131 / #152 / #159。2026-08-14 stage5 ゲート） | 退避 |
| A115 | :115 | `scripts/review-package` の生成物も同じ形で壊れる | 層1 |
| A116 | :117 | `^diff --git` が 0 行なのは「差分が無い」ではなく「不合格（測れていない）」である | 層1 |
| A117 | :117 | 実例: team-lead が「新しい doc の断定は増えていない」と誤判定した | 退避 |
| A118 | :118 | 回避策を渡すときは、渡す前に自分で 1 回実行する | 層1 |
| A119 | :118 | 罠: `git --no-ext-diff diff` はオプション位置が誤りで `unknown option` になる | 層1 |
| A120 | :118 | 実例: 台帳 #128 — #127 の手当てとして逐語で配ったコマンドが、一度も実行されていなかった | 退避 |
| A121 | :119 | 観測点: 差分を解析するコマンドに `--no-ext-diff` が付いており、かつ dispatch へ入れる前に実行した記録が在ること | 層1 |
| A122 | :121 | 見出し: 断定は、宛先を問わず、偽になる観測を 1 つ示してから書く | 層1 |
| A123 | :123 | 機構: dispatch・中継・報告へ書く断定は、implementer の production コメントへそのまま流れる | 層1 |
| A124 | :123 | 実測: stage5 で 3 回（台帳 #116 / #154 / #167） | 退避 |
| A125 | :127 | 実例表 1: 制約の「理由」が未検証のまま `ci.yml` のコメントへ逐語転記され、測ると成立していなかった（#116） | 退避 |
| A126 | :128 | 実例表 2: 逐語指定した 2 assert が、捕まえるはずの変異に一度も当てられないまま渡された（#154） | 退避 |
| A127 | :128 | 指定は処方であり、処方は渡す前に当てる（`task-reviewer.md` の同義務と同じ） | 層1 |
| A128 | :129 | 実例表 3: parked 項目の裁定文に「族として閉じる手段が現行の技法に無い」と書いた（#167） | 退避 |
| A129 | :129 | 技法の不在も断定である。試作で反証を試みてから書く | 層1 |
| A130 | :131 | 【規則】性質を断定する文を書くなら、それが偽なら赤くなるもの（変異・試作・実測コマンド）を 1 つ添える | 層1 |
| A131 | :131 | 添えられないなら「未検証の予測」と明記する | 層1 |
| A132 | :132 | なぜ: 断定は指示に変換され、implementer の production コメントまで届く | 層1 |
| A133 | :132 | 理由は結論より速く、そして遠くまで伝播する | 層1 |
| A134 | :133 | 観測点: dispatch に書いた「理由」に、実測の出所か「未検証の予測」の明記があること | 層1 |
| A135 | :135 | 見出し: dispatch は定義ファイルと食い違ってはならない | 層1 |
| A136 | :137 | 実測: stage5 で 4 回、3 定義ファイルと正面から食い違った（2026-08-14 昇格。台帳 #135 / #139 / #153 / #163） | 退避 |
| A137 | :141 | 実例表 1: 「`定義の穴` 欄はタスク単位では出さない」⇔ dispatch「出せ」— PR 28 で 3 レビュー、PR #74 で 10 本中 1 本だけが欄を空にした（2 回目） | 退避 |
| A138 | :142 | 実例表 2: 「変異は 1 回に 1 箇所」⇔「2 箇所同時に当てよ」— 例外の扱い未定義のまま、レビュアーが自分で表を分けて記録した（2 回） | 退避 |
| A139 | :144 | 【規則】定義ファイルと違うことを要求したくなったら、dispatch に書かず、定義ファイル側へ例外を書かせる要求として team-lead へ上げる | 層1 |
| A140 | :144 | 帰結: 受け手は食い違いを BLOCKED で返すので、dispatch で押し切っても 1 往復増えるだけ | 層1 |
| A141 | :145 | なぜ: 受け手ごとに解釈が割れ、割れたことは割れた後でなければ分からない | 層1 |
| A142 | :145 | 10 本中 1 本だけが違う結果を出しても、その 1 本を読むまで誰も気づかない | 層1 |
| A143 | :146 | 観測点: 受け手から「dispatch と定義ファイルの食い違い」の BLOCKED が 0 件であること | 層1 |
| A144 | :147 | `定義の穴` 欄を観測点にしない — タスク単位レビューはその欄を出さないので、構造的に測れない | 層1 |
| A145 | :147 | この警告は受け手 3 ファイルと棚卸し表の群 V 行にも同じ形で書いてある（同期の宣言） | 層1 |
| A146 | :149 | 閾値表は受け手 3 ファイルと同一の表である | 層1 |
| A147 | :149 | 理由: 書き手側に無いと、書式の追加要求まで team-lead へエスカレートする | 層1 |
| A148 | :153 | 閾値 1: 定義ファイルが禁止していることを要求する → 食い違い。dispatch に書かず team-lead へ上げる | 層1 |
| A149 | :154 | 閾値 2: 定義ファイルが必須としていることを免除する → 食い違い。同上 | 層1 |
| A150 | :155 | 閾値 3: 定義の無い追加項目・追加書式の要求 → 食い違いではない。書いてよい（例: `STATUS:` に加えて `VERDICT:` も置かせる） | 層1 |
| A151 | :156 | 閾値 4: 定義ファイルより厳しい条件 → 食い違いではない。書いてよい | 層1 |
| A152 | :158 | 行 1・2 は行 3・4 に優先する | 層1 |
| A153 | :158 | 「打つ変異が増える」は行 4 にも読めるが、受け手が禁止されている形なら行 1 が先に当たる | 層1 |
| A154 | :159 | 既知の例外: 「変異は 1 回に 1 箇所」に対する「陽性の対照として production とテストの 2 箇所同時」は、3 条件つきで認められた例外 | 層1 |
| A155 | :159 | 3 条件 = 箇所ごとの確認 / 表を分ける / 単独の結果も併記。受け手 3 ファイルに同じ 3 条件が書いてある | 層1 |
| A156 | :159 | dispatch では条件のほうを引くこと | 層1 |
| A157 | :159 | 出典: 2026-08-14。台帳 #139 / #153 | 退避 |
| A158 | :160 | 本表の形の列は受け手 3 ファイルと同一、判定列は役割に合わせた読み替え（受け手「BLOCKED」/ 書き手「team-lead へ上げる」） | 層1 |
| A159 | :160 | 形の列だけを同期し、判定列を機械的に揃えない — 揃えると読み替えが壊れる | 層1 |
| A160 | :160 | 片方の形を直したらもう片方も直す | 層1 |
| A161 | :162 | 見出し: 規則を書いた便には、その規則を自分で当てる | 層1 |
| A162 | :162 | 本節が群 U の正典である（群 V の裏側） | 層1 |
| A163 | :164 | 機構: 規則を書く行為が「もう効いている」という感覚を作り、書いた当人が自分の出力へ規則を当てる工程を飛ばす | 層1 |
| A164 | :164 | 実測: stage5 で 5 回（台帳 #124 / #125 / #128 / #142 / #144。team-lead 側でも同じ形） | 退避 |
| A165 | :168 | 破り方 1: 「同じ編集にならないことを確かめる」と書いた dispatch 自身の M-K と MTAIL が同一の 1 編集だった（#161） | 退避 |
| A166 | :169 | 破り方 2: #127 の手当てとして配ったコマンドが、一度も実行されていなかった（#128） | 退避 |
| A167 | :170 | 破り方 3: 節を 2 度改名した PR が、同じ PR が足した `SKILL.md` の行を旧名の引用のまま残した（#124） | 退避 |
| A168 | :172 | 【規則】規則・指摘・手当てを書いた便を送る前に、その規則をその便自身へ 1 回当てる | 層1 |
| A169 | :172 | 例: 「数を転記しない」と書いた便に数が入っていないか / 変異リストが実際に相異なるか / 渡したコマンドを 1 回実行したか | 層1 |
| A170 | :174 | 手当ては「気をつける」ではなく、打つコマンドの形にする | 層1 |
| A171 | :174 | 帰結: 警告文だけで閉じた項目は、次の便で同じ形を打たれる | 層1 |
| A172 | :175 | なぜ: 群 U は 5 ファイル全部に在るのに、両側で同日再発した（2026-08-14。#196 / #201。#196 の行自身が「警告文では閉じない。手当てはコマンドの形」と逐語で書いている） | 退避 |
| A173 | :176 | 観測点: 罠を記録した報告に、それを回避する具体コマンドが 1 行あること（例: `git -C <リポジトリ絶対パス> diff --no-ext-diff -- <path>`） | 層1 |
| A174 | :176 | 観測点: 「注意する」「気をつける」で終わっている手当てが 0 件であること | 層1 |
| A175 | :178 | 帰結: 破りは同じ文書の中で起きる。別の便へ伝播する前に、その場で読み返せば捕まる | 層1 |
| A176 | :179 | 観測点: 規則を書いた便に「その規則を自分の出力へ当てた結果」が 1 行あること | 層1 |
| A177 | :179 | 「規則を読んだ」ではなく「当てた」である | 層1 |

## パート A の判定して外した候補（規範項目に数えなかったもの）

- **構造見出しそのもの 4 件**（`## 自分でやらないこと` :20 / `## implementer の使い分け` :26 / `## dispatch の作り方` :39 と表ヘッダ行）: 節の名前であり、内容は配下の各項目が持つ。`## 開始手順（この順で）` :12 だけは「この順で」が順序義務を述べるので A9 として数えた。
- **見出し内の群ラベル・日付・「書き直した」注記 5 件**（:51「（群 H）」/ :75「（群 G）」/ :81「（群 K）」/ :113「（2026-08-14、stage5 ゲート）」/ :121「（群 E。2026-08-14 に書き直した）」/ :135「（群 V）」/ :162「（群 V の裏側…）」のラベル部分）: 書式メタ。日付は各節の経緯行（A64 / A93 / A97 / A114 / A136 / A157 / A164 / A172）が退避として引き受ける。ただし :162 の「本節が群 U の正典」だけは他ファイルからの参照を受ける宣言なので A162 として数えた。
- **:63 の見出し句「【dispatch 前に必ず行う 3 つ】」**: 配下 3 項目（A50 / A51 / A53)が内容を持つ。

## この範囲の注意（統合時に効くもの）

- **A66 / A80 / A145 / A146 / A155 / A158**: 受け手 3 ファイル（`rust-implementer.md` / `web-implementer.md` / `task-reviewer.md`）との**同期義務を宣言する行**。書き直しで文言を変えるときは受け手側の表と「形の列」が一致し続けることを検算する（A159 が同期の仕方自体を定める）。
- **A88–A90（:92）と A98–A110（:98-111）は群 K 節の中に群 α の書き直しブロックが入れ子になった形**。新版では 1 つの節（変異の指定）として統合して書いてよいが、台帳上は別項目として全件挙げた。
- 群ラベル → 節名ポインタの張り替え（README-rewrite 引き継ぎ 2）で、この範囲の該当は群 H / G / K / E / V / U / α / β〜μ の各節名。

**件数サマリ: 総数 177 / 層1 138 / 層2:records 0 / 退避 39 / 削除 0**

### パート B（:181-424）

# 突き合わせ台帳 パート B（lane-controller.md :181-424）

対象: `.claude/agents/lane-controller.md` の :181〜:424（群 β〜μ・群 Q 拡張・並列起動禁止・起床手段・対処 1・対処 1 の続き・対処 2・worktree 自動削除）。
粒度は「規則を 1 つ述べている単位」（文・節）。理由・帰結の文も 1 件に数えた（文末の様相で絞っていない）。
code fence の中身は論理的なまとまりにつき 1 行で引いた（fence のバイト保存は `check-rewrite.py` 検査 11 が守る）。
行き先: `層1` / `層2:records` / `退避`（`.claude/references/lane-controller-cases.md`）/ `削除(理由)`。

| # | 旧版 | 規則の逐語コア | 行き先 |
|---|---|---|---|
| B1 | :181 | 見出し: 証拠に借りたコマンドの出力が、2 状態を区別するか | 層1 |
| B2 | :181 | 見出し装飾（群 β。2026-08-15 に昇格。11 行） | 退避 |
| B3 | :183 | 群 K / 群 α は「自分で設計した観測」、本節は既存コマンド・指標を借りて証拠に使う話 —— 機構が違う | 層1 |
| B4 | :185 | 【規則】借りた出力・指標を証拠に使う前に「不合格のとき何が見えるか」を 1 行書く | 層1 |
| B5 | :185 | 書けないコマンドは証拠にしない | 層1 |
| B6 | :186 | 「終了ステータス 0」「出力が空」「件数が同じ」はそれ自体では成立の証拠にならない | 層1 |
| B7 | :188 | `<コマンド> \| tail -n` の終了コードは `tail` のもの。検証コマンドをパイプせず、単独で走らせて `$?` を読む | 層2:records |
| B8 | :188 | 実測（2026-08-15 に team-lead がゲートで踏み、`cargo test` が 8 件しか見えなかった。実際は 783） | 退避 |
| B9 | :189 | `cargo test --workspace` に `--no-fail-fast` を付ける —— 先頭 target の赤で残り 3 target が黙って飛ぶ | 層2:records |
| B10 | :189 | (#40) | 退避 |
| B11 | :190 | `gh pr merge --delete-branch` が worktree から `fatal: 'main' is already used by worktree` を返してもマージ成功のことがある。`gh pr view <n> --json state,mergeCommit` と `git ls-remote --heads origin <br>` で確かめる | 層2:records |
| B12 | :190 | (#118 / #191) | 退避 |
| B13 | :191 | `git status --porcelain` は `.gitignore` 配下の成果物を見ない。`git ls-files -s` はインデックスのモードを返すので worktree の実行ビット喪失を見ない | 層2:records |
| B14 | :191 | (#113 / #169) | 退避 |
| B15 | :192 | 後片付けの glob には `(N)` か `2>/dev/null` を付ける —— zsh の no-match はコマンド全体を中断し、先行パターンの削除が実行されないまま終了ステータスだけが返る | 層2:records |
| B16 | :192 | (#114) | 退避 |
| B17 | :193 | ローカルの緑は CI の緑ではない | 層2:records |
| B18 | :193 | 実測（worktree に残ったバイナリで全緑になり、CI だけが `resource path … doesn't exist` で落ちた。#115） | 退避（帰結の文は 層2:records にも生存） |
| B19 | :195 | 観測点: 証拠に使った各コマンドに「不合格ならこう見える」の 1 行が報告に在ること | 層2:records |
| B20 | :197 | 見出し: 処方・訂正が閉じる族の境界を書く | 層1 |
| B21 | :197 | 見出し装飾（群 γ。2026-08-15 に昇格。10 行） | 退避 |
| B22 | :199 | 【規則】処方・訂正指示・値の差し替えを渡す前に「この処方が閉じる族はどこまでか。同じ族の残りは何か」を 1 行書く | 層1 |
| B23 | :200 | 「1 語だけ」「この N 行だけ」と絞るときは、絞った外側に同型が無いことを `grep` で測ってから絞る | 層1 |
| B24 | :201 | 値を差し替えるときは、その値と同じ章が定めている前提（測り方・母数・単位）も一緒に持ってくる | 層1 |
| B25 | :202 | なぜ: #132 / #69 / #140 / #72 / #117 / #155 / #156 / #143 / #3 / #1 の 10 実例（各詳細付き） | 退避 |
| B26 | :203 | 観測点: 「閉じる族」「同型の残り」の 1 行 / 絞った語の `grep -c` 併記 / 差し替えコミット本文に「測り方」の逐語 | 層2:records |
| B27 | :205 | 見出し: 同じ作業に実体は 1 体だけ | 層1 |
| B28 | :205 | 見出し装飾（群 ε。2026-08-15 に昇格。3 行） | 退避 |
| B29 | :207 | 【規則】implementer / reviewer を起動する前に、その作業に既に就いている実体を数える | 層1 |
| B30 | :207 | 並列起動禁止（既存規則）だけでなく、引き継ぎの前後で二重に就く形も同じ穴 | 層1 |
| B31 | :208 | 引き継ぎ報告を返して終了したレーンへ `SendMessage` を送ると再稼働する。追加の指示は後継へ送る | 層1 |
| B32 | :209 | なぜ: #185 / #205 / #7（各詳細付き） | 退避 |
| B33 | :210 | 観測点: 起動指示に先行実体の停止を確かめた出力が 1 行あること | 層2:records |
| B34 | :212 | 見出し: 裁定は `SendMessage` を待たず、契約ファイルの末尾を自分で読む | 層1 |
| B35 | :212 | 見出し装飾（群 ζ。2026-08-15 に昇格。3 行） | 退避 |
| B36 | :214 | 実測（「裁定は届いていない」と判断して止まる形が 3 回） | 退避 |
| B37 | :214 | 裁定が着地する場所（契約ファイル）と適用者が見る場所（`SendMessage` / ledger）が別で、繋ぐ工程がどの定義にも無かった | 層1 |
| B38 | :216 | 【規則】契約変更要求を上げたら `SendMessage` を待たず `tail -100 00-contracts.md`（または `grep -n '^### <新章番号>'`）を自分で打つ | 層1 |
| B39 | :217 | 契約の受領は ledger へ 1 行記帳する。無いと次の読み手が同じ件を二重に上げる | 層1 |
| B40 | :218 | なぜ: #147 / #165 / #203（各詳細付き） | 退避 |
| B41 | :219 | 観測点: `grep -c '§[0-9]*' <ledger>` が着地章数以上 / 「末尾を自分で読んだ」記録が在ること | 層2:records |
| B42 | :221 | 見出し: 一覧に載せないと判断したものは、件数と理由を書く | 層1 |
| B43 | :221 | 見出し装飾（群 η。2026-08-15 に昇格。3 行） | 退避 |
| B44 | :223 | 【規則】「判定対象外」「射程外」「修正禁止」と書くときは件数を必ず添える。空欄で母数を減らさない | 層1 |
| B45 | :224 | なぜ: #11 / #134 / #166（各詳細付き） | 退避 |
| B46 | :225 | 観測点: deferred minor 行の件数と PR 単位レビューの Minor 件数の和が一致すること | 層2:records |
| B47 | :227 | 見出し: 工程を定義したら、産物の書き先・書き手・時点を同じ場所に書く | 層1 |
| B48 | :227 | 見出し装飾（群 ι。2026-08-15 に昇格。3 行） | 退避 |
| B49 | :229 | 「後で実施する」節との分業 —— あちらは「書いた物が残るか」、本節は「誰がいつどこへ書くかが決まっているか」 | 層1 |
| B50 | :231 | 【規則】工程を定義・宣言したら、産物を「誰が・いつ・どこへ」書くかを同じ場所に書く | 層1 |
| B51 | :232 | なぜ: #33 / #162 / #188（各詳細付き） | 退避 |
| B52 | :233 | 観測点: 宣言した工程それぞれに産物の書き先が 1 行併記されていること | 層2:records |
| B53 | :235 | 見出し: 稼働中の他者が使っている状態へ書かない | 層1 |
| B54 | :235 | 見出し装飾（群 θ。2026-08-15 に昇格。2 行） | 退避 |
| B55 | :237 | 【規則】ビルド成果物・作業ツリー・ブランチを書き換える前に、それを参照している他の実体を測る | 層1 |
| B56 | :238 | `git status --porcelain` はこれを構造的に見ない | 層1 |
| B57 | :239 | なぜ: #112 / #179（各詳細付き。「汚染なしと判定できたのは mtime を測ったからで偶然」を含む） | 退避 |
| B58 | :240 | 観測点: 上書きする作業の報告に `lsof <パス>` か `pgrep -f kamux` の出力が 1 行あること | 層2:records |
| B59 | :242 | 見出し: 参照は本文照合してから書く | 層1 |
| B60 | :242 | 見出し装飾（群 κ。2026-08-15 に昇格。2 行） | 退避 |
| B61 | :244 | 【規則】契約の章番号・ファイルの行番号を書いたら、その節を開いて主張の逐語を 1 行引く | 層1 |
| B62 | :245 | 「旧い文字列が `grep` で 0 件」は置換が起きた証拠であって、置換先が正しい証拠ではない | 層1 |
| B63 | :246 | なぜ: #149 / #150（rev1 の手当てが rev2 の穴を作った詳細付き） | 退避 |
| B64 | :247 | 観測点: 参照 1 件につき `grep -n '<逐語>' 00-contracts.md` の出力 1 行 / 「旧文字列 0 件」だけの確認が 0 件 | 層2:records |
| B65 | :249 | 見出し: 原本が在る値を手で写さない | 層1 |
| B66 | :249 | 見出し装飾（群 λ。2026-08-15 に昇格。2 行） | 退避 |
| B67 | :251 | 【規則】原本が実在する値・規則を手で写さない。写すなら原本の所在（`<ファイル>:<行>`）を併記する | 層1 |
| B68 | :252 | なぜ: #4 / #110（`build_hook_settings()` の第 2 の正典・4 検査は 4 つとも合格していた詳細付き） | 退避 |
| B69 | :253 | 観測点: 値を写した行に `<ファイル>:<行>` が併記されていること | 層2:records |
| B70 | :255 | 見出し: 列挙・件数は機械で導出して突き合わせる | 層1 |
| B71 | :255 | 見出し装飾（群 μ。2026-08-15 に昇格。3 行） | 退避 |
| B72 | :257 | 【規則】列挙・件数が機械で導出できるなら導出して突き合わせる。「N 件」宣言は実体の件数と突き合わせる | 層1 |
| B73 | :258 | なぜ: #197 / #199 / #200（行数検算は 2 回とも合格を返した、を含む） | 退避 |
| B74 | :259 | 観測点: 「N 件」宣言の直後に実体を数えたコマンドの出力が併記されていること | 層2:records |
| B75 | :261 | 見出し: 数値には単位・時点・母数の定義を添える | 層1 |
| B76 | :261 | 見出し装飾（群 Q の射程を広げた。2026-08-15。4 行） | 退避 |
| B77 | :263 | 経緯実測（群 Q は報告ファイルにしか掛かっていなかった。`74f9a73` で `grep -c REPORT_WRITTEN_AT` = 0） | 退避 |
| B78 | :265 | 【規則】他者が読む恒久記録（PR 本文・production の doc・dispatch・引き継ぎ報告）の数値に単位・時点（sha または日付）・母数の定義を添える | 層1 |
| B79 | :266 | sha を引くときは `git merge-base --is-ancestor <sha> origin/main` が真になるものだけを引く | 層1 |
| B80 | :267 | なぜ: #122 / #186 / #189 / #190（各詳細付き） | 退避 |
| B81 | :268 | 観測点: dispatch と報告の数値に単位 / 引用 sha に `merge-base --is-ancestor` が 0 を返すこと | 層2:records |
| B82 | :270 | 実装 implementer を並列に起動しない。1 タスクにつき implementer は 1 人で、レーン内は必ず直列 | 層1 |
| B83 | :272 | 「作業ツリーを分ければ並列にできる」と考えない —— 1 レーン = 1 worktree であり、あなたが worktree を増やしてはならない | 層1 |
| B84 | :272 | 分けたところで、統合時に同じ衝突が一度に来る | 層1 |
| B85 | :274 | 実例: 2026-08-05、M2-1 で Task 4/5 並行 → 同じ `runtime_state.rs` を両者が編集し作業が消失（復旧に一時 worktree と `git update-ref`） | 退避 |
| B86 | :275 | 消失しなくてもレビュー範囲が汚染される —— 未コミットの変更が同じファイルに乗ると、レビュアーが読む「差分」が実物と食い違う | 層1 ⚠️要裁定: 指示は退避だが帰結・理由（並列禁止の第 2 の根拠）のため層1 と仮判定 |
| B87 | :275 | 実例詳細（Task 4 の再レビュー vs Task 5 の未コミット 184 行） | 退避 |
| B88 | :276 | 実例: M1-2 では二重起動で両方が同じタスクの実装を完了する事故 | 退避 |
| B89 | :278 | 見出し: implementer の起動と、あなたの起床手段 | 層1 |
| B90 | :278 | 見出し内実績（2026-08-05 に 4 回・2026-08-06 に 3 回止まり、計 5 時間以上を失った） | 退避 |
| B91 | :280 | 見出し+核心: `Agent` は `run_in_background: false` でも非同期で返る | 層1 |
| B92 | :282 | 「`false` を明示すれば同期的に待てる」は誤りである | 層1 |
| B93 | :282 | 実測出典（M1-4 は明示していたのに 2 度止まった。`TEAM-LEAD-RULINGS.md` §11.6 / §14。「当時の書き先であり恒久的な引用先ではない」注記） | 退避 |
| B94 | :284 | implementer を起動した直後に必ず一度終了する | 層1 |
| B95 | :285 | 残る起床手段は `Monitor`（完了イベント）だけになり、1〜2 時間遅れて届く事象が複数回起きた | 層1 |
| B96 | :287 | `SendMessage` による再開もまったく同じ経路。初回起動だけの問題ではない | 層1 |
| B97 | :289 | 見出し+補足: dispatch と同じターンで監視ループを張る。これだけでは足りず「対処 1 の続き」が本体 | 層1 |
| B98 | :291 | `Agent` 呼び出しと同じ応答の中で `Bash`（`run_in_background: true`）に投げる。ループが抜けたときに起こされる | 層1 |
| B99 | :293 | ループを張るのは半分である。起きたあとに何をするかを決めるのは「対処 1 の続き」 | 層1 |
| B100 | :293 | 実測（M3-1 ではループが抜けて通知も届いたのに 3 回止まった。最大 82 分） | 退避 |
| B101 | :296-299 | fence: `cd <worktree 絶対パス>` / `ROUND` はあなたが発行（`<フェーズ>-t<タスク>-r<ラウンド>`）/ `R=<報告ファイル絶対パス>` / `END` 上限 58 分・必ず有限で抜ける | 層1 |
| B102 | :300-302 | fence: `until grep -q "^STATUS:.*(round: $ROUND)" "$R" … \|\| [ $SECONDS -ge $END ]; do sleep 60; done` | 層1 |
| B103 | :303-305 | fence: 出力 3 点（`grep -n '^STATUS:' \| tail -2` / `git log --oneline -3` / `git status --short`） | 層1 |
| B104 | :308 | 待つ対象は「新しいコミット」ではない。コミットは作業消失を防ぐためのもので完了信号ではない | 層1 |
| B105 | :308 | 経緯実測（2026-08-08 に「先にコミット → 変異 → 復元」を配った / コミット待ちで起きて変異検証前のコードをレビューに回しかけた。M3-2） | 退避 |
| B106 | :309 | 待つ対象は「当該ラウンド固有のトークンを持つ完了マーカ」。`task-reviewer` はコミットを作らないのでレビュー待ちにはこれしか無い。実装完了もレビュー完了も同じ形にする | 層1 |
| B107 | :310 | レポートファイルの存在を条件にしない —— 骨子を置いた瞬間に真になりループは 1 秒で抜ける。「まだ始まっていない」と区別がつかない | 層1 |
| B108 | :311 | 素の `grep -q '^STATUS:'` では足りない（追記式なので旧ラウンドのマーカに一致）。`tail -1` でも足りない（旧マーカは新ラウンド開始まで常に最終行）。マーカにラウンド固有のトークンを載せる | 層1 |
| B109 | :311 | 実測（M3-2 の advisor が発見 / M2-2 が `task-5-report.md` で実測） | 退避 |
| B110 | :313 | 判定は「位置」ではなく「ラウンド固有のトークン」。dispatch のたびに一意のトークンを決め、逐語で渡す | 層1 |
| B111 | :315-317 | dispatch へ逐語で入れる文（`STATUS: <…> (round: <トークン>)` の 1 行・発行者は lane-controller・すべて終わり報告を書き終えてから書く） | 層1 |
| B112 | :319-328 | 検算表（2026-08-10。6 件、罠つき入力 2 つ。`sed` で述語を抜き出して当てた） | 退避 |
| B113 | :330-337 | 位置判定を捨てた経緯（実ファイル 2 本で両方偽・位置判定の表・「合格が必ず不合格を返す」台帳 #99 同型・群 A の 5 停止を閉じる規則が毎フェーズ必ず停止に化けた） | 退避 |
| B114 | :339 | なぜトークンなら足りるか —— トークンはラウンド開始までファイルのどこにも存在しえないので、古いマーカに原理的に一致しない。識別子を与えれば位置の条件が要らない | 層1 |
| B115 | :340 | トークンは「書いた後も作業が続く」ことは防がない。それは (2) の完了通知が担う。マーカは代替であって権威ではない | 層1 |
| B116 | :341 | トークンは毎ラウンド変える。使い回すと前ラウンドの行に一致して 1 秒で抜ける | 層1 |
| B117 | :342 | 上限 58 分を必ず入れる。無限ループは起床手段にならない | 層1 |
| B118 | :343 | 出力に `git log --oneline -3` と `git status --short` を必ず含める —— 起きた瞬間に切り分けでき、無いと 1 往復増える | 層1 |
| B119 | :344 | 上限で抜けたら、コミットも report も無い場合に限り同じループをもう一度張る。2 回続けて空なら BLOCKED として team-lead に上げる | 層1 |
| B120 | :345 | 契約 §0 の「ポーリングループ禁止」は製品コードの制約で、レーン運用の監視ループは射程外。これを理由に本節を消さない | 層1 |
| B121 | :347 | 【一般則】不在（無応答・停止）を検知する規則は、能動的な起床機構を同時に置いて初めて発火する | 層1 |
| B122 | :347 | 装飾（群 ξ。2026-08-15 に昇格。1 行・例外） | 退避 |
| B123 | :348 | 待つ対象は implementer だけではない —— CI 待ち・マージ待ち・裁定待ち・他レーン完了待ちも同じ形。「待つ」と決めた瞬間に同じ応答の中で起床条件を 1 つ張る | 層1 |
| B124 | :349 | 実例（2026-08-15、PR #80 の停止に 2 時間 14 分気づかず。起床を通知に依存させると「通知が来なくなる」を構造的に検出できない） | 退避 |
| B125 | :349 | 正典ポインタ: `SKILL.md`「事象が起きないことを検知する規則は、能動的な起床機構を同時に置く」 | 層1 |
| B126 | :350 | 沈黙を成功と読まない。待ち条件には進捗だけでなく終端状態（`MERGED:` / `CLOSED:` / `BLOCKED`）も入れる | 層1 |
| B127 | :351 | 観測点: 「待つ」と書いた便に `Monitor` の登録結果か待ちループの投入が 1 行あること。「様子を見る」とだけ書いた便は不合格 | 層1 |
| B128 | :353 | 見出し: 起きたら、同じ応答の中で次の一手を打つ | 層1 |
| B129 | :353 | 見出し装飾（2026-08-10 に 5 回目を受けて書き直した。群 A） | 退避 |
| B130 | :355 | 経緯（旧版は規律だけで書かれ 5 回とも効かなかった。機構の問題だった） | 層1 |
| B131 | :357-358 | 実測（stage3 で 3 回・stage4 で 2 回、計 5 回とも実装完了 → レビュー dispatch の遷移点で停止） | 退避 |
| B132 | :358 | 機構: `task-reviewer` はコミットを作らないので、「新しいコミット」という起床条件はレビュー待ちでは構造的に存在しない | 層1 |
| B133 | :359 | 再発防止の宣言では閉じない —— 宣言した直後の区切りで再発した | 層1 |
| B134 | :361 | したがって規則は 3 段になる | 層1 |
| B135 | :363 | (1) 起床条件は「当該ラウンドのトークンを持つ完了マーカが現れること」に一本化する | 層1 |
| B136 | :365 | 実装完了もレビュー完了も同じ形。ループの `grep` 1 本で判定し位置は見ない。コミットは完了信号ではない | 層1 |
| B137 | :367 | トークンを書く義務は受け手の定義ファイル 3 本が持つ。dispatch へ逐語で入れるのは念押しで、それだけでは義務にならない —— dispatch はタスク 1 本で消える（出典: `kamux-promotion/SKILL.md`「昇格するかの判断」） | 層1 |
| B138 | :369 | (2) マーカは「エージェントが書いた」ことしか保証しない。「終わった」ことは保証しない | 層1 |
| B139 | :371-374 | 機構表: 完了通知（team-lead 中継）= 権威（停止時にしか出ない）/ 報告ファイルのマーカ = 代替・保険（書いた後も作業は続きうる） | 層1 |
| B140 | :376 | 禁止を dispatch に逐語で書いたうえで起きている —— 禁止という手段が効いていない | 層1 |
| B141 | :376 | 実測（stage4 でマーカ後の作業継続が 4 回。台帳 #51 / #64） | 退避 |
| B142 | :378-384 | 実害 4 件表（`APPROVED` 4 分後の反転 / 古い差分の受け渡し / 2 体並列 / PR 本文の数値ずれ） | 退避 |
| B143 | :385 | 【規則】不可逆に近い工程（PR 作成 / 次の implementer 起動 / レビューへの差分受け渡し / マージ）の前は、完了マーカではなく team-lead の完了通知の中継を待つ | 層1 |
| B144 | :386 | 時間による緩和（mtime が N 秒安定）は解ではない —— それより長い反転がありうるので、時間ではなく完了通知を権威にする | 層1 |
| B145 | :386 | 実測（反転は 4 分後で 90 秒の安定判定を素通り） | 退避 |
| B146 | :387 | 「レビューが終わった」ではなく「誰も居ない」を見る。連言条件は工程の完了を判定するが、エージェントの終了は判定しない | 層1 |
| B147 | :387 | (台帳 #92 / #93) | 退避 |
| B148 | :388 | 稼働中のツリーには、コミットも復元も掛けない | 層1 |
| B149 | :388 | 実測（台帳 #65。`git restore` がベースライン測定を汚染。既知の注意はコミット側の片方向のみで復元側が抜けていた） | 退避（規則の文は 層1 にも生存） |
| B150 | :389 | 振動する信号（作業ツリーの clean）を 1 回のスナップショットで読んで完了と断定しない。単調に増える信号（`^STATUS:` マーカ）を連言で含める | 層1 |
| B151 | :389 | 実測（台帳 #88。谷と山で観測が割れ、どちらのスナップショットも正しかった） | 退避 |
| B152 | :391 | (3) 起きたら、同じ応答の中で次の一手を打つ | 層1 |
| B153 | :393-397 | 分岐表 3 行: マーカ在り → その場で次工程を dispatch（不可逆なら (2) の中継待ち）/ コミットのみ → まだ動いている・再ループ / どちらも無 → 2 回続けて空なら BLOCKED | 層1 |
| B154 | :399 | 「確認しました」「次はレビューです」だけで応答を終えない。次の `Agent` 呼び出しか `Bash` のループを同じ応答の中に必ず含める | 層1 |
| B155 | :400 | `Agent` のツール結果を読んでから応答を終え、agentId の存在を確認点にする。`name` を渡すと dispatch が丸ごと失敗して待ちループだけが走り、「異常終了」と同じ観測になる。`name` を渡さない | 層1 |
| B156 | :400 | 実測（3 レーンとも踏んだ） | 退避 |
| B157 | :401 | 待ちループに「生存の陽性証拠」を持たせる —— 上限で抜けたとき「無音」と「動いていた形跡」を区別できる | 層1 |
| B158 | :402 | 【判定基準・群 α】本ブロックが正典であり、`SKILL.md` はここを指すだけで写さない | 層1 |
| B159 | :402 | 装飾（2026-08-15 に明文化） | 退避 |
| B160 | :403 | 待ち条件は次の 3 つをすべて満たすこと。1 つでも欠けたら不合格 | 層1 |
| B161 | :405 | 条件 1: 系統が 2 つ以上。同一アーティファクトへの複数条件は 1 系統と数える（mtime と終端行は 1 系統） | 層1 |
| B162 | :406 | 条件 2: 少なくとも 1 系統は相手の成果物と別のアーティファクト由来の「生存の陽性証拠」。成果物だけでは「まだ書き始めていない」と「死んだ」が同じ観測になる | 層1 |
| B163 | :407 | 条件 3: 終端状態を成功と失敗の両方の形で含める。沈黙を成功と読まない | 層1 |
| B164 | :409 | 実測（台帳 #230。mtime 1 点の `Monitor` が 10 分で誤爆。「存在を条件にしない」は存在の側だけの警告で mtime 側が抜けていた） | 退避 |
| B165 | :410 | 条件 1 に「同一アーティファクトは 1 系統」が要る理由 —— 件数だけを課すと同じファイルへの 2 条件で満たせ、#230 の機構がそのまま通る | 層1 |
| B166 | :410 | 実測（PR #88 の r3 レビューがフィクスチャで実測した） | 退避 |
| B167 | :411 | タスク境界では 20 分で見に行く | 層1 |
| B168 | :411 | `SKILL.md` の「1 時間」との分業 —— あちらは停止と断じてよい上限、こちらは見に行く契機（対応表は `SKILL.md` 側）。20 分で見た結果を実測で確定させるので 1 時間を待つことにはならない | 層1 |
| B169 | :411 | 実測（stage4 の 2 件は 18 分と 23 分で検出した） | 退避 |
| B170 | :413 | 見出し+規則: 修正ラウンドは `SendMessage` ではなく新しい `Agent` の同期起動で回す | 層1 |
| B171 | :415 | `SendMessage` はツール仕様上つねにバックグラウンド実行になる | 層1 |
| B172 | :415 | 新しい implementer にレポートファイルのパスを渡す。report が記憶を担うので文脈は失われない | 層1 |
| B173 | :417 | 見出し: worktree は「コミット 0 件 + レーン終了」でハーネスに自動削除される | 層1 |
| B174 | :419 | 真因（起動直後に必ず一度終了する）と合わさるので、コミットが 1 つも無い初回タスクの最中は削除条件に必ず当たる | 層1 |
| B175 | :419 | 実例（M1-2 と M2-1 で実際に消えた） | 退避 |
| B176 | :421 | `isolation: "worktree"` に頼らず、team-lead が `git worktree add` で作った作業ディレクトリ（ハーネス管理外）を使う。パスは起動プロンプトで渡される | 層1 |
| B177 | :422 | 各タスクの完了ごとに必ずコミットする。削除条件を外す唯一の手段でもある | 層1 |
| B178 | :423 | 全コマンドを `cd <worktree の絶対パス> && ...` の形にする。ブランチ名とテスト件数の両方を見てから緑と判断する | 層1 |
| B179 | :423 | 実例（M1-3 でシェルの cwd が `main` へ戻り、ゲート 1 回分が偽の緑を出した） | 退避 |

## パート B の判定して外した候補

- 0 件（この範囲で「読者の行動を変える文」から外した文は無い。装飾・実測・実例はすべて「退避」行として母数に入れた）。
- ⚠️要裁定 1 件: B86（:275 のレビュー範囲汚染の帰結）。指示の割り付けでは実例 3 bullets ごと退避だったが、並列禁止の第 2 の独立した根拠（帰結）であり README-rewrite「欠落はいつも理由・帰結の側で起きる」に該当するため、機構文を層1・実例詳細（B87）を退避に分けた。

## パート B の件数サマリ

総数 179 / 層1 106 / 層2:records 17 / 退避 56 / 削除 0（修正ラウンド 1 で B130 を退避から層1 へ訂正した後の値）

### パート C（:425-855）

# 突き合わせ台帳 パート C（lane-controller.md :425-856）

対象: `.claude/agents/lane-controller.md` の 425〜855 行（`ea7dc7f` = origin/main 時点）。

**拘束（この範囲に固有）**: 契約 §32.2 / §101.2 が「マージのコマンドブロックは `.claude/agents/lane-controller.md` にコピーされている実体。差し替えは team-lead が行う（§101.8）」とパスを名指しでピンしている（00-contracts.md:2648, :7559, :14159）。**したがってマージ節の bash ブロック（検証 A〜F / P1 / P3）と 7 条件表は層1 から動かさない。** ブロック内の操作的なコマンド行は 1 バイトも変えない。code fence の中身のバイト保存は check-rewrite.py 検査 11 が守るため、fence 内は 1 コマンド（論理的まとまり）につき 1 行で数える。

**既知の未適用**: 契約 §108.5 の「検証 C へ §108.2 ブロックを入れ『1 つ』を落とす」処置は本ファイルに未適用のまま（team-lead 持ち）。本書き直しでは適用しない（内容を変えないため）。PR 本文に明示する。

行き先: `層1` / `層2:completion`（`.claude/references/lane-controller-completion.md`）/ `層2:handoff`（`.claude/references/lane-controller-handoff.md`）/ `退避`（`.claude/references/lane-controller-cases.md`）/ `削除(理由)`。

| # | 旧版 | 規則の逐語コア | 行き先 |
|---|---|---|---|
| C1 | :427 | implementer が「契約に無い」と言ってきたら、あなたの判断で契約を変えてはならない | 層1 |
| C2 | :427 | 契約変更要求は所定の形で team-lead に上げ、確定するまでそのタスクを止める | 層1 |
| C3 | :429-435 | 契約変更要求テンプレ（フェーズ・タスク / 不足している内容 / 既存のどの章では表現できないか / implementer の提案 / あなたの意見） | 層1 |
| C4 | :439 | フェーズは複数の PR に分かれる。区切りは契約 27.4 章の表が正典であり、あなたが勝手に決めない | 層1 |
| C5 | :439 | 1 PR = 4〜7 タスク、全 33 本 | 層1 |
| C6 | :441 | 区切りのタスクまで終わったら（各タスクは task-reviewer 済み）PR 作成手順を行う | 層1 |
| C7 | :443-447 | fence: `git push -u origin <契約 §27.4 のブランチ名>` / `gh pr create --fill --base <main または直前の PR のブランチ>` / `gh pr checks --watch` | 層1 |
| C8 | :449 | CI が赤のまま次の PR に進まない | 層1 |
| C9 | :449 | 理由: 赤を積み上げると原因の切り分けができなくなる | 層1 |
| C10 | :450 | CI の失敗を自分で直さない。該当タスクの implementer を `SendMessage` で再開させ、失敗ログを渡す（タスクの修正ループと同じ規律） | 層1 |
| C11 | :451 | 同じ原因で 3 回連続して赤になったら BLOCKED として team-lead に上げる。環境差の可能性がある | 層1 |
| C12 | :455 | あなたがマージする。ただし 7 条件をすべて満たしたときだけ | 層1 |
| C13 | :459 | 条件 1: CI がグリーン。`gh pr checks <n>` が全項目 pass。単体テスト・E2E・lint はこの中で走る。ローカルの緑は条件にならない | 層1 |
| C14 | :460 | 条件 2: 全タスクのレビュー解消。ledger に `Task <N>: complete`。`parked` があるなら裁定文が書かれていること | 層1 |
| C15 | :461 | 条件 3: PR 単位レビュー承認。差分全体に task-reviewer を 1 回。タスク単位では見えない齟齬を捕まえる | 層1 |
| C16 | :462 | 条件 4: `needs-human-verification` ラベルが無い。付いている PR は絶対にマージしない | 層1 |
| C17 | :463 | 条件 5: このレーンにマージ許可が下りている | 層1 |
| C18 | :464 | 条件 6: 作業ブランチに未 push のコミットと未コミットの変更が無い（検証 A。契約 43.2 章） | 層1 |
| C19 | :465 | 条件 7: ローカル `main` が `origin/main` より先行していない（検証 B・C。契約 43.2 章） | 層1 |
| C20 | :467 | 条件 6・7 の由来: 2026-08-01 の事故（契約 §43.1）。PR #1 の未 push 2 コミット（`eslint-plugin-react-hooks` ピンと `--max-warnings 0`）が main に入らなかった | 退避 |
| C21 | :467 | 条件 1〜5 は「PR ないし ledger の状態」しか見ないので、5 条件すべてを満たしたまま内容が失われる | 層1 |
| C22 | :470 | fence: `gh pr checks <n> --watch`（条件 1） | 層1 |
| C23 | :471-472 | fence 注記: 条件 2〜5 を確認したうえで `gh pr merge` の直前に必ず実行する。PR 作成時に一度通しただけでは足りない（事故は PR 作成後に足したコミットで起きた） | 層1 |
| C24 | :473-474 | fence: `BR`/`TIP` の取得。TIP は ref ではなく SHA（`--delete-branch` がローカルブランチも消すため） | 層1 |
| C25 | :477-479 | fence: 検証 A（条件 6）— HEAD = `origin/$BR` の一致 / `git status --porcelain` が空 | 層1 |
| C26 | :481-483 | fence: 検証 B（条件 7）— ローカル main が先行していれば NG（自分で直さず team-lead に上げる。§43.3） | 層1 |
| C27 | :485-486 | fence: 検証 C（条件 7、目視）— `gh pr view <n> --json commits` で無関係なコミットの混入を見る | 層1 |
| C28 | :487-492 | fence 注記: 許可 3 種以外の行が現れたら不合格（契約 §101.5.2 が正典）。種 2 のマージコミットは★メッセージ本文に「検証 D の前提 P1 を成立させた」旨を書く。書かないと次の読み手が不合格にする | 層1 |
| C29 | :493 | fence 注記: 3 種以外で他レーンの成果物・エージェント定義・設計文書が並んでいたら検証 B 違反の痕跡 | 層1 |
| C30 | :495-496 | fence 注記: 例外 2 を書かないと P1 の処置を入れた瞬間に C が偽 BLOCKED を出す（契約 §101.1 の 9 で実測）。「D を直すと C が鳴る」は両方を同時に直さないと閉じない | 層1 |
| C31 | :497-498 | fence 注記: `gh` はマージコミットを 1 行に畳み、引き込まれた他 PR のコミットを一覧に出さない。見えない分は検証 D が $MC 基準で拾う | 層1 |
| C32 | :500-503 | fence: P1（契約 §101.2）— `origin/main` が PR ブランチの祖先であること。偽なら `git merge origin/main` で前提を真にする | 層1 |
| C33 | :504-505 | fence 注記: P1 が偽のまま進むと検証 D が「main に在ってブランチに無いファイル」を全部並べる（実測 17 files / 2138 deletions。§101.1 の 2）。取りこぼしと区別が付かない | 層1 |
| C34 | :506 | fence 注記: 検査を弱めず、前提のほうを真にする。これが入ると検証 C にマージコミットが 1 行増える（例外 2） | 層1 |
| C35 | :507-508 | fence 注記: コンフリクト解決が発生したら §32 の条件 3 も取り直す。発生しなければ差分は 1 バイトも変わらないので不要（契約 §101.4） | 層1 |
| C36 | :510-512 | fence 注記: ★ここで一度止まり「`gh pr merge` の前に」の節を実行、転記後に検証 A・B・C と TIP を取り直して戻る。TIP はコミットを足すと古くなり検証 D が偽の非空を出す | 層1 |
| C37 | :514-516 | fence: P3（契約 §101.2）— `$TIP` が「マージされる head」（`headRefOid`）と一致すること。古ければ取り直す | 層1 |
| C38 | :518 | fence: `gh pr merge <n> --squash --delete-branch` | 層1 |
| C39 | :521-522 | fence: マージ後 `git fetch origin --prune`、`MC` 取得（P2: 基準は `origin/main` ではなく `mergeCommit`） | 層1 |
| C40 | :523 | fence: 検証 D（最重要）— `git diff "$MC" "$TIP" --stat` が空であること。非空なら取りこぼしている | 層1 |
| C41 | :524-525 | fence: 検証 E — squash されている（親が 1 つ。`git show -s --format=%P "$MC" \| wc -w \| tr -d ' '`） | 層1 |
| C42 | :526 | fence: 検証 F — `git rev-parse --verify "origin/$BR"` が失敗すること（ブランチが消えている） | 層1 |
| C43 | :529-532 | 検証 E の 1 版目（`git rev-list --count --merges`）は履歴全体を遡るため必ず 1 を返し常に空振りしていた（PR #63 で発覚。真のマージ `0bed3b7` が全ブランチの祖先。検算 `993dafe`/`3cc3225` 両方 1。合格と不合格が同じ出力 = §99.2/§101.7 の族） | 退避 |
| C44 | :533-536 | 2 度目の手当て（`cat-file -p \| grep -c '^parent '`）も誤り。コミットメッセージ本文の `parent …`（テスト名）を数え、squash（罠あり）= 2 / 真のマージ = 2 で同じ値になった（PR #61 `689d21e` で偽の警報） | 退避 |
| C45 | :537-538 | 現行の `git show -s --format=%P \| wc -w` は親ハッシュだけを出すのでメッセージ本文の影響を受けない。検算 3 件（`689d21e` = 1 / `5d87fae` = 1 / `0bed3b7` = 2） | 退避 |
| C46 | :539-540 | 2 度目の手当て（`= "1"` 文字列比較）も macOS で壊れていた。BSD の `wc` は数値を右詰めし「合格が必ず不合格を返す」。次のレーンが `--delete-branch` 後に偽 BLOCKED を上げる（PR #64 で実測） | 退避 |
| C47 | :541 | team-lead は自分の実行に `\| tr -d ' '` を足しており、手順書に書いた形を一度も実行していなかった | 退避 |
| C48 | :542 | 検算は、書いた逐語そのものを実行すること。手元の変種で確かめない | 層1 |
| C49 | :543 | 現行の形はこの節から `sed` で 2 行を抜き出して 4 件へ当てて検算した（`74925de` / `f78b3dc` / `689d21e` / `0bed3b7`。前 3 件 OK / 最後だけ NG） | 退避 |
| C50 | :545 | 一般則: 出力を解析する検査は、その解析が曖昧になる入力に当てて検算する（群 I） | 層1 |
| C51 | :547 | 検証 E は 3 版連続で壊れ、同じ機構が 4 件目（ステージ完了ゲートの台帳集計）でも起きたため、規則を検証 E の節から出して一般則に格上げした | 退避 |
| C52 | :549-556 | 空振り 6 件表（rev-list / cat-file / wc 右詰め / awk -F'\|' の 3 行落ち / §25.4 ドリフト検査の向き / ポーリング禁止 grep の別形） | 退避 |
| C53 | :558 | 検査には向きがある。A→B を測る検査は B→A を測らない。両方向が要るなら 2 本書く | 層1 |
| C54 | :559 | パターン検査は、対象が別の形（別 API・行に割れる・別の綴り）を取りうるかを 1 つ挙げて確かめる | 層1 |
| C55 | :560 | 観測点: 検査を新設・修正したコミットに、逆向きの入力での実行結果が併記されていること | 層1 |
| C56 | :562-563 | 検査を書いた/直したら (1) 検算集合にその解析が曖昧になる入力を必ず 1 つ入れる。無ければ作る | 層1 |
| C57 | :563 | 理由: 壊れた版はどれも「普通の入力」では合格していた | 層1 |
| C58 | :564 | (2) 書いた逐語そのものを実行する。手元の変種で確かめない | 層1 |
| C59 | :565 | (3) 検査を足すときは、隣の既存の検査も実測する。壊れた検査は「在るから通っている」と読まれ続ける | 層1 |
| C60 | :567 | なぜ: 空振りは不合格と見分けが付かない。空振りする検査は無い検査より悪い —— 在ることが合格の根拠として引用される | 層1 |
| C61 | :569 | 検証 D の基準は `origin/main` ではなく `$MC`（P2）。`origin/main` のままだと自分のマージ後に他 PR が載った瞬間その内容が全部並ぶ。並列ステージでは日常的に起きる | 層1 |
| C62 | :569 | 実測 7 files / 1675 deletions（§101.1 の 5） | 退避 |
| C63 | :570 | P1 と P2 は別の穴。片方を塞いでももう片方は残る。`mergeCommit` を基準にしても P1 は閉じない（tree は「マージ時点の main + PR 差分」） | 層1 |
| C64 | :571 | 検証 D が非空だったら、ブランチを消さずに追加 PR で回収する。「マージは済んだので誤差」として流さない | 層1 |
| C65 | :571 | §43.1 の事故はこの 1 行で検出できた | 退避 |
| C66 | :572 | 非空を見たら、まず P1〜P3 が成立していたかを確かめる。前提が偽ならそれは取りこぼしではない。出力からは分けられないので前提を先に真にしておくのが唯一の分け方（契約 §101.3.1 は「前提が偽のまま実行した場合の読み方」を書かないと裁定 —— 書けば自己申告の逃げ道になる） | 層1 |
| C67 | :573 | 検証 E・F が失敗しても検証 D が空なら内容は失われていない。ブランチを手で消し、次回のマージ方式を正す。BLOCKED にはしない | 層1 |
| C68 | :574 | §32 条件 1（CI 緑）は `headSha` を照合する（契約 §101.6）。`gh pr checks --json` の 9 フィールドに SHA が無いので `gh pr view <n> --json statusCheckRollup` 側で見る。照合しないと古いコミットに対する緑を読む | 層1 |
| C69 | :576 | `--auto` は使わない。条件 2〜7 は GitHub から見えないため、auto-merge は条件 1 だけで通してしまう | 層1 |
| C70 | :580 | 定義の穴の転記は契約 §32 の 7 条件に足す 8 個目ではない（条件の一覧は契約が所有）。7 条件を満たしたあと、マージを実行するまでの間に必ず通す手順 | 層1 |
| C71 | :582 | PR 単位レビューの報告の `定義の穴:` 欄が `なし` 以外だったら、`.claude/AGENT-LESSONS.md` の `## 事象` の表に 1 行足し、その PR に含める | 層1 |
| C72 | :584 | 必ずマージの前に行う。マージ後に書くと main へ届ける経路が無い（`--delete-branch` でブランチは消え、隔離 worktree なら team-lead のゲートから永久に見えず、次の PR に紛れれば検証 C に自分で引っかかる） | 層1 |
| C73 | :586 | この転記コミットは検証 C の唯一の例外（コードブロックの★に明記）。本 PR で意図して足したものであり検証 B 違反の痕跡ではない | 層1 |
| C74 | :588 | 転記に使うのは最終ラウンドの報告（レビュアーがそれまでのラウンドの `定義の穴` をすべて再掲している）。1 件 1 行で転記する | 層1 |
| C75 | :590-599 | fence: 転記コミットの手順（`git add .claude/AGENT-LESSONS.md` → commit → push → `gh pr checks <n> --watch` で条件 1 取り直し → ★検証 A・B・C と `TIP=$(git rev-parse HEAD)` 取り直し） | 層1 |
| C76 | :601 | 取り直すのは条件 1 と検証 A・B・C と `TIP` だけ。条件 3 は取り直さない（取り直すと終わらない。転記コミットは `.claude/AGENT-LESSONS.md` 1 ファイルだけを触るのでレビューし直す内容を持たない） | 層1 |
| C77 | :603 | `TIP` の取り直しを飛ばすと検証 D が偽の非空を出す。そのときの復旧手順（追加 PR で回収）は `--delete-branch` 済みなので実行できない | 層1 |
| C78 | :605-613 | 台帳の列定義表（`#` = 最大値+1 / `日付` / `ステージ` / `出典` = `PR #<n>` / `事象` は 1 行に収める（セルは改行を含められない）/ `穴のあった定義` / `昇格` = `—`） | 層1 |
| C79 | :615 | `なし` のときは何も書かない。「特になし」の行を残すとファイルが埋まり実在の事象が埋もれる | 層1 |
| C80 | :616 | 要約して規則の形に直さない。「◯◯すべき」ではなく「◯◯が起きた」のまま残す | 層1 |
| C81 | :617 | 昇格の判断はあなたがしない。ステージ完了ゲートで team-lead が行う | 層1 |
| C82 | :618 | 契約に書くべき内容が混じっていたら転記せず契約変更要求として上げる（分界は「作り方の話か、作るものの話か」） | 層1 |
| C83 | :624 | 許可あり: PR は毎回 `git fetch origin && git switch -c <branch> origin/main` で切る。前の PR をマージしてから次を始める。squash マージなのでスタックさせてはならない（同じ変更が二重適用される） | 層1 |
| C84 | :625 | 許可なし: PR をスタックする（`--base` は直前の PR ブランチ）。マージはしない。許可が下りたら `main` を取り込んでから下から順にマージする | 層1 |
| C85 | :627 | 許可の有無は起動プロンプトで指示される。書かれていなければ「許可なし」として扱い team-lead に確認する | 層1 |
| C86 | :631 | レーンはローカル `main` にコミットしてはならない。ローカル `main` は `origin/main` の fast-forward 追従にのみ使う | 層1 |
| C87 | :632 | ブランチは `origin/main` から切る。`git switch main && git switch -c <branch>` は使わない（ローカル main が古ければ古い土台から切り、先行していれば無関係なコミットを引き込む） | 層1 |
| C88 | :632 | §43.1 では PR #1 の差分に無関係な 3 コミットが混入した | 退避 |
| C89 | :633 | 検証 B が非空でも自分で解消しない。`git reset`/`git rebase` はそこにしか無いコミットを消す可能性がある。BLOCKED として team-lead に上げる（「CI の失敗を自分で直さない」と同じ規律） | 層1 |
| C90 | :637 | 人間検証 PR の対象は PR 12 / 17 / 19 / 20 / 26 / 32 / 33 の 7 本だけ | 層2:completion |
| C91 | :639 | 作成時 (1): `gh pr create --label needs-human-verification`。ラベルが無ければ `gh label create needs-human-verification --color B60205 --description "マージ前に実機での動作検証が必要"` | 層2:completion |
| C92 | :640 | 作成時 (2): PR 本文の末尾に検証セクション（手順 / 期待結果 / 自動化できない理由を契約 §26.4 のカテゴリ番号で示す） | 層2:completion |
| C93 | :642 | そのうえで BLOCKED として team-lead に上げる。人間の確認が返ってラベルが外れるまでその PR はマージしない | 層2:completion |
| C94 | :644 | この 7 本以外にラベルを付けてはならない。「念のため見てほしい」は理由にならない。§26.4 のカテゴリで説明できない検証は E2E に落とすべきもの | 層2:completion |
| C95 | :648 | CI で初めて気づくのは往復が無駄。implementer に push 前チェックを実行させ、緑を確認してから push する | 層1 |
| C96 | :650-653 | fence: `npm run lint && npm run fmt:check && npx tsc --noEmit && npx vitest run` / `npm run e2e`（M1-2 Task 17 以降）/ `cd src-tauri && cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace` | 層1 |
| C97 | :658 | M1-1 は Task 19 → Task 20 → Task 1 → … → Task 18 の順に実行する。lint と CI は全 PR が乗る土台なので先に入れる。タスク番号は文書上の ID であって実行順ではない | 層2:completion |
| C98 | :660 | M3-4 は Task 1〜14 → 16〜21 → 15 の順。Task 15 は最終スモークでプロジェクト全体を締める。§29/§30 のスクラッチと shim を後ろに置くと未検証の 6 タスクが最終検証の後になる | 層2:completion |
| C99 | :664 | 契約 §26 によりフロント単体 E2E（Playwright + IPC モック、`e2e/*.spec.ts`）を採用。基盤は M1-2 Task 17 が作る | 層2:completion |
| C100 | :666 | M1-3 以降のフェーズは、計画の手動スモークチェックリストを完了前に仕分ける | 層2:completion |
| C101 | :668 | 仕分け 1: DOM 操作で検証できる項目 → `e2e/<フェーズ名>.spec.ts` に spec を追加（web-implementer に投げる） | 層2:completion |
| C102 | :669 | 仕分け 2: 人間にしかできない 5 カテゴリ（契約 §26.4）に当たる項目 → 手動に残す | 層2:completion |
| C103 | :670 | 仕分け 3: 手動に残す判断には 5 カテゴリのどれに当たるかを必ず書く。「面倒だから」は理由にならない | 層2:completion |
| C104 | :671 | 仕分け 4: 結果を完了報告に「E2E に移した項目 / 手動に残した項目」として明記する | 層2:completion |
| C105 | :673 | E2E はユニットテストの代替ではない。純関数は vitest / cargo test で検証し、E2E に書くのは DOM とライブラリを跨ぐ経路だけ | 層2:completion |
| C106 | :677 | 人間ゲートは implementer が BLOCKED を返す前に、あなた自身が計画を読んだ時点で気づいたら先に上げてよい | 層2:completion |
| C107 | :681 | M1-4: `.app` を Finder からダブルクリックして PATH 解決を確認（`npm run tauri dev` では再現しない。契約 §18）/ Task 12: shim ディレクトリが rc 通過後も PATH に残るか（§30.3、§26.4-5） | 層2:completion |
| C108 | :682 | M2-2: 実 PTY の対話モードで `Notification`/`PermissionRequest` の payload をキャプチャ。hook の stdout がターミナル表示を汚さないか実測 | 層2:completion |
| C109 | :683 | M2-3: 通知のクリック応答を実機で確認（`notify-rust` を選んだ唯一の理由） | 層2:completion |
| C110 | :684 | M3-2: `Cmd+D` / `Cmd+[` / `Cmd+]` が WebView に奪われないか実機確認 | 層2:completion |
| C111 | :685 | M3-4: 起動 1 秒未満 / セッション 5 個で 300MB 未満 / アイドル CPU の実測（Task 15）/ Task 20: `Cmd+W` が macOS 既定メニューに奪われないか（§29.8） | 層2:completion |
| C112 | :687 | 「たぶん通る」で ✅ にしない。headless で再現しない項目を通ったことにするのが、この計画で最も危険な失敗である | 層2:completion |
| C113 | :691 | `Task <N>: complete (commits <base7>..<head7>, review clean)` を毎タスク追記する | 層1 |
| C114 | :691 | 理由: コンテキストが圧縮されると現在地を見失い、完了済みタスクを丸ごと再実行する。ledger と `git log` はあなたの記憶より信用できる | 層1 |
| C115 | :693 | 完了行の書式は 1 つに固定する。ledger は §32 条件 2 の根拠である | 層1 |
| C116 | :695 | 条件 2 は「ledger に `Task <N>: complete`」を根拠にする。記録が在っても探せなければ在らないのと同じになる | 層1 |
| C117 | :697-700 | 2 回の実測（書式が 3 形式に割れ grep 0 件・Task 7 漏れ = 台帳 #96 / セッション中断で完了行 4 本欠落 = 台帳 #85） | 退避 |
| C118 | :702 | 【規則 1】完了行は逐語の形だけを使う。行頭から始め、`Task <N>: complete` の後ろに括弧で補足。別の言い回しを混ぜない | 層1 |
| C119 | :703 | 【規則 2】レーンを再開したら、最初に ledger の最終完了行とブランチの最終コミットが対応しているかを確かめる。対応していなければ「コミットは在るが完了の根拠が無い」状態であり、レビュー記録を先に復元する | 層1 |
| C120 | :705-712 | 検算 3 レーン表（2026-08-10。M2-2 = 12 一致 / M3-2 = 9 で 2 本足りない / M2-3 = 0 で書式が別） | 退避 |
| C121 | :713 | アンカー（`^`）を外さないこと。行頭なしだと本文中の言及まで拾う。「多く出るほうが安全」ではない —— 多く出た時点で完了の根拠として数えられなくなる | 層1 |
| C122 | :713 | 実測: M2-2 では行頭なしにすると 30 件出る | 退避 |
| C123 | :717 | 予告だけが ledger に残り実施されないまま終わった工程が実在する（M3-1 の条件 3 = 台帳 #26 / フェーズ完了報告と §26.5-3 の仕分け記録 = 台帳 #30） | 退避 |
| C124 | :719 | 予告を書いたら、実施したときに同じ行の下へ結果を書く | 層1 |
| C125 | :720 | 結果が書かれていない予告は、次に読む者から「未実施」として扱われる | 層1 |
| C126 | :722 | team-lead から `SendMessage` で受け取った裁定は、受け取った時点で ledger か `progress.md`「## 裁定」に書き写す。`SendMessage` の内容はリポジトリのどこにも残らない（task-reviewer が grep しても確認できず検証不能扱いになる） | 層1 |
| C127 | :722 | 要約して書かない —— 要約の過程で裁定の意味が反転し、反転後の記述はドリフト検査を素通りする | 層1 |
| C128 | :722 | 実例: M3-1 で「契約の表に無い 4 つ目の変更」→「契約の射程内」に反転（台帳 #12 / #13） | 退避 |
| C129 | :726 | 起動時に ledger の最終完了行を記録し、そこから 2 タスク進んだら、次のタスクには着手せず引き継ぎ報告を返して終了する | 層1 |
| C130 | :726 | 起動時に記録する理由: 完了行がレーン名を持たないので、それ以外に自分の担当分を数える手段が無い | 層1 |
| C131 | :726 | 自分で次のレーンを起こしてはならない（team-lead が起こす） | 層1 |
| C132 | :728 | 次のどれかが先に来たら、2 タスクに満たなくてもそこで引き継ぐ | 層1 |
| C133 | :730 | PR をマージした（= 検証 F まで通したこと。`gh pr merge` を打った時点ではない）。ただしフェーズ最後の PR だった場合は引き継がず「フェーズ完了時」へ進む | 層1 |
| C134 | :731 | PR を作成してスタックした（マージ許可が無いレーン）。このレーンにマージは来ないので、ここで切らないとフェーズ 1 本を走り切ることになる | 層1 |
| C135 | :732 | 「切れる場所」の局面に入ったら引き継ぐ | 層1 |
| C136 | :734 | 2 タスク目が PR の区切りでもあるときは、先に PR を作成（許可があればマージまで）してから引き継ぐ。手順は「PR の運用」が持つ | 層1 |
| C137 | :738 | 1 ターンごとに文脈の全量を読み直すので、レーン 1 本が長く走るほどタスク 1 つが高くつく | 層1 |
| C138 | :740-745 | 粒度の実測表（1 レーン = 1 PR で 23.72M/タスク vs 1.75 タスクで 5.90M。4.02 倍。PR 17 は 3 つが検証タスクという限定付き） | 退避 |
| C139 | :747 | 引き継ぎは安い（初回ターン 51K はレーン起動の固定費で、引き継ぎの有無に依らない。実測 4 レーン） | 退避 |
| C140 | :747 | 「引き継ぐと高くつく」を理由に長く走ってはならない | 層1 |
| C141 | :751 | 降りてよいかの判定条件は「タスク境界かどうか」ではなく `git status --short` が空かどうか | 層1 |
| C142 | :751 | 各タスクの完了ごとにコミットするので、作業ツリーが clean なら状態は ledger と `git log` から復元できる | 層1 |
| C143 | :753 | implementer が動いている最中は終了してはならない（未コミットの変更が残り、次のレーンからは「まだ始まっていない」と区別がつかない） | 層1 |
| C144 | :754 | レビュー → 修正ループの途中は終了してはならない（指摘が ledger に無いので、次のレーンは指摘そのものを失う） | 層1 |
| C145 | :755 | `TIP` を保持している間（取得から検証 F まで。`gh pr merge` の後も続く）は終了してはならない。`TIP` はシェル変数なので、途中で降りると後任は検証 D・E・F を実行できない。`--delete-branch` 済みなら復元できず、取りこぼしを検出する手段が永久に失われる | 層1 |
| C146 | :755 | スタックしたまま置く PR はこれに当たらない —— マージ手順に入っていないので `TIP` を保持していない | 層1 |
| C147 | :757 | 例外はセッション上限だけ。上限が近いときは未コミットが残っていても降りてよい —— 上限で殺されると ledger の完了行そのものが落ちる。その場合は引き継ぎ報告に `未コミットあり` と書き `git status --short` の出力を添える | 層1 |
| C148 | :759-766 | 切れる場所 4 局面の表（PR のマージ直後/スタック直後 = clean / 人間ゲート待ち = BLOCKED と同じ応答で引き継ぎ報告 / 契約変更要求の裁定待ち = 裁定は「## 裁定」に残るので引き継げる / セッション上限 = clean でなくてよい） | 層2:handoff |
| C149 | :759,:761-766 | 実測値（2026-08-10 M2-2 PR 17 で 4 件。人間ゲート待ち 2 回・裁定待ち 1 回・上限 1 回） | 退避 |
| C150 | :768 | 4 局面のうち 3 つはタスクの途中で起きている（当該タスクの完了行は未記入だった）。判定をタスク境界ではなく作業ツリーの状態で書いているのはこのため | 層2:handoff |
| C151 | :772 | 引き継ぎ報告は `SendMessage` で team-lead に送る。地の文に書いても届かない | 層1 |
| C152 | :772 | 同時に `Handoff:` の 1 行を ledger にも書く —— `SendMessage` の内容はリポジトリのどこにも残らないので、これが無いと引き継ぎが起きたこともマージ許可が渡ったことも後から数え直せない | 層1 |
| C153 | :774-776 | fence: `Handoff: Task <N> まで完了 → 次 Task <N+1>（PR #<n>、マージ許可 あり\|なし、<理由>）` | 層1 |
| C154 | :780-790 | fence: `SendMessage` 本文テンプレ 9 項目（フェーズ・PR / ブランチ・HEAD / 完了タスク / 次のタスク / マージ許可 / 人間ゲート待ち / park・契約変更要求 / 未コミット / ledger に書ききれていない判断） | 層2:handoff |
| C155 | :785 | マージ許可は規約からは復元できない。必ず書く | 層2:handoff |
| C156 | :792 | 差分やコード全文を貼らない。次のレーンは ledger と `git log` から復元する | 層2:handoff |
| C157 | :796 | 引き継ぎを受けた側は最初に ledger 節【規則 2】を実行する。その行があなたが 2 タスクを数える起点になる | 層2:handoff |
| C158 | :798 | 不一致でレビュー記録を復元した場合は、復元後の最終完了行を起点に取り直す。復旧で完了行が 1 本増えるので、取り直さないと起点が古いまま 3 タスク走る | 層2:handoff |
| C159 | :800 | 完了行が `no commit` のタスクでは規則 2 の検査は一致も不一致も返さない。evidence ファイルの存在で代える。所在は当該完了行が持っている | 層2:handoff |
| C160 | :800 | `docs/superpowers` と `.superpowers` は `.git/info/exclude` により worktree に存在しないので、絶対パスで参照する | 層2:handoff |
| C161 | :804-808 | 効果の測り方 3 指標の表（タスクあたり文脈総和 = jsonl の usage 合算 / 引き継ぎ回数 = `grep -c '^Handoff:'` / マージ許可の受け渡し） | 退避 |
| C162 | :810 | ledger は `.superpowers/sdd/.gitignore`（1 行 `*`）により git 管理外なので、履歴が無く、新規 clone に無く、消えたことを検出できない | 退避 |
| C163 | :810 | ⚠️「`SKILL.md` §6 の『永続的に数えられるのは PR 本数と台帳行数の 2 つだけ』」はダングリング参照（PR #93 で `kamux-promotion/SKILL.md`「判断の物差しが壊れていないか」へ移設済み）。退避時に張り替える | 退避 |
| C164 | :814 | フェーズ完了時の手順は、そのフェーズの最後の PR を担当したレーンだけが実行する。引き継ぎで終了したレーンは対象外 | 層2:completion |
| C165 | :816 | 順序を書き直した経緯（旧版は (1) マージ → (2) 全ブランチレビュー → (3) 修正の順で、(2) の時点でブランチが消えており (3) は構造的に実行できない。台帳 #211） | 退避 |
| C166 | :817 | 実測: M2-4 で Critical 1 件（L-1）が手当ての経路を持たないまま main に着地し、契約 §127 が単独 hotfix（PR H2）を裁定して初めて宛先を得た。毎フェーズの最後で必ず起きる形 | 退避 |
| C167 | :818 | 【規則】全ブランチレビューは、最後の PR をマージする前に実施する。レビューの結果を反映できる状態（ブランチが在る / PR が open）が残っている時点に検査を置く —— これが群 δ の一般形 | 層2:completion |
| C168 | :820 | 手順 1: 最後の PR を open にしたまま、全ブランチレビューを `opus` で 1 回だけ実施する（`superpowers:requesting-code-review` の code-reviewer） | 層2:completion |
| C169 | :821 | 手順 2: 指摘があれば修正 implementer を 1 体だけ起動し、まとめて直す（指摘ごとに 1 体起動しない）。修正は最後の PR のブランチへ積む | 層2:completion |
| C170 | :822 | 手順 3: 修正を積んだら、必ず再レビューを 1 回通してから手順 4 へ進む。手順 2 は `<承認 sha>..HEAD` を構造的に非空にするので、そのままマージすると群 δ 規則 1（非空ならレビューへ戻す）に必ず当たる。例外にはしない | 層2:completion |
| C171 | :822 | 根拠は契約 §101.4 —— 「解決結果は誰もレビューしていない著作物であり、条件 3 の射程そのもの」。全ブランチレビューの指摘を直した差分も同じ射程に入る | 層2:completion |
| C172 | :822 | §32 条件 3 の逐語は「PR の差分全体に対して 1 回」であり承認対象の sha を名指ししていない。「1 回」を「一度通せば以後は不要」と読むと本節は成り立たない —— 成り立たせているのは §101.4 の原理（レビュアーが見る差分が変わったか）のほう | 層2:completion |
| C173 | :822 | 再レビューが `APPROVED` を返した sha が、次の測定の起点になる | 層2:completion |
| C174 | :823 | 手順 4: そのうえで最後の PR をマージする（契約 §32 の 7 条件） | 層2:completion |
| C175 | :824 | 手順 5: マージ後に見つかった穴は、あなたの手では台帳へ入らない。完了報告の `定義の穴 / 台帳へ足す行:` 欄に書いて team-lead へ渡す | 層2:completion |
| C176 | :824 | 手順 1〜3 で見つかった分も同じ欄に書く —— マージ前転記の指示は「PR 単位レビューの報告」を転記元に限定しており、全ブランチレビューの `定義の穴` はどちらの経路にも乗らない。転記路がマージと同時に閉じる | 層2:completion |
| C177 | :825 | 手順 6: 完了報告を team-lead に返す | 層2:completion |
| C178 | :827-838 | fence: 完了報告テンプレ 8 項目（フェーズ / ブランチ・worktree / 完了タスク N/M / コミット範囲 / テスト 5 種の結果 / E2E に移した項目・手動に残した項目（各項目の §26.4 カテゴリ）/ 持ち越した指摘 / 契約への追記要求 / 人間ゲート待ち / 定義の穴・台帳へ足す行） | 層2:completion |
| C179 | :840 | 差分やコード全文を報告に貼らない | 層2:completion |
| C180 | :842 | 報告と PR 本文に書く数値は、最終コミットに対して取り直す | 層1 |
| C181 | :844-850 | 同じ形で 3 回外した実例表（implementer の報告が実装途中の grep 逐語 / lane-controller が検証せず転記しドリフト検査の不合格を緑と報告 / PR 本文の vitest・cargo test 件数が数コミット前の値。台帳 #5 / #6 / #15） | 退避 |
| C182 | :852 | implementer の報告に載っている数値・grep 出力をそのまま転記しない。あなたが最終コミットの上で実行し直した値だけを書く | 層1 |
| C183 | :853 | PR 本文はマージ後も残る恒久的な記録である。報告や ledger と違って訂正の機会が乏しい | 層1 |
| C184 | :855 | `npm run lint` と `npm run fmt:check` を報告に必ず含める | 層1 |
| C185 | :855 | 経緯: M3-1 ではフェーズを通じて一度も実行・報告されず、team-lead が PR 作成直前に初めて走らせた（台帳 #14） | 退避 |
| C186 | :734 | 実測: 契約 §27.4 の 33 本のうち 14 本（42%）が「2 タスク目 = PR の区切り」の形 | 退避 |

## パート C の判定して外した候補

外した候補は 0 件。読者の行動を変えない純粋な繋ぎ文（「次を行う」等）は直前・直後の規範項目の行に含めて数えた（C6 / C132 など）。

## ⚠️要裁定

- C33 / C62 など fence 内コメントの実測値（17 files / 2138 deletions 等）: fence のバイト保存（検査 11）と「日付・実測は退避」が衝突する。**fence 内は契約ピンによりバイト不変を優先し、fence の外の実測だけを退避する**と裁定した（C33 は fence 内なので層1 のまま、C62 は fence 外の散文なので退避）。
- C163: 退避先に写す際にダングリング参照の張り替えを伴う（唯一の「内容に触る」項目。#84 の参照張り替えスコープ内）。

## 件数サマリ

総数 186 / 層1 114 / 層2:completion 35 / 層2:handoff 9 / 退避 28 / 削除 0（修正ラウンド 1 で C136 を規則と実測に分割し C186 を追加）

（296df6b 時点の検算: 行数 185・連番一致・内訳一致。最初に手で書いたサマリは 4 値中 4 値が誤っており、機械導出で訂正した —— 群 μ の形がこの台帳自身でも出た。C186 追加後の現行値は総数 186 で、全体の再導出値は末尾の「修正ラウンド 1 の検算値」が持つ）

## 統合時の裁定（本便）

- B86: fork の仮判定（層1）を承認。並列起動禁止の第 2 の独立した帰結であり、README-rewrite「欠落はいつも理由・帰結の側で起きる」に該当する。
- C151 / C152 / C153（引き継ぎ報告の `SendMessage` 規則・`Handoff:` 行と理由・その fence）: 層1 に置いた（fork C の判定どおり。当初の新版草稿が handoff へ落としていたのを統合時に復元した）。
- C164（対象者）/ C167（順序規則）: 層2:completion から**層1 へ変更**。順序規則（全ブランチレビューはマージ前）は毎フェーズ必ず 1 回踏む分岐で、破ると回復不能（ブランチ消失）のため本体に残す。completion 側の手順 1〜4 は同じ順序を操作として体現するが、規則文の正典は層1 のみ（群 λ: 写さない）。
- fence の増減 27 件（`--fence=27:7802ff418006`）: 全件裁定済み。内訳は (a) マージブロックのコメント行 4 対 = 🔴 の表記除去（うち 1 対は `MC=$(…)` コマンド行の行内コメント。シェルとして実行されるバイトは 26/26 行不変。契約ピンの範囲は維持）、(b) 引き継ぎ報告テンプレ 9 行 = `lane-controller-handoff.md` の fence へ逐語移設、(c) 完了報告テンプレ 10 行 = `lane-controller-completion.md` の fence へ移設（`定義の穴` 行の「群 G。」ラベルのみ群ラベル廃止方針により削除。群 G の対応は cases が保持）。
- 見出しの改名 12 件（`--renamed=12:cde29ab449dc`）: 全件意図的。装飾・契約 § 表記・重文の後半を落とした改名 6 件（PR の運用 / マージ / 対処 1 / 完了行の書式 / 裁定転記 / E2E 仕分け=移設）と、層2 への節移設 6 件（この規則が効いているかの測り方 / なぜ粒度 / 人間にしかできない検証 / 実行順が特殊なフェーズ / 引き継ぎを受けた側 / 引き継ぎ報告の形）。移設先の見出しは各層2 ファイルが持つ。
- 検査 9 の期待 NG: 0 件（判定母数 31 件・厳格 21 件、全件解決。「切れる場所」は層1 に小節を復活させて解決した）。

## check-survival.py の欠落監査（削除監査ワークシート。母数 817 文）

初回実行で痕跡なし 15 件 → 理由・帰結 4 件を復元（旧 :313 トークン発行の逐語 / 旧 :386 反転の理由 / 旧 :611 レビュアー欄の理由 / 旧 :388 復元側の欠落経緯 → cases）。再実行で痕跡なし 11 件。全件の判定:

| 旧行 | 文 | 判定 |
|---|---|---|
| :98 | 追記ではなく書き直しが要る | 削除（経緯・メタ。昇格済み規則の再発を追記で足さない規則の正典は `kamux-promotion/SKILL.md` にあり、本ファイルが持つ必要が無い） |
| :308 | 待つ対象は「新しいコミット」ではない | 生存（言い換え。層1「『新しいコミット』を待たない」） |
| :339 | なぜトークンなら足りるか: 位置を見ていたのは… | 生存（言い換え。層1「トークンはラウンドが始まるまでファイルのどこにも存在しえない…位置の条件が要らなくなる」） |
| :358 | 起きる前提が成り立っていなかった | 生存（言い換え。層1「起床条件（新しいコミット）が構造的に存在しなかった」） |
| :365 | 位置は見ない（理由は上の検算表） | 生存（言い換え。層1 (1)「`grep` 1 本で判定する。位置は見ない」。検算表は cases） |
| :547 | 規則を検証 E の節から外に出す | 削除（経緯・メタ。一般則化の事実は cases「空振りした検査は 6 件ある（群 I が一般則になった理由）」が保持） |
| :700 | 記録そのものが失われた | 生存（言い換え。cases「セッション中断（週次上限）で完了行が 4 本欠落した」の表ラベル） |
| — | 日付・群ラベル・sha・章番号を含む 4 件（器は件数のみ出す） | 意図的退避（受け入れ条件により日付・台帳番号は本文 0 件。実体は cases が保持） |

削除は 2 件のみで、いずれも規則本体ではなく「書き直し・節移動」を述べるメタ文である。11 群の規則本体の削除は 0 件（冒頭の削除判定表のとおり）。

## before / after（実測。算式は cases「本ファイル自身の来歴」と同一）

| | before（origin/main `ea7dc7f`） | after（層1） |
|---|---|---|
| 行数 | 855 | 600（-30%） |
| 文字数 | 49,451 | 27,675（-44%） |
| 概算トークン（文字数 ÷ 2） | 24,725 | 13,837（-44%） |
| 🔴 / 太字 / 日付 / 台帳行番号 | 66 / 617 / 45 / 33（台帳形）+113（素 #N） | 3 / 1 / 0 / 0（両測度とも） |
| 200 文字超の行 / § | 32 / 86 | 0 / 16（値の用途と fence のみ） |

層2 + cases を含めた総量は 1,044 行（分割前より +189 行。修正ラウンド 1 適用後の実測）。削減の主指標は常時ロード分（層1）である。

## 修正ラウンド 1（独立レビュー 2 体の判定への対応。基点 296df6b）

r1 = CHANGES_REQUESTED（Important 2 / Minor 5）、r2 = CHANGES_REQUESTED（Important 2 / Minor 5）。両者が独立に「12 の規則」の件数誤りを検出した（独立の裏付け）。対応:

| 指摘 | 対応 |
|---|---|
| r1-I1: C136 の実測「33 本中 14 本（42%）」が無記録で消えた | cases「切れる場所…」の節へ復元し、C136 を規則（層1）と実測（C186・退避）に分割した |
| r1-I2 = r2-I1: 「12 の規則」の件数誤り（実体 11） | 層1 と records 冒頭を「11」へ訂正（12 本目 = 群 ξ の観測点は層1「対処 1」節末尾が持つ） |
| r2-I2: 棚卸し表・群 δ 行の観測点が移設で空振り | `lane-controller-completion.md`「フェーズ完了時の手順」の手順番号を指す形へ張り替えた |
| r1-M3 / r1-M4: A173 例示コマンド・B137 出典ポインタの脱落 | 層1 へ復元 |
| r1-M5: 「操作行 1 バイト不変」の文言が行内コメント込みでは偽 | 「シェル有効バイト 26/26 行不変。行内コメントの 🔴 1 個のみ除去」へ精密化。追認は PR の人間レビュー |
| r1-M6: 行数の自己失効（cases 274 行 / 総量 1,032 行） | 全修正の適用後に再実測して在地更新（下の before/after 表も同時に更新） |
| r1-M7 = r2-M3: B130 / B18 / B149 の行き先表記 | B130 → 層1 へ訂正、B18 / B149 に分置注記 |
| r2-M1: cases の引用が正典と 2 文字違い | 「PR の本数と台帳の行数」へ訂正（`grep -F` で正典との一致 = 1 を確認） |
| r2-M2: 全角/半角括弧の混在（層1 ledger 節） | 訂正 |
| r2-M4: パート C 見出しの off-by-one（:856） | :855 へ訂正 |
| r2-M5: 台帳の同名見出し重複 | パート名を見出しへ付けた |

自己適用（群 U / 修正ラウンドは穴を増やす形への手当て）: 復元した「42%」の一次資料は旧版 :734 の逐語（`git show ea7dc7f:.claude/agents/lane-controller.md` で照合）。引用訂正は `grep -Fc '永続的に数えられるのは PR の本数と台帳の行数' .claude/skills/kamux-promotion/SKILL.md` = 1 を実行して確かめた。編集後に台帳の連番・行き先集計を機械で再導出した（検算値は下の「修正ラウンド 1 の検算値」）。

## 修正ラウンド 1 の検算値（58545e3 適用後、本ラウンドで再導出）

台帳 542 行を機械パース（code span 退避後にセル分割）。連番 A1..177 / B1..179 / C1..186 に欠番・重複 0。行き先の内訳:

| パート | 総数 | 内訳 |
|---|---|---|
| A | 177 | 層1 138 / 退避 39 |
| B | 179 | 層1 106（B86 の要裁定承認・B130 訂正込み）/ 層2:records 17 / 退避 56 |
| C | 186 | 層1 114 / 層2:completion 35 / 層2:handoff 9 / 退避 28 |
| 計 | 542 | 削除 0（規則本体の削除は 0 件。経緯メタ 2 文の意図的削除は survival 監査表が持つ） |

器の実行形（修正ラウンド以降の正典。`--no-anchors` は origin/main から 2 コミット以上進むと FATAL になる fail-closed のため、復元逐語 2 件をアンカーで守る）:

```
python3 .claude/scripts/check-rewrite.py . .claude/agents/lane-controller.md \
  .claude/references/lane-controller-cases.md \
  --chapters=14:550c36763b66 --renamed=12:cde29ab449dc --fence=27:7802ff418006 \
  --anchor='昇格するかの判断' \
  --anchor='git -C <リポジトリ絶対パス> diff --no-ext-diff -- <path>'
```

実行結果（本ラウンドで実測）: 15 検査すべて OK・exit 0（`OK [12] 守る逐語 2 件のうち欠けたもの 0 件`）。
