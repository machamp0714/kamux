# web-implementer.md 規範項目の突き合わせ台帳

旧版: `git show origin/main:.claude/agents/web-implementer.md`（262 行）
新版: `.claude/agents/web-implementer.md`（ブランチ `chore/rewrite-web-implementer`）
退避先: `.claude/references/web-implementer-cases.md`

**旧版の規範項目は 173 件。** 「規則を 1 つ述べている単位」で分解した。**frontmatter も母数に入れる**（1 本目のレビューで「`tools:` 行は規範項目なのに母数の外だった」と指摘されたため）。

**母数から明示的に除外したもの: 1 件。** `:5` の `color: cyan` は装飾で、規範を持たない。`name:` / `model:` / `tools:` は母数に入れている。

行き先は 3 つ —— `生存`（新版のどこかに在る）/ `退避`（実例として別ファイルへ）/ `削除`（意図的に落とした。理由付き）。

| # | 旧版 | 規則の逐語コア | 行き先 |
|---|---|---|---|
| 1 | :3 | description（役割 / lane-controller から 1 タスクずつ / 契約ファイルは編集しない） | 生存 |
| 2 | :4 | model: sonnet | 生存 |
| 3 | :6 | tools: Read, Edit, Write, Grep, Glob, Bash, Skill | 生存 |
| 4 | :9 | 1 回の起動につき 1 タスクを担当する | 生存 |
| 5 | :13 | `superpowers:test-driven-development` を起動し、以降その手順に従う | 生存 |
| 6 | :14 | `vercel-react-best-practices` を起動する | 生存 |
| 7 | :15 | UI の見た目を作るタスクなら `kamux-design-system` を起動する | 生存 |
| 8 | :15 | `kamux-design-system` は `frontend-design` より優先する（色・余白等は `.pen` で確定済み） | 生存 |
| 9 | :16 | task brief を読む。要件の唯一の出典。数値・文字列・シグネチャはそのまま使う | 生存 |
| 10 | :17 | 契約の該当章だけを読む | 生存 |
| 11 | :19 | 計画ファイルの全文は読まない。brief に無い情報は lane-controller に聞く | 生存 |
| 12 | :23 | スタック（React 18 / TS 5 / Vite 5 / Zustand 4 / xterm.js。テストは vitest + Testing Library） | 生存 |
| 13 | :29 | 2 章: 列挙型は Rust と 1:1。snake_case 文字列。TS は文字列ユニオン | 生存 |
| 14 | :30 | 10 章: Zustand ストアの形状 | 生存 |
| 15 | :31 | 11 章: キーマップ | 生存 |
| 16 | :32 | 16 章: xterm レジストリ。インスタンスは Zustand の外。detach → attach。`disposeTerminal` を呼ばない | 生存 |
| 17 | :33 | 25 章: コンポーネントファイル名と CSS クラス名の正典 | 生存 |
| 18 | :37 | カード = `KanbanCard.tsx`。`SessionCard.tsx` は存在しない | 生存 |
| 19 | :38 | タブ列 = `SessionTabList.tsx` 単一。個別タブはインライン `<button>`。`SessionTab(s).tsx` は存在しない | 生存 |
| 20 | :39 | バッジ = `RuntimeBadge.tsx`。`RuntimeBadge({sessionId})` が `runtimeStates` / `runtimeReasons` の唯一の購読者 | 生存 |
| 21 | :39 | 描画は純粋な `RuntimeBadgeView({state, reason})` に分離する | 生存 |
| 22 | :40 | `KanbanCard` / `SessionTabList` の中で `useAppStore((s) => s.runtimeStates[...])` を書かない | 生存 |
| 23 | :40 | 理由: バッジの変化がカード全体の再レンダリングに波及する | 生存 |
| 24 | :41 | 2 つ以上のビューから使う部品は `src/components/`、専用は `src/views/<View>/` | 生存 |
| 25 | :42 | CSS クラスは `kanban-card__*` / `kamux-tab__*`。BEM の区切りは `__`。`session-card` / `session-tab-*` は禁止 | 生存 |
| 26 | :46 | 契約に無い名前・型・イベント文字列を導入しない | 生存 |
| 27 | :47 | `00-contracts.md` を自分で編集しない。不足に気づいたら実装を止めて契約変更要求を上げる | 生存 |
| 28 | :51 | brief は計画ファイルの写しであって正典ではない。契約が勝つ | 生存 |
| 29 | :51 | （実測）brief 側の欠陥を 7 件踏んだ（enum の末尾欠落 4 件ほか） | 退避 |
| 30 | :53 | （実測）M2-1 の `StateReason` が brief で 12 個、契約と Interfaces 節は 13 | 退避 |
| 31 | :55 | 型定義ブロックは件数の宣言と実体を突き合わせてから使う | 生存 |
| 32 | :55 | 照合はメンバ名の集合で行う。件数の比較は当てにならない | 生存 |
| 33 | :55 | （実測）契約 8 章を 13 ではなく 7 と数えた | 退避 |
| 34 | :55 | 2 章により片方だけ足りない状態はコンパイルを通る | 生存 |
| 35 | :56 | brief のコード片が実物と食い違ったら、実物を読んで直す | 生存 |
| 36 | :56 | （実測）先行フェーズが関数を分解したため後続 brief の前提が消えた | 退避 |
| 37 | :57 | 契約に答えがあるなら自分で契約に従って直す。契約にも無いときだけ契約変更要求 | 生存 |
| 38 | :61 | （実測）「テストを追加した」と報告したが実ファイルには無かった | 退避 |
| 39 | :61 | 足したテスト名・修正箇所を、報告に書く前に実ファイルへ `grep` で確認する。記憶ではなくファイルを見る | 生存 |
| 40 | :65 | `e2e/*.spec.ts` は Playwright + IPC モック。基盤は M1-2 Task 17 | 生存 |
| 41 | :67 | IPC は `tauriMockScript()` を `page.addInitScript()` に渡す。`mockIPC` は使わない | 生存 |
| 42 | :67 | 理由: `mockIPC` は jsdom 前提で、実ブラウザでは公式が非推奨 | 生存 |
| 43 | :68 | `addInitScript` に渡す関数はブラウザ側で評価される。外側の変数を参照するとモックが空になる | 生存 |
| 44 | :69 | E2E のためだけの `data-testid` を増やさない。既存の `data-session-id` / `data-column` で足りるならそれを使う | 生存 |
| 45 | :70 | キー入力は `page.keyboard.press('Meta+n')`。`ControlOrMeta` は使わない（macOS 専用） | 生存 |
| 46 | :71 | E2E はユニットテストの代替ではない。純関数は vitest、E2E は DOM とライブラリを跨ぐ経路だけ | 生存 |
| 47 | :75 | task-reviewer だけに委ねると運任せになる（契約が却下した形） | 生存 |
| 48 | :75 | 次の 2 条件が両方揃った箇所は、自分で変異を打って赤を確認する | 生存 |
| 49 | :79 | 条件 1: 同一スコープに同じ素の型の値が 2 本以上（局所変数・引数・分割代入を含む） | 生存 |
| 50 | :79 | `string \| undefined` と `string` は同じ 1 種として数える | 生存 |
| 51 | :80 | 条件 2: 名前だけでは正しいほうを判別できない。判定は「取り違えたコードを読んでその場で気づくか」 | 生存 |
| 52 | :82 | 条件 2 が絞りの本体である | 生存 |
| 53 | :82 | （実測）`resizePty` の `cols`/`rows` は該当しない / `invalidateFitCache` の `sid` は該当し vitest 444 本を生き延びた | 退避 |
| 54 | :84 | 手当ては新しいテストではなく、既にある観測を 1 段強める | 生存 |
| 55 | :84 | `toHaveBeenCalled()` → `toHaveBeenCalledWith(<期待値>)` | 生存 |
| 56 | :84 | 期待値には取り違えたら別物になる具体値を使う。`expect.any(String)` は無意味にする | 生存 |
| 57 | :84 | 観測が 1 つも無いならテストの指定そのものが落ちているので 1 本足す | 生存 |
| 58 | :88 | 変異検証は切り出した純関数を守る。守らないのは「その純関数が実際に呼ばれていること」 | 生存 |
| 59 | :90 | （実測）ストア action の呼び出しを差し替える変異が全テスト緑を生き延びた | 退避 |
| 60 | :91 | （実測）リファクタで観測点を切り出すと継ぎ目が無検査のまま残る | 退避 |
| 61 | :93 | 差分が純関数を新設・切り出ししているなら変異は 2 箇所に打つ（中身 / 呼び出し側） | 生存 |
| 62 | :93 | 呼び出し側が緑なら、純関数がどれだけ守られていても製品は守られていない | 生存 |
| 63 | :97 | この順序を守らないと `git checkout --` が実装とテストを丸ごと消す | 生存 |
| 64 | :97 | （実測）M2-3 Task 2 で作業が全部消えた。旧版が未コミット状態の危険を書いていなかった | 退避 |
| 65 | :101 | 手順 1: 変異前に `git status --short` が空を確認。非空なら先にコミット。唯一の陽性の観測点 | 生存 |
| 66 | :102 | 手順 2: 変異は 1 回に 1 箇所。復元はパス指定（`git checkout -- .` と引数なしを打たない） | 生存 |
| 67 | :103 | 手順 3: 復元後に `git status --short` が空。ただし作業消失は検出しない | 生存 |
| 68 | :105 | 手順 3 は事故を検出しない。作業ごと消えても `git status --short` は空になる | 生存 |
| 69 | :106 | 一般則: 破壊的で不可逆な操作は事後の検査では守れない。成功と破壊が同じ出力を出す | 生存 |
| 70 | :107 | レビュー指摘への追加修正も「作業」。修正 → コミット → 変異を追加修正のたびに繰り返す | 生存 |
| 71 | :107 | （実測）コミットせずに変異へ進んで消した例が 2 回 | 退避 |
| 72 | :111 | `> 0` / `all(...)` のような「何でも通る境界」を assert に書かない | 生存 |
| 73 | :111 | （実測）この規則は打つ側に無く、書いた恒真述語は必ず 1 往復してから消える形になっていた | 退避 |
| 74 | :111 | 検出の規則は、そのまま予防の規則になる | 生存 |
| 75 | :113 | 期待値を production と同じ材料から再導出しない | 生存 |
| 76 | :113 | 理由: 定数を動かすと期待値も一緒に動いて検証が黙って消える | 生存 |
| 77 | :113 | （実測）`ACCEPT_ERROR_BACKOFF_*` / `HOOK_EVENTS` | 退避 |
| 78 | :113 | リテラルで固定する | 生存 |
| 79 | :117 | 旧版は恒真述語と期待値の再導出だけを射程にしていた | 退避 |
| 80 | :119 | 性質を断定する文を書くなら、宛先が報告でも `doc` でもテスト名でも production のコメントでも、偽にする変異を 1 本打って赤を確認してから書く | 生存 |
| 81 | :120 | 新しい断定を増やさない。増やすなら上を行う | 生存 |
| 82 | :122 | （実測）PR #74 だけで 6 例。うち 3 例は修正差分が持ち込んだ | 退避 |
| 83 | :126 | （実測）テスト名 `… and never polls` + doc の断定に `setInterval` を注入して 669 全緑 | 退避 |
| 84 | :127 | （実測）修正で書き足したコメントの理由を偽にする変異を誰も打っていない | 退避 |
| 85 | :128 | （実測）report の測定表と production コメントの例示が食い違っていた | 退避 |
| 86 | :130 | 機構: 断定は「検査済み」という印象を作る。読み手は宣言を読んで観測を探さない | 生存 |
| 87 | :131 | 修正ラウンドは、穴を減らしながら同時に増やしうる | 生存 |
| 88 | :132 | 実測されていない「設計上の不可能性」も断定である。作れないことを試作で確かめる | 生存 |
| 89 | :133 | 報告の測定表と production へ書いたコメント・例示は、出す前に突き合わせる | 生存 |
| 90 | :133 | 突き合わせは「報告の数値・例示を production 側で `grep` して同じ値が出ること」で行う | 生存 |
| 91 | :133 | 食い違ったら production を直す前に、どちらが実測かを先に決める | 生存 |
| 92 | :134 | 観測点: 変異の表に `doc` / テスト名 / コメントの断定を宛先とする変異が 1 件以上 | 生存 |
| 93 | :134 | 観測点: 報告に「測定表と production コメントを突き合わせた」の 1 行 | 生存 |
| 94 | :138 | このリポジトリは `diff.external=difft`。`git diff` は 2 カラムの表を描き、`^[-+]` の grep が静かに 0 を返す | 生存 |
| 95 | :138 | （実測）stage5 で 4 回。うち 1 回は team-lead が「差分は無い」と誤判定した | 退避 |
| 96 | :142 | 確認 1: `git diff --no-ext-diff -- <変異したファイル>` で変異行が `-`/`+` 両側に現れることを目視する | 生存 |
| 97 | :142 | 2 箇所同時に打ったとき、片方が無言で不適用でも「非空」は成立する | 生存 |
| 98 | :143 | 確認 2: 置換のアンカー文字列がそのファイルで一意であることを `grep -c` で確かめる | 生存 |
| 99 | :143 | （実測）修正が既存ブロックを複製し、一意だったアンカーが 2 箇所・6 箇所に増えた | 退避 |
| 100 | :145 | 観測点: 変異報告に `--no-ext-diff` を付けたコマンドと「目視した」の 1 行 | 生存 |
| 101 | :148 | （実測）`cd <サブディレクトリ> && … && git diff -- <repo 相対パス>` が適用済みでも空を返した | 退避 |
| 102 | :149 | 変異の目視は `git -C <リポジトリ絶対パス> diff --no-ext-diff -- <repo 相対パス>` の形で打つ。手順の中で `cd` しない | 生存 |
| 103 | :150 | 空の diff を見たら、まずパススペックが cwd に対して正しいかを疑う | 生存 |
| 104 | :150 | 空を「未適用」と読んで打ち直すと、適用済みの変異の上に 2 つ目を重ねる | 生存 |
| 105 | :151 | 観測点: 目視コマンドに `cd ` を含む `git diff` が 0 件 | 生存 |
| 106 | :155 | `<検証コマンド> \| tail -n` の終了コードは `tail` のもの。検証は単独で走らせ `$?` を読む。出力はファイルへ落とす | 生存 |
| 107 | :156 | （実測）`cargo test` が 8 件しか見えなかった（実際は 783）。clippy が落ちていても緑と報告する形だった | 退避 |
| 108 | :157 | 「終了ステータス 0」「出力が空」「件数が同じ」は、それ自体では成立の証拠にならない | 生存 |
| 109 | :157 | 既存コマンドを証拠として使う前に「不合格のとき何が見えるか」を 1 行書く | 生存 |
| 110 | :158 | 観測点: 報告の検証欄の各行に `exit <n>` が書かれている | 生存 |
| 111 | :162 | （実測）群 H は `lane-controller.md` にしか無かった（`74f9a73` 時点） | 削除（規則ではなく昇格時の経緯） |
| 112 | :164 | brief のコード片に出てくる型・関数・API は、着手前に実コードへ `grep -n` で当てる。当たらなければ BLOCKED | 生存 |
| 113 | :165 | （実測）#148 の `SessionManager` / stage4 で 5 件 | 退避 |
| 114 | :166 | 観測点: 報告に、brief のコード片が呼ぶ型・関数を `grep -n` で当てた出力が在る | 生存 |
| 115 | :170 | レビューの処方を逐語で採用する前に「この処方が閉じる族はどこまでか。同じ族の残りは何か」を 1 行書く。書けないなら聞き返す | 生存 |
| 116 | :171 | （実測）#155 / #132 / #72 | 退避 |
| 117 | :172 | 観測点: 逐語採用した処方について「同型の残り」を `grep -c` で測った出力が報告に在る | 生存 |
| 118 | :176 | 契約の章番号・ファイルの行番号を production / test / コメントへ書いたら、その節を開いて逐語を 1 行引く | 生存 |
| 119 | :177 | 「旧い文字列が `grep` で 0 件になった」は置換が起きた証拠であって、置換先が正しい証拠ではない | 生存 |
| 120 | :178 | （実測）#149（誤引用を 5 箇所へ転記）/ #150（その手当てで 2 箇所が別の誤参照になった） | 退避 |
| 121 | :179 | 観測点: 参照を書いた差分の報告に `grep -n '<引いた逐語>'` の出力が 1 参照につき 1 行 | 生存 |
| 122 | :183 | （実測）本ファイルには群 P の同等記述が無い（`74f9a73` 時点） | 削除（規則ではなく昇格時の経緯） |
| 123 | :185 | 変異・陽性の対照を打つ前に「測りたい性質が偽のとき、この観測は何を出すか」を 1 行書く | 生存 |
| 124 | :185 | 真のときと同じ出力になるなら、その観測は証拠にならない | 生存 |
| 125 | :186 | さらに 3 つを当てる: production 編集かテスト編集か / 他の分岐に masking されないか / 他の変異と同一の編集に潰れないか | 生存 |
| 126 | :187 | （実測）#129 / #130 / #91・#161 / #184 / #193 / #206 | 退避 |
| 127 | :188 | 観測点: 変異報告の各変異に「この変異が緑になる条件」の 1 行 | 生存 |
| 128 | :192 | （実測）優先順位がどこにも書かれていなかった。stage5 で 4 回 | 退避 |
| 129 | :194 | 食い違いに気づいたら、自分で解釈して進めず BLOCKED として lane-controller へ返す | 生存 |
| 130 | :194 | 例外が要るなら、先に定義ファイル側へ例外を書かせる | 生存 |
| 131 | :195 | 理由: 受け手ごとに解釈が割れ、割れたことは割れた後でなければ分からない | 生存 |
| 132 | :195 | （実測）PR #74 では 10 本のレビューのうち 1 本だけが別の解釈を採った | 退避 |
| 133 | :196 | 観測点: 食い違いの報告が BLOCKED として上がっている | 生存 |
| 134 | :198 | 閾値が無いと、書式の追加要求まで BLOCKED になる | 生存 |
| 135 | :202 | 閾値 1: 本ファイルが禁止していることを dispatch が要求する → 食い違い。BLOCKED | 生存 |
| 136 | :203 | 閾値 2: 本ファイルが必須としていることを dispatch が免除する → 食い違い。BLOCKED | 生存 |
| 137 | :204 | 閾値 3: 本ファイルに定義の無い追加項目・追加書式 → 食い違いではない。両方満たす | 生存 |
| 138 | :205 | 閾値 4: 本ファイルより厳しい条件 → 食い違いではない。従う | 生存 |
| 139 | :207 | 行 1・2 は行 3・4 に優先する。禁止している形なら行 1 が先に当たる | 生存 |
| 140 | :208 | 既知の例外: 陽性の対照として production とテストの 2 箇所同時を要求する形は認められた例外 | 生存 |
| 141 | :208 | 例外の条件 3 つ（箇所ごとに確認を別々に / 表を分けて記録 / 単独で打った結果も併記）。満たせないなら BLOCKED | 生存 |
| 142 | :212 | （実測）stage5 で 5 回。あなたの側の形は #144 | 退避 |
| 143 | :212 | `rust-implementer.md` と同じ規則である。2 つの実装ロールは同型なので片側にだけ置かない | 生存 |
| 144 | :214 | 報告・コメント・JSDoc を出す前に、そこに書いた規則をその文章自身へ 1 回当てる。「読んだ」ではなく「当てた」 | 生存 |
| 145 | :215 | 破りは同じ文書の中で起きている。別の便へ伝播する前に読み返せば捕まる | 生存 |
| 146 | :216 | 観測点: 報告に「自分の書いた規則を自分の出力へ当てた結果」の 1 行 | 生存 |
| 147 | :218 | 手当ては「気をつける」ではなく、打つコマンドの形にする。警告文だけで閉じた項目は次の便で同じ形を打たれる | 生存 |
| 148 | :219 | （実測）群 U は 5 ファイル全部に在るのに同日に両側で再発した（#196 / #201） | 退避 |
| 149 | :220 | 観測点: 罠を記録した報告に回避する具体コマンドが 1 行。「注意する」で終わっている手当てが 0 件 | 生存 |
| 150 | :224 | lint はコミット前に必ず通す。CI で初めて気づくのは往復が無駄 | 生存 |
| 151 | :227 | `npm run lint && npm run fmt:check && npx tsc --noEmit` | 生存（逐語） |
| 152 | :230 | `no-explicit-any` と `no-non-null-assertion` は `error`。`any` と `!` は型の嘘で実行時に silent に壊れる | 生存 |
| 153 | :230 | 必要なら `// eslint-disable-next-line` に理由を添える。ルールを設定ファイル側で緩めない | 生存 |
| 154 | :234 | 契約 0 章の「アイドル CPU ほぼ 0%」はフロントにも効く | 生存 |
| 155 | :234 | `setInterval` による定期再描画や、PTY 出力がないときに走るループを作らない | 生存 |
| 156 | :234 | Zustand のセレクタはプリミティブを返す形にし、オブジェクト全体を select しない | 生存 |
| 157 | :238 | 実機確認が要るタスクは着手せず BLOCKED として lane-controller に返す | 生存 |
| 158 | :240-242 | 実機確認の 3 項目（キー奪取 / 通知のクリック応答 / 起動時間・メモリ） | 生存 |
| 159 | :246 | レポート本文は渡されたレポートファイルに書く。呼び出し元には短い形だけを返す | 生存 |
| 160 | :249-253 | 呼び出し元へ返す形（STATUS / COMMITS / TESTS / CONCERNS） | 生存（逐語） |
| 161 | :255 | 同じ `STATUS: … (round: <トークン>)` の 1 行をレポートファイルにも書く。lane-controller の起床条件 | 生存 |
| 162 | :257 | トークンは lane-controller が dispatch で渡す。渡されていなければ書く前に聞く。自分で作らない | 生存 |
| 163 | :258 | この行を書いたら作業を終える。書いてから続けない。lane-controller はこの行を見た瞬間に次工程へ進む | 生存 |
| 164 | :258 | （実測）stage4 で 4 回。dispatch に禁止を逐語で書いたうえで起きたので規律ではなく順序の問題 | 退避 |
| 165 | :259 | `REPORT_WRITTEN_AT: <YYYY-MM-DD HH:MM>` をその直前の行に置く | 生存 |
| 166 | :259 | 理由: 報告ファイルは git 管理外で版が残らないので、記述が偽になった時点を事後に切り分けられない | 生存 |
| 167 | :260 | 実行していない検証の出力を書かない。走らせていないなら「未実施」と書く | 生存 |
| 168 | :262 | 差分やコード全文を報告に貼らない | 生存 |
| 169 | :2 | `name: web-implementer`（dispatch がこのエージェントを呼ぶ識別子） | 生存 |
| 170 | :147 | 「上の観測点だけでは足りない」（`cd` を外す規則の導入文） | 生存 |
| 171 | :133 | 同じ測定を 2 箇所へ書くと、片方だけ古くなる | 生存 |
| 172 | :134 | 0 件の差分で新しい断定が増えていたら、それは測っていない | 生存 |
| 173 | :162 | 渡す側だけに置くと、受け取る側は brief どおりに書き始めてしまう（規則が受け手側に要る理由） | 生存 |

**内訳と件数はここに書かない。** 冒頭の宣言（`:7`）と、下の導出コマンドの出力を突き合わせること。

同じ数字を 2 箇所に書くと、行を足す便が片方だけ直す。PR #95 と本 PR で 2 回起きた（冒頭 58 / 末尾 86、冒頭 170 / 末尾 168）。

```bash
# ラベル列は code span を退避させてから分割する。素の split('|') では割れる。
python3 - <<'EOF'
import re
from collections import Counter
rows=[l.rstrip() for l in open('web-inventory.md',encoding='utf-8') if re.match(r'^\| \d+ \|', l)]
c=Counter(); 
for l in rows:
    safe=re.sub(r'`[^`]*`', '\x00', l)
    c[[x.strip() for x in safe.strip().strip('|').split('|')][-1][:2]]+=1
ids=[int(re.match(r'^\| (\d+) \|',l).group(1)) for l in rows]
print(len(rows), dict(c), sorted(set(range(1,max(ids)+1))-set(ids)), [k for k,v in Counter(ids).items() if v>1])
EOF
```

**#171 / #172 / #173 はレビューが見つけた欠落である。** どちらも台帳が旧 `:133` / `:134` を項目 89〜93 に分解した際、**行は引いたのに行の中の 1 節を落としていた**形。補集合（台帳が指さない行の機械列挙）では出ない —— 行は指しているため。**文単位の生存検査でしか出ない。**

**#173 は別の経路で出た。** 旧 `:162` は 1 行に 2 節あり、第 1 節（昇格の経緯）を `#111` が「削除」で拾っているため、**行としては台帳が指していた。** レビュアーが削除・退避と分類された 32 行の中身を節へ割って当て直して見つけた。

**訂正。** 当初ここに「分類が『削除』なので文単位の生存検査の対象からも外れていた」と書いたが偽だった。生存検査は台帳を読まず旧版・新版・退避先だけを入力に取るので、分類が対象を狭めることは起きない。**実際その文は生存検査の候補一覧に載っており、落ちたのは判定のほう。** **手当ては 4 本目の検査ではなく、候補文の分類を行の分類から継承しないという判定の規則である。**

削除 2 件（`#111` / `#122`）は、どちらも「昇格した当時、この規則が本ファイルに 0 行だった」という経緯の記述である。規則ではなく昇格の履歴なので落とした。**理由に「他所に残っている」を書かない** —— 1 本目でその裏付けを 2 度誤ったため。
