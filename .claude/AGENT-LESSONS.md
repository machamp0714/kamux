# エージェント定義の学び（AGENT-LESSONS）

**このファイルは「事象」を溜める場所であって、規則を書く場所ではない。** 規則の形にするのは昇格時である。

設計は `docs/superpowers/specs/2026-08-05-agent-self-improvement-design.md`（**git 管理外**。設計書は git で管理しない方針なので、clone しただけの環境には無い）。

## 正典の分界

> **作るものの話（仕様・型・命名・順序・値）は契約** `docs/superpowers/plans/2026-08-01-kamux/00-contracts.md`。
> **作り方の話（誰がいつ何を確認するか）が本ファイルと `.claude/agents/*.md` / `.claude/skills/kamux-lane/SKILL.md`。**

**契約に書けば防げたものを本ファイルに書かない。** それは契約変更要求であり、contract-owner の専管である。

## 書き方

| 規則 | 内容 |
|---|---|
| **事象で書く** | 「◯◯すべき」ではなく「◯◯が起きた」。規則の形で溜めると微妙に違う規則が並び、**2 件目が同じ穴かどうかを判定できなくなる** |
| **1 行に収める** | **表のセルは改行を含められない。** 改行した瞬間に表が壊れる |
| **0 件なら何も書かない** | 「特になし」の行を残さない。**0 件は正常な結果である。** 残すとファイルが「なし」で埋まり、実在の事象が埋もれる |
| **棄却も行として残す** | 一度考えて捨てた判断を消すと、**同じ議論が再燃する**（契約が全章に却下記録を持つのと同じ理由） |

## 入力源

| 源 | `出典` 列に書く値 | 誰が書くか | どこへコミットするか |
|---|---|---|---|
| PR レビューの `定義の穴:` 欄 | `PR #<n>` | lane-controller | **その PR のブランチ。** 時点は `lane-controller.md` が持つ |
| **ユーザーからの指摘** | `user` | team-lead | **ブランチを切って PR にする。** ローカル `main` に直接コミットしない（契約 §43.3） |
| PR のマージ手順で出たもの | `PR #<n> のマージ` | マージしたレーン | 同上 |
| ステージ完了ゲートで出たもの | `stage<N> ゲート` | team-lead | 同上 |
| レーン運用中に team-lead 自身が起こしたもの | `team-lead` | team-lead | 同上 |
| 契約の裁定過程で出たもの | `契約 §<n>` | team-lead | 同上 |
| 特定タスクの実行中に出たもの（PR 番号がまだ無い） | `<フェーズ> Task <N>` / `PR H<n>` | lane-controller | 同上 |

> **🔴 この表は 2026-08-10 に書き直した。台帳 #36（語彙表に無い値が使われて 18 行に増えた）と**同じ機構の 2 回目**である。**
> **#36 は `昇格` 列について起き、PR #57 で昇格した。本件は `出典` 列で、昇格後に別の列で再発した。** 昇格時に**列を 1 本しか見ていなかった。**
> **【規則】`出典` は「その事象がどの工程で観測されたか」を後から辿れる語であればよい。上の 7 行に無い工程が生まれたら、行を足してから使う。**
> **⚠️ 語彙を増やすときは、**その語で grep して何件出るか**を確かめること。1 件しか出ない語は、工程ではなく個別の出来事を書いている。

**ゲートでの棄却・昇格の記入も PR 経由で行う。** 未コミットのまま残すと、次にこのリポジトリで走るレーンの検証 A（`git status --porcelain` が空）に他人が引っかかる。

## 昇格

**判定の手順・閾値・昇格 PR の要件・人間承認ゲートは `.claude/skills/kamux-promotion/SKILL.md` が持つ。本ファイルには書き写さない** —— 書き写した瞬間に二重の正典になり、片方だけ直せば静かに乖離する。**判定を起動する時点（ステージ完了ゲート）は `.claude/skills/kamux-lane/SKILL.md` の「ステージ完了ゲート」が持つ。**

**本ファイルが持つのは `昇格` 列の語彙だけである。**

| 値 | 意味 | 誰が・いつ書くか |
|---|---|---|
| `—` | 未昇格。ゲートが数える対象 | 転記した時点の初期値 |
| `昇格済み（PR #<n>）` / `昇格済み（PR #<n> / 群 <X>）` | 定義ファイルへ反映済み。**複数事象を 1 群として昇格したときは群ラベルを付ける**（棚卸し表の規則と対応づけるため） | **昇格 PR 自身がこの列を書き換える**（同じ PR に含める） |
| `棄却（<理由>）` | 昇格しないと判断した。**行は消さない** | ゲートで判断した team-lead |
| `契約 §<n> として採用（PR #<m> …）` | **作り方ではなく作るものの話だった。** 契約変更要求として上げ、contract-owner が裁定した | 裁定を受けて定義側へ反映した PR |

> **`昇格済み` はマージ後に書くのでは遅い。** マージコミットの sha はマージするまで存在しないので、それを待つと**行は `—` のまま残り、次のステージのゲートが同じ行を数えて同じ昇格 PR をもう一度作る。** だから PR 番号を使い、**昇格 PR の中で書き換える。**

## 事象

| # | 日付 | ステージ | 出典 | 事象 | 穴のあった定義 | 昇格 |
|---|---|---|---|---|---|---|
| 1 | 2026-08-06 | — | PR #54 | 計画の逐語ブロックを適用するとき、置換行の前後を読まずにコミットし、同一節内に矛盾が残った | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #78 / 群 γ） |
| 2 | 2026-08-06 | — | PR #54 | `.claude/` の定義ファイル変更を team-lead がインラインで実装し、implementer もタスク単位レビューも経由しなかった（`SKILL.md` §7 の禁止は「コードを直す」としか書かれておらず、定義ファイルが該当するか読み取れない） | `SKILL.md` | 昇格済み（PR #69 / 群 M） |
| 3 | 2026-08-06 | — | PR #54 | レビュー指摘の文言だけを直して記述位置（runbook の実行順）を直さず、上から実行する読み手には同じ失敗経路が残った | `lane-controller.md` | 昇格済み（PR #78 / 群 γ） |
| 4 | 2026-08-06 | — | PR #54 | 二重の正典を 1 ファイルから削った同じコミットで、同じ閾値が別ファイルに新設された | `SKILL.md` / `CLAUDE.md` | 昇格済み（PR #78 / 群 λ） |
| 5 | 2026-08-06 | stage3 | PR #55 | rust-implementer が報告に貼ったドリフト検査 10 の grep 逐語が、コミット済みの最終コードで実行した結果と一致しなかった（実装途中の結果を貼っており、最終形では 2 ファイルで不合格だった） | `rust-implementer.md` | 昇格済み（PR #57 / 群 D） |
| 6 | 2026-08-06 | stage3 | PR #55 | lane-controller が implementer の報告の逐語を検証せずに team-lead へ転記し、ドリフト検査の不合格を緑として報告した（検査を自分では 1 度も実行していなかった） | `lane-controller.md` | 昇格済み（PR #57 / 群 D） |
| 7 | 2026-08-06 | stage3 | PR #55 | team-lead が繰り越し項目の受け手を名指ししたが、その項目に既に着手していた実行中の別エージェント（contract-owner）が居るかを確認しておらず、受け手が 2 人になりうる状態を作った | `SKILL.md` | 昇格済み（PR #78 / 群 ε） |
| 8 | 2026-08-06 | stage3 | PR #55 | 監視ループの完了通知は届いていたが、レーンが次の一手を打たずに 82 分停止した（`lane-controller.md` の起床手段の節は機構を張ることは求めているが、起床後に次の一手を打つことは求めていない） | `lane-controller.md` | 昇格済み（PR #57 / 群 A） |
| 9 | 2026-08-06 | stage3 | PR #55 | 前回の停止の直後、レーンが自ら再発防止を宣言した次の区切り（Task 4 クローズ後）で同じ停止が再発した。宣言では閉じない性質の穴である | `lane-controller.md` | 昇格済み（PR #57 / 群 A） |
| 10 | 2026-08-06 | stage3 | PR #55 | タスク境界（実装完了 → レビュー dispatch）で 3 度目の停止。3 回とも同じ遷移点であり、レーンが再発防止を宣言した後も同じ場所で起きた | `lane-controller.md` | 昇格済み（PR #57 / 群 A） |
| 11 | 2026-08-06 | stage3 | PR #55 | task-reviewer が契約 §77.3 を「`keyRouting.test.ts` への追加も禁止」と読み、是正可能な指摘を「修正禁止・記録のみ」に分類した（同節が禁じているのは `registry.ts` への変更範囲と `registry.test.ts` の既存テスト削除のみで、`keyRouting.test.ts` への追加は裁量として明示的に許されている） | `task-reviewer.md` | 昇格済み（PR #78 / 群 η） |
| 12 | 2026-08-06 | stage3 | PR #55 | team-lead が `SendMessage` で渡した裁定がリポジトリ上に残らず、task-reviewer が `TEAM-LEAD-RULINGS.md` を grep しても確認できないため「承認条件が満たされていない可能性」として検証不能扱いになった | `SKILL.md` | 昇格済み（PR #57 / 群 F） |
| 13 | 2026-08-06 | stage3 | PR #55 | lane-controller が team-lead の裁定を報告へ要約した際に意味が反転した（「契約の表に無い 4 つ目の変更」→「契約の射程内」）。反転後の記述はドリフト検査を素通りさせるため、申告を課した目的そのものが消える | `lane-controller.md` | 棄却（群 A-2「中継の言葉は対象ごとに書き分ける」（PR #69）と群 E の書き直し（PR #75）で射程に入った。裁定の中継が意味を保存することは両者を当てれば測れる） |
| 14 | 2026-08-06 | stage3 | PR #55 | フェーズを通じて `npm run lint` / `npm run fmt:check` が一度も実行・報告されず、team-lead が PR 作成直前に初めて走らせた（レーンの報告は vitest / tsc / e2e / cargo のみだった） | `lane-controller.md` | 昇格済み（PR #57 / 群 H） |
| 15 | 2026-08-06 | stage3 | PR #55 | PR 本文が「すべて最終コミットに対して実行」と宣言しながら、`vitest`（392 / 実測 397）と `cargo test`（361 / 実測 363）の 2 つが数コミット前の値だった。PR 本文はマージ後も残る恒久的な記録であり、報告や ledger と違って訂正の機会が乏しい | `lane-controller.md` | 昇格済み（PR #57 / 群 D） |
| 16 | 2026-08-06 | stage3 | PR #55 | 全差分レビューのパッケージ生成後に team-lead の指摘でブランチへコミットが積まれ、レビュー対象と実物がずれた（レビュアーが新 tip で再検証して吸収したが、そうする取り決めは無かった） | `task-reviewer.md` / `SKILL.md` | 昇格済み（PR #78 / 群 δ） |
| 17 | 2026-08-06 | stage3 | PR #56 | 純粋関数は変異検証で守られていたが、それを呼ぶ側（Rust の `resolve_cwd` 配線 / TS のストア action）が実際に呼んでいることを守るテストが無く、呼び出しを差し替える変異が全テスト緑を生き延びた（Task 4 / Task 6 の 2 件） | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #69 / 群 S） |
| 18 | 2026-08-06 | stage3 | PR #56 | lane-controller がレビューへ指定した変異は全部赤だった一方、レビュアーが自分で設計した変異が Important 4 件すべてを暴いた（指定分は既に心配されている箇所しか見ず、自発分が誰も気づいていない箇所を発見した） | `task-reviewer.md` / `lane-controller.md` | 昇格済み（PR #57 / 群 B） |
| 19 | 2026-08-06 | stage3 | PR #56 | E2E の共有モックが実 API の呼ぶ内部フックを持たず、`unlisten` が必ず TypeError になる状態が M1-2 の導入以来 4 フェーズ分放置されていた（その経路を踏む spec が書かれるまで発現しなかった） | `SKILL.md` | 棄却（契約 §128.7。§26.2 の【必須】は `invoke` の戻り値 × §7/§7.1 に限られ #19（`listen` が返す `unlisten` の内部フック）を覆わない。射程を広げない理由は消費者 0 —— 未着手は M3-4 のみで `M3-4-ops-ux.md` の `e2e` は 0 件。知識は `e2e/support/tauriMock.ts:84-87` の逐語コメントに在り、契約より読み手に近い） |
| 20 | 2026-08-06 | stage3 | PR #56 | レイアウトを守るはずの E2E assert が `> 0` で書かれており、実測で高さが 665 から 288 へ退化しても緑のままだった（`> 0` は「何でも通る境界」であり assert の形をした空振りだった） | `web-implementer.md` | 昇格済み（PR #57 / 群 E） |
| 21 | 2026-08-06 | stage3 | PR #56 | team-lead が「クラス名を新しいものに置き換えたので assert の守りが戻る」として承認したが、置き換え後の assert も恒真であり、変異を打つまで誰も気づかなかった | `SKILL.md` | 昇格済み（PR #57 / 群 E） |
| 22 | 2026-08-06 | stage3 | PR #56 | 手動スモークの項目が契約と逆の期待値を持っており（`Cmd+J`/`Cmd+K` が editor 画面で効く、と記載）、そのまま実施していれば契約どおりの正しい実装が不合格と判定されるところだった | `lane-controller.md` | 昇格済み（PR #57 / 群 C） |
| 23 | 2026-08-06 | stage3 | PR #56 | 手動スモークの手順（PATH から nvim を外す）が実装のどの分岐にも届かず必ず空振りする状態だった（探査がログインシェルから PATH を組み直し、フォールバック 4 ディレクトリも無条件に追加するため） | `lane-controller.md` | 昇格済み（PR #57 / 群 C）（🔴 ただし捕まえられていない。#32 参照。**PR #69 で人間ゲートの検査 4「前提条件の到達可能性」として再昇格し、表の 2 行目からの帰属を外した**） |
| 24 | 2026-08-07 | stage3 | user | 手動スモークの期待結果が「nvim が開く」までしか書かれておらず、PTY が 80×24 のまま描画領域が画面の一部しか埋めない状態でも合格として通る状態だった（ユーザーが目視で気づかなければ見逃していた） | `SKILL.md` | 昇格済み（PR #57 / 群 C） |
| 25 | 2026-08-07 | stage3 | user | brief が指定した変異 3 件は全部赤だったが、レビュアーが自分で設計した変異が修正の核心行（`resizePty` の引数）の無防備を暴いた。**#18 と同じ穴の 2 回目** | `task-reviewer.md` / `lane-controller.md` | 昇格済み（PR #57 / 群 B） |
| 26 | 2026-08-07 | stage3 | PR #56 | PR 作成前に実施するはずの契約 §32 条件 3（PR 単位レビュー）が実施されず、ledger には「Task 8 のレビュー後・PR 24 作成前に実施する。いま dispatch しない」という予告だけが残ったままレーンが終了した（実施済みか未実施かを後から判別できる記録が求められていない） | `lane-controller.md` / `SKILL.md` | 昇格済み（PR #57 / 群 F） |
| 27 | 2026-08-07 | stage3 | PR #56 | 同一スコープに同じ型の局所変数が 2 本ある呼び出し（`invalidateFitCache(sid)` に対する `sessionId`）の取り違えを、実装者の 5 変異・レビュー 2 ラウンドとも一度も試さず vitest 444 本を丸ごと生き延びた。カテゴリ 3 の判定基準が「構造体のフィールド」に限定して書かれていた | `task-reviewer.md` / `web-implementer.md` | 契約 §81 として採用（PR #57 で定義側へ反映） |
| 28 | 2026-08-07 | stage3 | PR #56 | アサーションの反転（`toHaveBeenCalledTimes(1)` → `not.toHaveBeenCalled()`）はテスト本数が変わらないため、「本数が減っている差分では統合か検証の消失かを判定する」既存規則の網に掛からなかった | `task-reviewer.md` | 昇格済み（PR #78 / 群 β） |
| 29 | 2026-08-07 | stage3 | PR #56 | 実装変更に対する変異が多行の手編集になり、`git checkout --` で機械的に戻せず逆編集で復元する形になった（復元の正しさを `git diff --stat` の一致でしか確かめられない） | `task-reviewer.md` | 昇格済み（PR #69 / 群 B） |
| 30 | 2026-08-07 | stage3 | user | M3-1 のフェーズ完了報告が出ないままレーンが終了し、契約 §26.5-3 が求める「E2E に移した項目 / 手動に残した項目」の仕分け記録が残らなかった（仕分け自体は行われていたが記録が無い） | `lane-controller.md` / `SKILL.md` | 昇格済み（PR #57 / 群 F） |
| 31 | 2026-08-07 | stage3 | PR #57 | 昇格 PR で「同じ穴」の粒度を判定する基準が無く、別機構 3 件を 1 群に寄せたか同一機構 3 回かを検算できないまま回数が閾値を超えた | `SKILL.md` | 昇格済み（PR #57 / 群 G） |
| 32 | 2026-08-07 | stage3 | PR #57 | 書き直した規則が、回数を作った 3 件目（#23「観測点は実在するが前提条件を作れない」）を捕まえられない —— 検算①（観測点を grep する）では #23 は空振りせず合格してしまう | `SKILL.md` | 昇格済み（PR #69 / 群 L） |
| 33 | 2026-08-07 | stage3 | PR #57 | ステージ完了ゲートの実施記録を書く場所がどこにも定義されておらず、確認 1（ドリフト検査）が「本 PR の後」という予告のまま恒久記録に残った | `SKILL.md` | 昇格済み（PR #78 / 群 ι） |
| 34 | 2026-08-07 | stage3 | PR #57 | `.claude/` の定義ファイル変更を team-lead がインラインで実装する形が PR #52 / #54 / #57 と 3 度続いたが、台帳 #2 は — のまま据え置かれ、閾値 2 回が発火しなかった（同じ事象を再記録する規則が無い） | `SKILL.md` | 昇格済み（PR #69 / 群 M） |

| 35 | 2026-08-07 | stage3 | PR #57 | 昇格 PR の本文を後続コミットの後に取り直す規則が無く、タイトル・群の表・台帳の状態・確認 1 の欄が最終状態と食い違ったまま残った（同一 PR 内で 3 回） | `SKILL.md` | 昇格済み（PR #57 / 群 I） |
| 36 | 2026-08-07 | stage3 | PR #57 | 台帳の語彙表が `昇格済み（PR #<n>）` しか登録しておらず、実際に使われた群ラベル付きの形が未登録のまま 18 行に増えた（前ラウンドの `訂正済み` 未登録と同型・規模 18 倍） | `AGENT-LESSONS.md` | 昇格済み（PR #57 / 群 I）（🔴 語彙表は昇格後に**別の列（`出典`）で再発**した。**PR #69 で入力源の表を書き直した** —— 昇格時に列を 1 本しか見ていなかった） |
| 37 | 2026-08-07 | stage3 | PR #57 | 閾値表に「同じ PR のレビューで出た穴を同じ PR で直したとき `昇格` 列に何を書くか」の行が無く、1 回の事象が `昇格済み` として閉じられた | `SKILL.md` | 昇格済み（PR #57 / 群 I） |
| 38 | 2026-08-07 | stage3 | PR #57 | `rust-implementer.md` に変異検証の記述が 1 つも無く、契約 §81.2 の 2 条件が打つ側の Rust に無い（§72 の原型は Rust の `Session.id` / `project_id` だった） | `rust-implementer.md` | 昇格済み（PR #69 / 群 K） |
| 39 | 2026-08-08 | stage4 | PR #60 | 変異を `git checkout --` で戻した直後に再ビルドせず `target/debug/` のバイナリを直接起動し、変異後のバイナリで測って実在しないバグと誤認しかけた（1 回。復元はソースを戻すがビルド成果物は戻らない） | `task-reviewer.md` / `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #69 / 群 C） |
| 40 | 2026-08-08 | stage4 | PR #60 | `cargo test --workspace` を `--no-fail-fast` 無しで回したため、先頭 target が赤になった時点で残り 3 target が黙って飛ばされ、母数が過小のまま報告されかけた（1 回） | `task-reviewer.md` / `rust-implementer.md` | 昇格済み（PR #78 / 群 β） |
| 41 | 2026-08-08 | stage4 | PR #60 | 変異と無関係の先在フレークが変異検証中に赤くなり、変異由来の赤と混ざって母数が実行ごとに変わった（1 回。無改変ツリーでも再現したため変異の効果と切り分け直した） | `task-reviewer.md` / `rust-implementer.md` | 昇格済み（PR #69 / 群 N） |
| 42 | 2026-08-08 | stage4 | PR #60 | 定義が前提しているツール・形式が実際には成立せず、変異や後片付けが空振りした（レビュアーに `Edit` が無い / review package が `git diff` 形式でなく描画済みの表で保存され `grep '^diff --git'` が 0 件 / 変異が `let_underscore_lock` lint に弾かれ赤を測る前にビルドが落ちた / 後片付けの glob が zsh の no-match でコマンド全体を中断し終了ステータスから分からなかった。計 4 回） | `task-reviewer.md` | 昇格済み（PR #69 / 群 R） |
| 43 | 2026-08-08 | stage4 | PR #60 | レビュー指摘への追加修正をコミットせずに変異検証へ進み、復元の `git checkout --` でその修正が丸ごと消えた（1 回。M2-3 Task 2 と同型で 2 回目。定義の順序「1. 先にコミット → 2. 変異」を追加修正の分だけ飛ばしていた） | `rust-implementer.md` / `web-implementer.md` / `task-reviewer.md` | 昇格済み（PR #69 / 群 B） |
| 44 | 2026-08-08 | stage4 | PR #60 | lane-controller が破壊的操作（`git checkout --`）の直前に確認点を置き、変異適用後は必ず偽になる実行不能な条件をレビュアーに課した（1 回。述語が要るのは復元の直前ではなく変異の直前だった） | `lane-controller.md` | 昇格済み（PR #69 / 群 B） |
| 45 | 2026-08-08 | stage4 | PR #60 | lane-controller が測らずに書いた事実を下流が仕様として読んだ（review package 切り出し後に implementer のコミットが 1 本増え、レビュアーが古い diff を仕様として読んだまま判定を進めた / 修正差分を「2 コミット」と書いたが実際は 4 コミットで `--stat` の数字だけが一致していた。計 2 回） | `lane-controller.md` | 昇格済み（PR #69 / 群 A） |
| 46 | 2026-08-08 | stage4 | PR #60 | リファクタで観測点を純関数へ切り出した結果、レビューが名指しした変異箇所が意味的に 2 分割され、切り出し元と切り出し先の継ぎ目が無検査のまま残った（1 回。追加変異を打つまで気づかなかった） | `rust-implementer.md` / `task-reviewer.md` | 昇格済み（PR #69 / 群 S） |
| 47 | 2026-08-08 | stage4 | PR #60 | 実測されていない「設計上の不可能性」が報告に書かれ、相手が試作で反例を作った（implementer の「DI で分解してもテストが恒真になるだけ」をレビュアーが 6 行で反証 / レビュアーの「`static Mutex` の直列化で閉じる」を implementer が原因機構で反証。計 2 回・双方向） | `task-reviewer.md` / `rust-implementer.md` | 昇格済み（PR #69 / 群 E） |
| 48 | 2026-08-08 | stage4 | PR #60 | 診断プローブや対照入力そのものが判別力を持たず、仮説が正しくても再現しない形になった（出力条件が正当に無音の否定テストまで拾い最初の 120 回が全件ヒットで判別力ゼロ / 観測スレッドが callsite の初回登録を先に取る構造のプローブを 2 本続けて作った / 否定テストの対照でメッセージが quiet 側と loud 側で同一のため条件反転の変異が緑になった。計 3 回） | `task-reviewer.md` / `rust-implementer.md` | 昇格済み（PR #69 / 群 P） |
| 49 | 2026-08-08 | stage4 | PR #60 | レビュー指摘への修正として追加したテストハーネス自体が並列実行下で間欠に「イベント 0 件」を返し、契約 §84.1.1 の条件反転を塞ぐ否定テスト 3 本が確率的に恒真になった（1 回。削除変異が出す赤とも区別が付かなかった） | `task-reviewer.md` / `rust-implementer.md` | 昇格済み（PR #60） |
| 50 | 2026-08-08 | stage4 | PR #60 | 既存の間欠テストが本ラウンドで触った箇所と同じ errno で落ち、前ラウンドのレビューが使った「別ディレクトリだから無関係」という切り分け根拠が変更後は流用できなくなっていた（1 回。grep で別関数と確認して切り分け直した） | `task-reviewer.md` | — |
| 51 | 2026-08-08 | stage4 | PR #60 | implementer が報告の最終行に `STATUS: COMPLETE` を書いたあとも作業を続けてコミットを足し、lane-controller が古い差分をレビュアーへ渡した／PR 本文の数値を 1 コミット前の値で書いた（本 PR 内で 2 回。dispatch に禁止を逐語で入れたうえで再発し、検出したのは team-lead の完了通知との SHA 突き合わせだった） | `rust-implementer.md` / `web-implementer.md` / `task-reviewer.md` | 昇格済み（PR #69 / 群 A） |
| 52 | 2026-08-08 | stage4 | PR #60 | 守られていない性質の主張が、テスト側の doc と名前だけでなくそれが説明する実装側の関数 doc にも書かれており、テスト側を直したあとに 2 箇所目が残っていた（1 回。転記の連鎖であって単独の書き間違いではなく、同 PR の `RAW_DEBUG_CAP` の doc も同型だった） | `task-reviewer.md` / `rust-implementer.md` | 昇格済み（PR #69 / 群 E） |
| 53 | 2026-08-08 | stage4 | PR #60 | レビュー完了後に起きた事象はレビュアーの `定義の穴` 一覧に載りようがなく、lane-controller が一覧を写すだけの転記を行ったため 2 件が漏れた（1 回。漏れた分は次の PR で回収したので、その間 main には無かった） | `lane-controller.md` | 昇格済み（PR #69 / 群 G） |
| 54 | 2026-08-08 | stage4 | PR #60 | 無視パターンが特定の場所（`src-tauri/target/`）を焼き込んでおり、ワークスペース化でその場所が動いたときに黙って死んだ（1 回。CI は `target/` を持たないので、この保護が効いているかを CI からは永久に観測できない） | `AGENT-LESSONS.md` | 昇格済み（PR #78 / 群 β） |
| 55 | 2026-08-09 | stage4 | PR H2 | `task-brief` スクリプト自身がファイルを書くのに lane-controller が `>` でも同じファイルを開き、1 行目が状態メッセージで上書きされた brief ができた（1 回。`grep -c` の 0 が「語が本文に無い」と「ファイルが壊れている」で同じ出力になり、検算しなければ implementer が先頭の欠けた仕様を読んでいた） | `lane-controller.md` | 昇格済み（PR #69 / 群 H） |
| 56 | 2026-08-09 | stage4 | PR H2 | 報告に、実際には生成されていない検証の出力が書かれた（未実行の `git diff` を実行済みと記載。**確定 1 回 + 判別不能 1 件で保留** —— 後者は「復元していないのに `git status --short` → 出力なし」で、読んだ時点で偽だったことは確定しているが、書かれた時点で偽だったかは報告ファイルが git 管理外のため復元できない。書いた時点で真なら機構は #64 である） | 変異を打つ 3 者 | 昇格済み（PR #69 / 群 Q-2）— **例外: 1 回（確定分）。**「実行していない検証の出力を書かない」を 3 ファイルへ書いたので開かない。**`REPORT_WRITTEN_AT` は本件に効かない**（書いた時点で既に偽だった） |
| 57 | 2026-08-09 | stage4 | PR H2 | 変異がハングとして現れる場合に `timeout` の exit 124 を赤として採る手順が定義に無く、implementer が最も価値の高い 2 変異を「赤ではなく測定不能」として記録だけで終えた（1 回。レビュアーが `timeout 120` を挟んだところ 2 本とも exit=124 の赤だった） | 変異を打つ 3 者 | 昇格済み（PR #69 / 群 O）— **例外: 1 回。**「ハングは測定不能ではなく `timeout` の exit 124 が赤である」を 3 ファイルへ書いたので開いたままにできない。**群 C（変異が実際には測られていない）とは機構が違う** —— こちらは「読めない結果を測定不能として捨てた」 |
| 58 | 2026-08-09 | stage4 | PR H2 | 変異検証の結果をどのコミットのツリーで測ったかを報告に書く欄が無く、レビュー修正コミットの前で測った変異が「後に打った」と記載され、母数の 1 の差でしか検出できなかった（1 回。決め手は `git show <sha>:<file> \| grep -c '#[test]'` のコミット間比較だった） | 変異を打つ 3 者 | 昇格済み（PR #69 / 群 Q） |
| 59 | 2026-08-09 | stage4 | PR H2 | 修正ラウンドの差分が、元の指摘が閉じたかとは別に新規コードとして変異検証されず、修正そのものが無防備なまま残った（**4 回**。レビューの焦点が「元の指摘が閉じたか」に寄り、修正が持ち込んだ無防備がその外側に落ちる。**4 回目が最も重い —— 修正対象が安全性論証の全前提（`kill(-pid, …)` の符号）で、符号を落としても 424 全緑だった**） | `task-reviewer.md` | 昇格済み（PR #69 / 群 D） |
| 60 | 2026-08-09 | stage4 | PR H2 | 間欠不具合の調査で入れたプローブ自身が対象のタイミングを変え（`eprintln` が race をずらす／子が既にゾンビで判別力が無い／観測スレッドが callsite の初回登録を先に取る）、仮説が正しくても再現しない形になった（3 回） | `task-reviewer.md` / 変異を打つ 3 者 | 昇格済み（PR #69 / 群 P） |
| 61 | 2026-08-09 | stage4 | PR H2 | 昇格済みの規則（`> 0` の恒真述語を疑う）がレビュアー側の定義にしか無く、implementer が書いた恒真述語は必ず 1 往復してから消える形になっていた（1 回） | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #69 / 群 K） |
| 62 | 2026-08-09 | stage4 | PR H2 | 契約 §0 のポーリング禁止を `set_nonblocking` / `sleep` の grep で判定する運用になっており、`Incoming` のようにエラー時も `None` を返さないイテレータで生じる無待機スピン（CPU 1 コア占有）が検査に掛からなかった（1 回） | `task-reviewer.md` | 昇格済み（PR #78 / 群 I） |
| 63 | 2026-08-09 | stage4 | PR H2 | Rust のテストがサンドボックス有効のままだと環境起因で 5 件落ちることがどこにも記録されておらず、次に回す者が「自分の変更が壊した」と読むか「既知の赤」として学習して本物の赤を見逃す形になっていた（1 回。`pty/launch_env.rs` の `probe_*` 3 件は偽シェルの exec が阻害され、`hooks_srv/mod.rs` の `real_relay_*` 2 件は relay がソケットへ 10 秒以内に接続しない） | `rust-implementer.md` | 昇格済み（PR #69 / 群 K） |
| 64 | 2026-08-09 | stage4 | PR H2 | 完了マーカ（`STATUS:` / `VERDICT:`）を役割を問わず次工程の引き金にしたため、マーカを書いた後も作業を続けている相手と次工程が重なった（4 回。implementer 3 + レビュアー 1。3 回目は同じ worktree での implementer 並列起動になり、4 回目はレビュアーが `APPROVED` の 4 分後に `CHANGES_REQUESTED` へ評決そのものを反転させた） | `lane-controller.md` | 昇格済み（PR #69 / 群 A） |
| 65 | 2026-08-09 | stage4 | PR H2 | 稼働中の implementer のツリーに lane-controller が `git restore` を掛け、進行中のベースライン測定を汚染した（1 回。既知の注意は「動作中の diff に反射的にコミットせよと言わない」という片方向にしか書かれておらず、復元の側が抜けていた） | `lane-controller.md` | 昇格済み（PR #69 / 群 A） |
| 66 | 2026-08-09 | stage4 | PR H2 | 計画のコード片が欠陥を持ったまま逐語で実装され、レビューで初めて出た（2 回。`accept` ループの `Err` 枝＝無待機スピン／`matches!` だけの assert＝失敗の理由を区別しない。どちらも implementer の逸脱ではない） | 計画を書く側（contract-owner / team-lead） | 昇格済み（PR #69 / 群 H） |
| 67 | 2026-08-09 | stage4 | PR H2 | 報告に「いつ書かれたか」が無いため、記述が偽になった時点（書いた時点か、その後の作業でか）を事後に切り分けられなかった（1 回。報告ファイルは git 管理外で版が残らない。手当てとして `REPORT_WRITTEN_AT:` を採用した） | 報告を書く全員 | 昇格済み（PR #69 / 群 Q） |
| 68 | 2026-08-09 | stage4 | PR H2 | 一般則から自分を除外する議論が書けてしまい、その議論そのものが誤っていた（3 回。§98.10 が「§97.9 の理由は本件に転移しない」と明示的に書いていた。規則が無いのではなく規則の適用外だと書いてある形で、最も見つけにくい） | contract-owner / team-lead | 昇格済み（PR #78 / 群 ν） |
| 69 | 2026-08-09 | stage4 | PR #59 | レビュー指摘への修正が 3 項の論理積のうち第 1 項だけを閉じ、同じ式の第 2 項が無観測のまま 6 本のタスク単位レビューを通過した（`mod.rs:186-188`） | `task-reviewer.md` | 昇格済み（PR #78 / 群 γ） |
| 70 | 2026-08-09 | stage4 | PR #59 | `perl -CSD -pi -e` の置換パターンにマルチバイト文字を直接書いた変異が無言で no-op になり、「変異したが全緑」と出力上まったく区別が付かなかった（今回のラウンドは ASCII と行番号指定のみに限定して回避した） | 変異を打つ 3 者 | 昇格済み（PR #69 / 群 C） |
| 71 | 2026-08-09 | stage4 | PR #59 | 変異検証中に無関係な実 PTY フレークが 1 本混入し、全緑の変異が初回に「1 failed」と出て赤と誤読されかけた。判別できたのは件数ではなく失敗テスト名を採取したからである | `task-reviewer.md` | 昇格済み（PR #69 / 群 N） |
| 72 | 2026-08-09 | stage4 | PR #59 | Task 4 で `only` のフィクスチャ不足を修正した直後の PR に、同一ファイル内の別テストで同型の `never` のフィクスチャ不足が残っていた。タスク単位では別タスクの差分に分かれており、どちらのレビュアーの視野にも「両方」は入らなかった | `task-reviewer.md` | 昇格済み（PR #78 / 群 γ） |
| 73 | 2026-08-09 | stage4 | PR #59 | reviewer サブエージェントは Edit ツールを持たないのに「マルチバイトを含む変異は必ず Edit ツールで行う」という手順が渡された。実行不能な指示で、ASCII と行番号指定に限定して回避した | `lane-controller.md` | 昇格済み（PR #69 / 群 R） |
| 74 | 2026-08-09 | stage4 | PR #59 | レビューが処方した手当ての「省略形は検出力が無い」という主張が未実測のまま書かれ（`pr18-review.md:43`）、implementer も測らずに「理解した上で適用した」と追認した（`task-6-report.md:239-241`）。実測すると省略形は無変異でも赤で、主張は偽だった | `task-reviewer.md` / `rust-implementer.md` | 昇格済み（PR #69 / 群 E） |
| 75 | 2026-08-09 | stage4 | PR #59 | レビュー報告が 2 箇所の関係を「相互に冗長」と双方向で書き、その誤った framing が production コメント（`mod.rs:187`）へそのまま転記された。実際は片方向（第 3 項 ⇒ 第 2 項）である | `task-reviewer.md` | 昇格済み（PR #69 / 群 E） |
| 76 | 2026-08-09 | stage4 | PR #59 | `match` にアームを足す変異はワイルドカード `_` より後ろに挿入すると unreachable pattern になって発火しないが、`git diff --stat` は非空のままなので「変異を打ったが全緑」と区別が付かない（無言 no-op の別形。挿入位置を明示して回避した） | 変異を打つ 3 者 | 昇格済み（PR #69 / 群 C） |
| 77 | 2026-08-09 | stage4 | stage4 ゲート | 人間ゲートの手順で実施者が **5 回**止まった。記号（A/B/C/D）が未定義 / 実在するものを間違った場所で探させた（WebKit のヘルパは `ppid = 1`）/ 観測対象の欠落（`Networking`）/ 観測を誰も出力していない（`event.key` の listener が無い）/ **操作対象が実装されていない**（停止ボタン）。team-lead の 4 検査（観測点の実在 / 契約との向き / 不合格の形 / 前提条件の到達可能性）は 5 件とも捕まえなかった —— 4 検査は**項目の主張**を見ており、**実施者が触る具体物**を見ていない | `SKILL.md` | **契約 §105.5 が正典を持った**（名詞 / 操作 / 観測の 3 欄に一次証拠を 1 つずつ）。**`SKILL.md` 側も PR #69 / 群 L で閉じた** —— 検査 4「前提条件の到達可能性」の追加と「規則を当てる工程」の新設。**管轄の分界（§105.5 = 項目を書く者の義務 / `SKILL.md` = 渡す前の検算）を明記した**（契約 §105.5.2）） |
| 78 | 2026-08-09 | stage4 | M2-3 Task 7 | 測定を指示するとき、**測る対象を名指ししなかった**。**同じ穴が 2 回**: (1) 測定 2 で「どのスレッドから測るか」を落とした（メインから測ると `is_waiting` はほぼ常に false になり別の量を測る） (2) 修正ラウンド 1 で「`wait_for_response` を**どのハンドルに対して**行うか」を落とし、項目 3〜6 の観測が全部**捨てる経路（試行 A）にぶら下がった** | `SKILL.md` | 昇格済み（PR #69 / 群 L） |
| 79 | 2026-08-09 | stage4 | M2-3 Task 7 | `open --env` の訂正を stage4 の手順書にだけ入れ、**同じ誤りを持つ Task 7 の手順書へ後方転記しなかった**。5 時間後に実施者へ渡す直前まで、直接起動の手順が残っていた（`kamux-gate-misses-backward-pointers` と同型。索引に載った分しか走らない） | `SKILL.md` | 昇格済み（PR #69 / 群 G） |
| 80 | 2026-08-09 | stage4 | M2-3 Task 7 | **平均値を根拠に設計判断をしかけた。** `is_waiting` の `ratio` が 0.73 の回で試行 A が通り、**0.82 の回で落ちた**。効くのは `show()` が着地した瞬間の値だけで、平均はそれを平滑化して隠す。**1 回しか回していなければ「A で十分」と結論していた** | `lane-controller.md` | 昇格済み（PR #69 / 群 L） |
| 81 | 2026-08-09 | stage4 | PR #64 | **テストのリネームで主張が実体より広がり、契約違反のほうを名前で正当化した。** `shell_command_gets_no_settings_flag`（狭いが正しい。`--settings` は Claude 専用フラグ）→ `non_claude_cli_kinds_get_no_hooks_wiring`（**「hooks 配線が無い」は契約 §30.2 に反する**）。CI は緑のまま、テストが違反側を固定していた（`kamux-test-name-claims-exhaustiveness` の逆向き —— 網羅の主張ではなく**否定の主張**が広がった形） | `task-reviewer.md` | 昇格済み（PR #78 / 群 E） |
| 82 | 2026-08-09 | stage4 | 契約 §103 | **裁定要求に添えた原因の見立てが、測ったら偽だった。** team-lead は §97.2.1 の「発火する」を「合成イベント `{ key: 'j', altKey: true }` のテスト由来」と説明したが、**そのテストは 1 本も存在しない**（contract-owner の実測）。結論（発火しない）は正しかったが理由が違った。**誤った理由は再利用される**（`kamux-measure-the-requesters-reason` の 2 例目） | `SKILL.md` | 昇格済み（PR #69 / 群 F） |
| 83 | 2026-08-09 | stage4 | PR #63 のマージ | **検証 E（squash されたか）が常に空振りしていた。**`git rev-list --count --merges "$MC" -1` は `$MC` 自身を見ず履歴全体を遡るので、PR #1 の真のマージコミット（`0bed3b7`）が全ブランチの祖先にある本リポジトリでは決して `0` を返さない。**既知の正常な squash 2 件（`993dafe` / `3cc3225`）に当てて両方 `1`** —— 合格と不合格が同じ出力になる形（契約 §99.2 / §101.7 の族）。**team-lead は同日 P1 / P3 を足す編集でこの行の真横を触ったが、検証 E を一度も実行しなかった** | `lane-controller.md` | 昇格済み（PR #69 / 群 I） |
| 84 | 2026-08-09 | stage4 | PR #61 のマージ | **検証 E の 1 度目の手当て（`git cat-file -p "$MC" \| grep -c '^parent '`）も誤りで、次のマージで偽の警報を出した。** `cat-file` はコミットオブジェクト全体を吐くので、squash されたメッセージ本文の `parent attach useLayoutEffect sees isStarted=false …`（テスト名）を親として数えた。**squash（罠あり）= 2 / 真のマージ = 2 で、#83 と同じく合格と不合格が同じ値になる。** **手当て時の検算 3 件に、罠を持つコミットが 1 つも入っていなかった** | `lane-controller.md` | 昇格済み（PR #69 / 群 I） |
| 85 | 2026-08-09 | stage4 | PR #64 | **セッション中断（週次上限）により ledger の完了行が 4 本欠落し、契約 §32 条件 2 の根拠が消えていた。** ブランチにはコミットが在るのに、それを「レビュー済みで完了」と主張する記録だけが失われた形である。**観測点: レーン再開時に、ledger の最終完了行とブランチの最終コミットが対応しているかを確かめる** | `lane-controller.md` | 昇格済み（PR #69 / 群 J-2）— **例外: 1 回。**#96 とは機構が違う（あちらは記録が在るが探せない、こちらは記録そのものが失われた）。手当て【規則 2】を書いたので開かない |
| 86 | 2026-08-09 | stage4 | PR #64 | **計画のコード片が実在しない API を書いており、そのまま渡すとコンパイルエラーになる**（本ステージ 4 件目。今回は `now_millis` / `self.conn.lock()` / `crate::store::tests::test_store()` / `create_project` / `create_session` / `TempDir` 落ちの 6 箇所）。**観測点: dispatch 前に、brief のコード片が呼ぶ関数名を実コードに grep で当てる** | `lane-controller.md` | 昇格済み（PR #69 / 群 H） |
| 87 | 2026-08-09 | stage4 | PR #64 | **計画と契約が正面衝突しており、どちらに倒しても悪い形になった**（計画 6 箇所 vs 契約 4 箇所）。**「該当 0 件」の grep（producer 側の実測）が衝突の証拠になり決着したが、その形の証拠を裁定要求に添える手順はどこにも無かった**。**観測点: 「該当 0 件」の grep は衝突の証拠になる。裁定要求に必ず添える** | `lane-controller.md` | 昇格済み（PR #78 / 群 V） |
| 88 | 2026-08-09 | stage4 | PR #64 | **振動する信号（作業ツリーの clean）を 1 回のスナップショットで読んで implementer の完了と断定した**（team-lead）。**実測では谷と山で観測が割れており、どちらのスナップショットも正しかった** —— 完了判定に、単調に増える信号（報告ファイルの `^STATUS:` マーカ）を連言で含めて解決した | `SKILL.md` / `lane-controller.md` | 昇格済み（PR #69 / 群 A） |
| 89 | 2026-08-09 | stage4 | PR #64 | **`wait_state(tx, id, want)` は `want` が `RuntimeState` の既定値（マップ未登録時に `current()` が返す `Idle`）のとき問答無用で恒真になるが、その罠がどこにも書かれておらず、brief のコード片自身がその形だった**（`stop_moves_to_idle`） | `rust-implementer.md` / `task-reviewer.md` | 昇格済み（PR #69 / 群 H） |
| 90 | 2026-08-09 | stage4 | PR #64 | **`cargo build --workspace` は `#[cfg(test)]` を型検査しないので素通りし、「テスト側で型が違うから変異は非該当」の検証は `cargo test --workspace`（または `cargo build --tests`）でしか `E0308` が出ない。** どの手順書にも書かれていなかった | `rust-implementer.md` | 昇格済み（PR #69 / 群 C） |
| 91 | 2026-08-09 | stage4 | PR #64 | **指定した変異 2 つが実装上は同一の編集結果になることに、指定の時点で誰も気づいていなかった**（team-lead 指定 #1「`PermissionRequest` を潰す」と #3「両方を潰す」。`Notification` 腕は元から `HookNotification` を送るため） | `SKILL.md` / `lane-controller.md` | 昇格済み（PR #78 / 群 α） |
| 92 | 2026-08-09 | stage4 | PR #64 | **レビュアーが変異を打ち終えて報告を書いた後も生存しており、次工程の implementer が同じ worktree でレビュアーの変異差分を観測した。** 変異を打つ前の `git status --short` 空確認は、自分以外の原因で非空になりうる | `task-reviewer.md` / `lane-controller.md` | 昇格済み（PR #69 / 群 A） |
| 93 | 2026-08-09 | stage4 | PR #64 | **連言条件は工程の完了を判定するが、エージェントの終了は判定しない。** レビュアーが変異を打っている最中に次の implementer が起動し、`git add -A` していれば契約 §12.4 が明文で禁じた「Stop を waiting_input に流用」相当の変異が成果物に入りえた（今回は入らなかった。60 insertions / 0 deletions で検算済み） | `lane-controller.md` / 変異を打つ 3 者 | 昇格済み（PR #69 / 群 A） |
| 94 | 2026-08-09 | stage4 | PR #64 | **レビューが指定した 4 変異はすべて「既に心配されている箇所」しか見ておらず、「陽性の対照が本当に生きているか」を測る変異（フィルタを全落ちにする）は誰も指定していなかった。** 再レビュアーの自発設計で初めて打たれ、それが Important 1 の判定根拠そのものになった | `task-reviewer.md` / `lane-controller.md` | 昇格済み（PR #69 / 群 K） |
| 95 | 2026-08-09 | stage4 | PR #64 | **「固定 `sleep` で非決定性を潰した」修正について、その値が十分かを測る手順（猶予を下げてしきい値を探るスイープ）がどの手順書にも無く、値の妥当性が誰にも測られないまま承認されうる形だった** | `rust-implementer.md` / `task-reviewer.md` | 昇格済み（PR #69 / 群 E-2）— **例外: 1 回。しかも「承認されうる形だった」で実現した事象ではない**（台帳の書き方に反する記述）。手当て（猶予を下げるスイープ）は書いたので開かない |
| 96 | 2026-08-09 | stage4 | PR #64 | **ledger の完了行が 3 つの形式に割れており、`grep '^Task .*: complete'` が 0 件・`grep 'Task .*: complete'` でも Task 7 が漏れた。** 契約 §32 条件 2 は ledger を根拠にするので、記録が在っても探せなければ在らないのと同じになる | `lane-controller.md` | 昇格済み（PR #69 / 群 J-1）— **例外: 1 回。**#85 とは機構が違う。手当て【規則 1】を書いたので開かない |
| 97 | 2026-08-09 | stage4 | PR #64 | **Task 11 は §73.5 の worklist に載った §102 を全項目照合して PASS したが、同じ Task 11 を縛る §30.2 の env 表は worklist に無かったため、brief のコードスケッチと契約の表の食い違いを実装者もタスクレビューも誰も踏まなかった**（同型が §102.1 の 5 として同じ M2-2 の Task 10 で既に記録されている） | `lane-controller.md` / `task-reviewer.md` | 昇格済み（PR #69 / 群 G） |
| 98 | 2026-08-09 | stage4 | PR #64 | **brief が網羅担保の例として「`match` の網羅ガード」を示したが、`match` の網羅エラーは配列リテラルの更新を強制しないため、フィクスチャを 1 種落としても 530 全緑のまま `every` を名乗るテストが残った**（`apply_hooks_for_every_cli_kind`。変異で実測） | `lane-controller.md` | 昇格済み（PR #69 / 群 H） |
| 99 | 2026-08-09 | stage4 | PR #64 のマージ | **検証 E の 3 版目（`[ "$(git show -s --format=%P "$MC" \| wc -w)" = "1" ]`）も壊れていた。** BSD の `wc` は数値を右詰めするので出力は `"       1"` で、文字列比較が必ず偽になる。**`#83`/`#84` の「合格と不合格が同じ値」と違い「合格が必ず不合格を返す」形。** **team-lead は自分の実行に `\| tr -d ' '` を足しており、手順書に書いた形を一度も実行していなかった。同じ検査で 3 回連続** | `lane-controller.md` | 昇格済み（PR #69 / 群 I） |

| 100 | 2026-08-08 | stage4 | team-lead | **申し送りファイル（`TEAM-LEAD-RULINGS.md`）に書かれた実測が古くなったまま次のステージの dispatch で渡され、レーンがそれを前提に裁定要求を組み立てた**（M3-2 §6.3(b)「`assignPane` は `focusedSessionId` を更新しない」は `c2dddf4` では偽。実物は `{ paneAssignment, activePane: pane, focusedSessionId: sessionId }` を返す）。申し送りは書かれた時点の真を保存するが、**鮮度を保証する規律も、渡す前に取り直す手順も無い** | `SKILL.md` | 昇格済み（PR #69 / 群 F） |
| 101 | 2026-08-08 | stage4 | team-lead | **`TEAM-LEAD-RULINGS.md` に「implementer へ逐語で渡せ」と書いた指示が、レーンに読まれた上で dispatch プロンプトへ転記されず落ちた**（M2-3 Task 1。RULINGS §7 は「定義ファイルに記述が無いので本ファイルが代わりに持つ」と明記していたが、**RULINGS は「レーンが読むもの」としてのみ定義されており、「dispatch へ転記するもの」という指定が無い**）。§73.5 の索引が「読む」を誰かが実行しなければ落ちると警告したのと同じ形が、**1 段下（レーン → implementer）で起きた** | `lane-controller.md` / `SKILL.md` | 昇格済み（PR #69 / 群 G） |
| 102 | 2026-08-08 | stage4 | team-lead | **観測の不在から結論を引き、観測経路が届いているかを確かめなかった（2 回）。** (1) #38 の判定材料として「rust-implementer が変異を打たなかった」を数えたが、レーンが §81.2 の 2 条件を dispatch へ入れ忘れており検証になっていなかった (2) M2-3 Task 3 について「完了報告に変異検証の記載が無い」と判定したが、レポート `:83` に `## 変異検証（契約 §81.2）` の節が実在し、要約の書式に含まれていないだけだった。**不在は「対象が失敗した」証拠にも「観測経路が届いていない」証拠にもなる。2 回とも後者** | `SKILL.md` | 昇格済み（PR #69 / 群 F） |
| 103 | 2026-08-08 | stage4 | team-lead | **すぐ確かめられる証拠を見ずに結論を断定し、レーンへ誤った指示を出した（2 回）。** (1) 「`task-8-brief.md` の describe は `setActivePaneReducer` を射程外」と書いたが `:154` の describe が 6 箇所すべてを覆っていた（ファイルを開かなかった） (2) brief のテスト本数のずれ 3 件から「brief の数え方に系統的なずれ」と断定したが Task 1・2 は完全一致で、原因はレーンの決定による計画の陳腐化だった（**系列の頭を見ずに末尾 3 点から一般化した**）。どちらも M3-2 レーンが実測して訂正した | `SKILL.md` | 昇格済み（PR #69 / 群 F） |
| 104 | 2026-08-08 | stage4 | user | **変異検証の手順そのものが作業を消す形で書かれていた。** `web-implementer.md` の「変異は必ず戻す。`git checkout -- <file>` のあと `git status --short` が空であることを確認してから報告する」を未コミット状態で実行すると、**変異だけでなくそのタスクの実装とテストが丸ごと `HEAD` へ巻き戻る**（M2-3 Task 2 の implementer が実際に踏み、Task 2 の作業が全部消えた。再実装で復旧）。**しかも `git status --short` 空の確認はこの事故を検出しない —— 作業ごと消えても「空」になる。** team-lead は 3 レーンの RULINGS を修正したが、**`.claude/agents/*.md` の側は 2026-08-10 まで未修正のまま残っていた** | `web-implementer.md` / `task-reviewer.md` / `SKILL.md` | 昇格済み（PR #69 / 群 B） |
| 105 | 2026-08-08 | stage4 | user | **team-lead が implementer の完了通知を中継したうえで「次のタスクへ進んでよい」と書き、レビューゲートを免除したように読める指示を 2 レーンへ出した**（M2-3 Task 4 / M3-2 Task 5）。**実装完了はタスク完了ではなくレビューが別工程として残るのに、レビュー通過後と同じ言葉を使っていた。** M2-3 レーンが解釈を確認してきて発覚。**止めなければ 2 レーンでレビューが飛んでいた** | `SKILL.md` | 昇格済み（PR #69 / 群 A） |
| 106 | 2026-08-10 | stage4 | stage4 ゲート | **未昇格の行を数える集計コマンド（`awk -F'\|'`）が、セルに `\|` を含む 3 行（#58 / #84 / #99）を静かに落とし、72 と出した。実測は 75。** Markdown のセル内でパイプは `\|` とエスケープされるが `-F'\|'` はそこでも分割するため、列がずれて `昇格` 列に別のテキストが入る。**落ちた 3 行のうち 2 行（#84 / #99）は、このゲートが昇格させる群 I（検証 E）の構成要素そのものである。** 機構は #83 / #84 / #99 と同一 —— **解析コマンドを、その解析が曖昧になる入力に当てて検算していない** | `SKILL.md` | 昇格済み（PR #69 / 群 I） |

| 107 | 2026-08-10 | stage4 | 契約 §109 | **契約 §0 の防護節の検査 2（`grep -ohE '#\[tauri::command\][^\n]*fn [a-z_]+'`）が、構造上どの対象に対しても 0 を返し、一度も動いていなかった。** `grep` は 1 行ずつ処理するので `[^\n]*` は `.*` と同じであり、属性と `fn` が**同一行にあること**を要求する。**Rust では属性は前の行に置くので、正しく書かれたコードに対して必ず 0 を返す。** 判定の型が「契約と一致」なので、**0 は「契約に無いコマンドは 0 個」と読めて合格に見えた**（補正版で計画 24 個 / 実装 15 個。全 39 個が §7 / §7.1 / §29.3 に行を持つ）。**群 I の 5 例目。今回は検証 E ではなく契約側の検査で出た** | `SKILL.md` | 契約 §109 として採用（contract-owner が裁定。**PR #69 で群 I の 5 例目として計上**） |
| 108 | 2026-08-10 | stage4 | 契約 §108 | **team-lead が契約変更要求に添えた「実測の 1 文」が偽だった。** 「PR #64 の 2 つのマージコミットは**両方の親**が `origin/main` のスナップショットであることを実測した」と書いたが、**祖先である親は第 2 親の 1 つだけである**（`b37e0264`: 第 1 親 `46ef42b2` = NO / 第 2 親 `993dafed` = YES。`14c620f2` も同型）。**第 1 親が祖先ならブランチに main に無い成果物が 1 つも無く、PR として成立しない。** 結論も要求 2 の言い換え（「自分のブランチ側でないほうが祖先」）も正しく、**偽なのは「実測して」を伴うこの 1 文だけ。同じメッセージに正しい言い換えと偽の記述が並んでおり、どちらが定義ファイルへ写されるかは偶然で決まる**（台帳 #82 と同じ機構の 2 回目） | `SKILL.md` | 昇格済み（PR #69 / 群 F） |
| 109 | 2026-08-10 | stage4 | stage4 ゲート | **contract-owner が「M2-3 Task 7 の人間ゲートは実行待ちである」を理由に契約 §107.3 の適用を依頼したが、その理由は偽だった**（人間ゲート A は 6/6 完了。`progress.md:1799` / `:1852`）。**結論（次のゲート実施の前に適用する）は正しく、実際に適用した。** 理由が偽になった経路は「手順書の**本文**に古い指示が残っている」を「**状態**が未実施」と読んだこと。**受け取った側が測らなければ、偽の理由が「実行待ちの人間ゲートが 1 件ある」として次ステージの起動プロンプトへ写っていた**（群 F の、受け取る側での現れ） | `SKILL.md` | 昇格済み（PR #69 / 群 F） |
| 110 | 2026-08-10 | stage5 | M2-2 PR 17 | **team-lead が人間ゲートの手順に置いた 1 行の長い `printf` が、貼り付け時に折り返されて生成物を壊した。** 実施者が `claude --settings` で `Invalid or malformed JSON` に当たった。実測（`cat -v -e`）では JSON 文字列の中に**生の改行 + 2 スペース**が入っており、`jq` は `control characters from U+0000 through U+001F must be escaped` を返した。**4 検査は 4 つとも合格していた** —— 観測点も契約の向きも不合格の形も前提の到達可能性も正しく、**壊れたのは手順の「転送」だけである**。かつ**この `printf` は `build_hook_settings()`（`session/cli_args.rs:149`）の出力を人間が書き写した第 2 の正典でもあった**（`HOOK_EVENTS` / `HOOK_TIMEOUT_SECS` / `shell_single_quote` の 3 つを手で複製していた） | `SKILL.md` | 昇格済み（PR #78 / 群 λ） |
| 111 | 2026-08-10 | stage5 | M2-2 PR 17 | **契約の「影響を受ける計画・ファイル」表の状態列が、書かれた直後に古くなる。同じ機構を 1 日で 4 セル観測した** —— §84.8 の最終行（Task 16 の `Files:` / Step 5 = 「未適用。team-lead へ回す」）は**実際は適用済み**、§105.6 の `M2-2-hooks-relay.md:453-458`（= 「未適用。team-lead が brief に反映」）も**適用済みで行番号が `:469-474` にずれていただけ**、§110.4 の 2 行（計画 Task 16 Step 4 / `hooks_srv/mod.rs` の doc）は**書かれた当日に両方とも適用された**。**表は「書いた時点の真」を保存する設計で、鮮度を持たない。** 害は「次の dispatch が未適用と読んで再依頼する」ことであり、実際に PR 17 の起動時に team-lead が 2 件を再オープンしかけた（RULINGS §12.4 で止めた）。**作り方ではなく契約の記帳の話なので、定義ファイルではなく契約側の論点である** | `SKILL.md` | 契約 §128.5 として採用（形式は §111.3 が既に持っていた。足したのは強制 = 検査 15 の ③。対象は §128 以降のセル） |
| 112 | 2026-08-11 | stage5 | PR #70 | **レビュアーの変異検証が、稼働中の実アプリが絶対パスで参照している `target/debug/kamux-relay` を上書きした。**アプリ（pid 49610）の `settings.json` は hook コマンドをその絶対パスで名指ししており、01:00〜01:02 に載っていた 1 版は `MAX_STDIN_BYTES = 999`（全 payload を `truncated:true` / `payload:null` にする）だった。**変異前の事前確認は `git status --porcelain` だけで、この状態を構造的に検出しない**（他プロセスが参照する成果物は作業ツリーの clean 判定に現れない）。evidence の mtime を測って汚染なしと判定できたが、判定できたのは偶然である | `task-reviewer.md` | 昇格済み（PR #78 / 群 θ） |
| 113 | 2026-08-11 | stage5 | PR #70 | **`.gitignore` 配下の生成物（`src-tauri/bin/kamux-relay-*` / `target/release/kamux-relay`）は `git status --porcelain` の空確認で覆えず、変異の復元を保証できない。**レビュアーは sha を手で控えて個別照合する必要があった。**「変異を戻した」の唯一の観測点が porcelain の空である運用は、gitignore 配下を触る変異に対して空振りする** | `task-reviewer.md` | 昇格済み（PR #78 / 群 β） |
| 114 | 2026-08-11 | stage5 | PR #70 | **後片付けの `rm -rf $TMPDIR/kamux-boot-from-* $TMPDIR/kamux-e2e-*.sock` が zsh の no-match で**コマンド全体**を中断し、先行するパターンの削除が 1 つも実行されなかった。**終了ステータスからは「消した」と「1 つも消さずに中断した」を区別できない | `task-reviewer.md` | 昇格済み（PR #78 / 群 β） |
| 115 | 2026-08-11 | stage5 | PR #70 | **`.gitignore` 配下のビルド成果物が、ローカルと CI を構造的に別物にした。**Task 15 の `externalBin: ["bin/kamux-relay"]` により `src-tauri` 配下のあらゆる cargo 呼び出しが sidecar の実在を要求するようになったが、`/src-tauri/bin/` は gitignore 配下で、CI は `build-relay.sh` を一度も走らせない。**ローカルは `cargo clippy` / `cargo test` とも全緑**（worktree に Task 15/16 が作ったバイナリが残っていたため）、**CI の `rust` ジョブだけが `resource path ... doesn't exist` で落ちた**。PR 単位レビューも変異検証つきで APPROVED を出しており、**ローカルで走る検査は原理的にこの差を見られない** | `lane-controller.md` | 昇格済み（PR #78 / 群 β） |
| 116 | 2026-08-11 | stage5 | PR #70 | **dispatch が配った制約の「理由」が未検証のまま production のコメントへ逐語転記された。**lane-controller は CI 修正の dispatch に「debug で揃えること。release だと `build-relay.sh` の `cmp` が `target/debug/kamux-relay` 不在で落ちる」と書いたが、**再レビュアーの実測ではこの理由は本 CI 構成（同一ジョブで `build-relay.sh` が 1 回しか呼ばれない）では成立しなかった**。実装者はその理由を `ci.yml` のコメントへそのまま写した。**結論（debug を使う）は妥当なので実害は無いが、誤った理由は次の誰かに再利用される** | `lane-controller.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 117 | 2026-08-11 | stage5 | PR #70 | **陳腐化した数値を新しい章の値に差し替えたが、その章が同時に定めている**測り方**を持ってこなかった。**§9 スモーク項目 12 のアイドル CPU で、レーンは閾値を「0.0〜0.3%」から契約 §104.3（60 秒平均 < 1.0% かつ最大サンプル < 5.0%）へ差し替えたが、**測り方は元の `top -pid $(pgrep -f 'kamux$')`（単一プロセス）のまま残した**。**§104.2 の理由 1 は逐語で「単一プロセスにすると偽の合格になる。Tauri はマルチプロセスであり、本体だけを見ると WebContent 分が丸ごと抜ける」と禁じており、§104.3 の閾値は 4 プロセスの合計に対して定められている。** つまり**閾値より小さい母数を新しい閾値と比べる形**で、**合格が偽になる**。team-lead が人間ゲートへ渡す前の 4 検査（検査 2）で捕まえ、手順ごと差し替えた。**閾値と母数は同じ章が定めるので、片方だけ持ってくると必ずずれる** | `lane-controller.md` | 昇格済み（PR #78 / 群 γ） |
| 118 | 2026-08-11 | stage5 | PR #70 | **`gh pr merge --delete-branch` を worktree から実行すると、リモートのマージは成功しているのにローカル後始末だけが失敗し、出力は「マージが失敗した」と読める。** `gh` はマージ後にローカルで `main` へ切り替えてブランチを削除しようとするが、`main` は primary worktree がチェックアウトしているので `fatal: 'main' is already used by worktree at ...` で落ちる。**実測（team-lead が独立に確認、2026-08-11）: `state=MERGED` / `mergedAt` あり / `mergeCommit=c7d69a45…` / `git ls-remote --heads origin <br>` が 0 件（リモートも削除済み）で、残ったのはローカルブランチだけ**（`git branch --list` が `+` 付きで表示 = 別 worktree がチェックアウト中）。**1 レーン = 1 worktree の運用では毎回起きる。** **危険な形はエラー文だけを見ての再実行で、2 回目は「既にマージ済み」の別のエラーになる。** 正しい手順は「`gh pr merge` がエラーを返したら、まず `gh pr view <n> --json state,mergeCommit` で実際の状態を測る」 | `lane-controller.md` | 昇格済み（PR #78 / 群 β） |
| 119 | 2026-08-11 | stage5 | PR #71 | **実測にもとづく最適化の PR について、観測点の恒久的な保存先がどの定義ファイルにも書かれておらず、観測点 3 つが PR 本文にしか残らなかった。** `SKILL.md` §6 は昇格 PR に観測点を要求し、台帳の棚卸し表が `観測点` 列を持つが、**失敗事象の昇格ではない PR はどちらの経路にも乗らない。** PR 本文はマージ後に探しにくく、次に測る者が測り方を復元できない | `lane-controller.md` | 昇格済み（PR #71） |
| 120 | 2026-08-11 | stage5 | PR #71 | **`name` の正典が 2 箇所に割れた。** `SKILL.md:127` が唯一の正典だったところへ、PR ごとに分けるレーン名（`lane-<phase>-pr<n>` + 連番）を `:127` を直さずに `:189` の表セルにだけ書いた。**表セルは正典を持てない** —— 次に読む者は `:127` を見て古い形を使う | `SKILL.md` | 昇格済み（PR #71） |
| 121 | 2026-08-11 | stage5 | PR #71 | **実測で観測されたレーンの切断理由が、どの節にも切断局面として登録されていなかった。** M2-2 PR 17 でレーンが 4 本に分かれた原因は**人間ゲート待ち 2 回・セッション上限 1 回**で、規則が想定した「タスク数で切る」は **0 回**。登録が無いと、実際に起きる切れ方に手順が用意されない。**しかもその 3 つはいずれもタスクの途中であり、「切ってよいのはタスク境界だけ」という当初の判定条件と衝突していた** | `lane-controller.md` | 昇格済み（PR #71） |
| 122 | 2026-08-11 | stage5 | PR #71 | **定義ファイルを変える PR の本文に、差分規模を最終コミットに対して取り直す義務が無い。** 修正コミット 1 本の `git diff --stat`（`+46 / -15`）を PR 全体の差分として本文に書いたが、実測は `+72 / -0` であり、**squash マージ後に残るのは後者。存在しない 15 行の削除を恒久記録に書いていた。** `lane-controller.md`「報告と PR 本文に書く数値は最終コミットに対して取り直す」はテスト件数と grep 出力を対象にしており、差分規模は射程外だった | `lane-controller.md` | 昇格済み（PR #78 / 群 Q） |
| 123 | 2026-08-11 | stage5 | PR #71 | **`no commit` の完了行に対する【規則 2】の代替手順に、evidence の所在の入手方法が書かれていなかった。** 「evidence ファイルの存在で代える」とだけ書いたが、**どこを見れば所在が分かるかが無い**（実測では ledger の当該完了行が唯一の手がかり）。しかも `docs/superpowers` と `.superpowers` は `.git/info/exclude` により worktree に存在しないので、相対パスでは届かない | `lane-controller.md` | 昇格済み（PR #71） |
| 124 | 2026-08-11 | stage5 | PR #71 | **節の見出しを改名したとき、他の定義ファイルからの引用を追随させる検査が無い。** 本 PR は `lane-controller.md` の節を 2 度改名し、**同じ PR が足した `SKILL.md` の行が旧名で引用したまま残った**（レビューで検出）。**次のステージ完了ゲートで扱う。** 検査の形: `` `<file>.md`「…」 `` を抽出し、`…` で分割して順序一致・記号と空白を正規化して**本文全体**と照合する（見出し限定にすると表セルや太字本文への正当な引用を誤検出する）。**判定に使うのは「その差分が追加・変更した行に由来する、解決しない引用が 0 件であること」** —— 既存件数のベースライン（5〜6 件。**正規化の粒度で 1 ずれる**。`-` や `.md` を落とすかどうかだけで反転する行がある）を定数で焼き込むと、正規化を 1 文字変えるたび規則が偽になる | `SKILL.md` §6 | 昇格済み（PR #75 / 群 U） |
| 125 | 2026-08-11 | stage5 | PR #71 | **1 つの規則の記述が複数の定義ファイルに分かれるとき、どれが正典かを PR 内で突き合わせる工程が無い。** 本 PR は観測点を 3 箇所（`lane-controller.md` / 台帳の棚卸し表 / PR 本文）に書き、**`SKILL.md` §6 が正典と定めた棚卸し表だけが 1 行欠けていた**（レビューで検出）。**次のステージ完了ゲートで扱う** | `SKILL.md` §6 | 昇格済み（PR #75 / 群 U） |
| 126 | 2026-08-11 | stage5 | PR #71 | **`SKILL.md` の 🔴 行と昇格の判定行が、1 回かつ手当て済みの台帳行の書式（`— 例外: 1 回。` の要否）について別の形を指定しており、優先順位が無い。** どちらに従っても他方に違反する。**次のステージ完了ゲートで扱う** | `SKILL.md` | 昇格済み（PR #78 / 群 V） |
| 127 | 2026-08-11 | stage5 | PR #72 | **このリポジトリは `diff.external=difft` が設定されており、変異検証中の素の `git diff <file>` がパッチ形式を返さない。** レビュアーが「置換が未適用」と誤読しかけた。`task-reviewer.md` の変異検証手順は `git status --short` は明記するが、**差分内容の確認手段を書いていない** | `task-reviewer.md` | 昇格済み（PR #75 / 群 T） |
| 128 | 2026-08-11 | stage5 | PR #72 | **lane-controller が dispatch に逐語で書いた回避策 `git --no-ext-diff diff` はオプション位置が誤りで `unknown option` になり、レビュアーが実行できなかった**（正しくは `git diff --no-ext-diff`）。代替の `git -c diff.external= diff` も `--stat` 無しでは `cannot run :` で落ちた。**#127 の手当てとして渡したコマンドが、それ自体は一度も実行されていなかった** | `lane-controller.md` | 昇格済み（PR #75 / 群 U） |
| 129 | 2026-08-11 | stage5 | PR #72 | **lane-controller が指定した変異（テスト配列から `CliKind::Shell` を落とす）は、アサーションを減らす編集であり原理的に赤になり得ない。** 緑は盲点の証拠にならないのに、指定側も受け手も打つまで気づかなかった（レビュアーが advisor 相談で発見）。**正しい形は 2 段** —— 配列から落とすと同時に production 側を `Shell` だけ誤答する形へ変える（`cli_kind != CliKind::Claude` を `!matches!(cli_kind, CliKind::Codex \| CliKind::Custom)` に）。**片方だけでは原理的に赤にならない。** 両ファイルに「変異先が production かテストコードか」を打つ前に判定する記述が無い | `task-reviewer.md` / `lane-controller.md` | 昇格済み（PR #78 / 群 α） |
| 130 | 2026-08-11 | stage5 | PR #72 | **lane-controller が指定した変異（`gate.rs` 規則 4 の `matches!` から `RuntimeState::Error` を落とす）は、同じ関数の内側の網羅 `match`（`_` ワイルドカード無しで `Error => None` を独立に持つ）に隠されて必ず緑になる。** advisor が dispatch 前に指摘して 2 箇所同時編集の 1 本へ直し、レビュアーが自発設計で単独編集を実際に打って全緑を再現した（穴の実在を 2 系統で確認）。**#129 とは別の形である —— こちらは production 編集なのに冗長ロジックに masking される。** このコードベースは網羅 `match` を多用するので構造的に再発する。両ファイルに「その変異が同じ関数の他の分岐に隠されないか」を打つ前に確かめる記述が無い | `lane-controller.md` / `task-reviewer.md` | 昇格済み（PR #78 / 群 α） |
| 131 | 2026-08-11 | stage5 | PR #72 | **`scripts/review-package` が生成した差分ファイルが difft の描画済み表になり、`grep -c '^diff --git'` が 0 件だった**（スクリプト内の `git diff` が `diff.external` を継承する）。レビュアーは HEAD の実ファイルを読んで代替したが、**全ファイルが新規追加だったから成立した代替であり、変更行のある差分では成立しない。** lane-controller が試した回避策（`GIT_CONFIG_VALUE_0=` 空文字。`git -c diff.external= diff` と同値）も `cannot run :` で落ち、`git diff --no-ext-diff` で手組みした。`lane-controller.md` は `review-package` を使えと指示するだけで、**`diff.external` が設定されたリポジトリで成果物が空振りすることを書いていない** | `lane-controller.md` | 昇格済み（PR #75 / 群 T） |
| 132 | 2026-08-12 | stage5 | PR #73 | 逐語で範囲を絞った訂正指示（「1 語だけ」「それ以外は変えない」）が、同じファイル内の同一の誤記を実装者に残させた（`mod.rs:18` は直り `mod.rs:39` は残った）。修正ラウンドを 1 往復増やした | `lane-controller.md` / `task-reviewer.md` | 昇格済み（PR #78 / 群 γ） |
| 133 | 2026-08-12 | stage5 | PR #73 | 「この harness では観測点を作れない」と書く前に、観測点を新設した試作で反証する手順が `rust-implementer.md` に無い。実装者が `loom` 必須と結論した箇所を、レビュアーが 2 行の `#[cfg(test)]` seam で観測できた。既載の「不可能と書かない」規則は**「既存の注入点で届かない」と「注入点を作れない」を区別していない** | `rust-implementer.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 134 | 2026-08-12 | stage5 | PR #73 | **3 つのタスク単位レビューが `定義の穴` 欄を「タスク単位レビューでは判定対象外」として一律「なし」にした。** 実際に転記すべき穴 2 件はそのレビュー報告の本文から採れており「判定対象外」は成立していなかった（**レビュアーの留保が母数を減らす形の 4 例目**） | `task-reviewer.md` | 昇格済み（PR #78 / 群 η） |
| 135 | 2026-08-12 | stage5 | PR #73 | `task-reviewer.md` は「`定義の穴` 欄はタスク単位レビューでは出さない」と定めているのに、dispatch が逐語で反対を指示し、**レビュアーが定義ファイルと dispatch のどちらに従うか実行時に自力解釈した**（3 レビューで再現）。優先順位の解決がどちらの定義からも導けない | `task-reviewer.md` / `lane-controller.md` | 昇格済み（PR #75 / 群 V） |
| 136 | 2026-08-12 | stage5 | PR #73 | `rust-implementer.md:108` の「無言の no-op は ASCII と行番号指定に限れば回避できる」に従って実装者が行番号 `perl` を使い、**行番号のずれで変異が適用されず空振りした。4 ラウンド連続で定義側は未修正** | `rust-implementer.md` | 昇格済み（PR #75 / 群 C。**書き直し**） |
| 137 | 2026-08-12 | stage5 | PR #73 | `task-reviewer.md` の変異手順ブロックに「置換が適用されたことの確認」の行が無く `rust-implementer.md` にだけある。**実装者の変異が `cargo fmt` の再フォーマットにより無言の no-op になり「打った → 全緑」と誤読しかけた** | `task-reviewer.md` | 昇格済み（PR #75 / 群 C。**書き直し**） |
| 138 | 2026-08-12 | stage5 | PR #73 | **処方と変異の 2 箇所を同時に当てる測定で、片方の置換が無言で不適用になっても `git diff` は（もう片方のせいで）非空になり「適用の確認」を通過した。** 既存の「`git diff` で当たったことを確認する」は 2 箇所同時のとき判別力を持たない | `rust-implementer.md` / `task-reviewer.md` | 昇格済み（PR #75 / 群 C。**書き直し**） |
| 139 | 2026-08-12 | stage5 | PR #73 | dispatch が指定した「陽性の対照」手順は恒真化と対象変異の**同時適用**を要求するが、`task-reviewer.md` の変異手順は「変異は 1 回に 1 箇所」を例外なく定めており、**2 箇所同時適用の扱いが定義ファイルに無い**（2 回発生） | `task-reviewer.md` / `lane-controller.md` | 昇格済み（PR #75 / 群 V） |
| 140 | 2026-08-12 | stage5 | PR #73 | **4 値の軸に対して「拡大」を 1 本だけ打ち、それで拡大方向は守られていると結論した結果、真の超集合 6 通りのうち 1 通りが無検査のまま残った**（「N 値の軸では拡大は単一の変異ではない。超集合を列挙して全部打つ」の 1 文が無い） | `task-reviewer.md` | 昇格済み（PR #78 / 群 γ） |
| 141 | 2026-08-12 | stage5 | PR #73 | 修正が既存テスト末尾ブロックを複製したため、それまで一意だったアンカー文字列が 2 箇所・6 箇所に増え、**1 行アンカーの変異ハーネスが無言で別のテストへ当たりうる状態になった。変異の前に「アンカーがまだ一意か」を測る手順が無い** | `rust-implementer.md` / `task-reviewer.md` | 昇格済み（PR #75 / 群 C。**書き直し**） |
| 142 | 2026-08-12 | stage5 | PR #73 | **レビュアーが Minor に添えた対照（「同一ファイルの他 2 箇所は正しい様式」）を、指摘対象と同じ方法で確かめずに書き、実装者が実際に失敗させたところ 4 箇所とも同じ様式で対照が偽だった**（欠陥として名指した性質と、正しいと名指した性質が同一だった） | `task-reviewer.md` | 昇格済み（PR #75 / 群 U） |
| 143 | 2026-08-12 | stage5 | PR #73 | レビュアーが 1 点の実測から「境界は `>=` である」と一般化した処方を渡し、実装者がそれを逐語で production の doc へ書いた結果、**実測で偽の断定が残った**。既載の「処方を当てて赤を確認する」は**「処方に一般則を書くなら反対側の 1 点も測る」を含んでいない** | `task-reviewer.md` | 昇格済み（PR #78 / 群 γ） |
| 144 | 2026-08-12 | stage5 | PR #73 | **レビュアーが処方の地の文に書いた未実測の絶対主張（「〜を通す唯一の経路」）が、実装者によって production 近傍のコメントへ逐語転記され、修正ラウンドで新しい未実測主張として残った**（元の指摘の主旨がまさに「測っていない主張をコメントに書いた」だった） | `task-reviewer.md` / `rust-implementer.md` | 昇格済み（PR #75 / 群 U） |
| 145 | 2026-08-12 | stage5 | PR #73 | 「修正が新しく書いた doc の帰属 claim が実測とずれている」が **4 ラウンド連続**で出たのは、**レビュー側が新しい散文の claim を毎回 1〜2 件ずつしか検算していなかったため**でもある。全 claim を 1 パスで列挙したら既存 2 件 + レビュアー自身が作った 1 件が同時に出た | `task-reviewer.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 146 | 2026-08-12 | stage5 | PR #73 | 既存テストのフィクスチャを差し替えた実装者が「元の形より強い観測」と主張して production 近傍のコメントにも書いたが、**旧フィクスチャだけが捕まえていた変異は新フィクスチャでは全緑だった。テスト本数は増えているため「本数が減った差分を疑う」既存規則に掛からない** | `rust-implementer.md` / `task-reviewer.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 147 | 2026-08-12 | stage5 | PR #73 | **契約の裁定（§117 / §118）がタスクの実装より後に着地した**（同日 18:26 実装 / 19:15 契約入り）ため、Critical が **PR 単位レビューでしか捕まらなかった。** レーンは裁定を `SendMessage` で待っており、**裁定が契約ファイルへ直接入る経路を再確認する手順が `lane-controller.md` に無い** —— 「契約変更要求を上げたら裁定を待つ」としか書かれていない | `lane-controller.md` | 昇格済み（PR #78 / 群 ζ） |
| 148 | 2026-08-13 | stage5 | PR #74 | brief のコード片が実在しない型 `SessionManager` を前提に書かれ、着手前に lane-controller が別ファイルで 6 件の読み替えを打ち消す必要が生じた（Task 13） | `lane-controller.md` | 昇格済み（PR #78 / 群 H） |
| 149 | 2026-08-13 | stage5 | PR #74 | 実装者が advisor 指摘の契約章番号（§53.9.1）を本文照合せずに信用し、production 2 箇所・test 2 箇所・CSS 1 箇所の計 5 箇所へ同じ誤引用をそのまま転記した（Task 16 rev1） | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #78 / 群 κ） |
| 150 | 2026-08-13 | stage5 | PR #74 | 「grep で旧い文字列が 0 件になったこと」を修正完了の確認手段にした結果、5 箇所中 2 箇所は新しい参照先が実際にその内容を持つかを本文照合しないまま別の誤った参照へ置き換わった（Task 16 rev2） | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #78 / 群 κ） |
| 151 | 2026-08-13 | stage5 | PR #74 | 変異検証の手順が単一行の `perl -pi -e` 置換を前提しており、複数行挿入が要る変異のためにレビュアーが独自の置換ヘルパを毎回書く必要があった（Task 18 rev1・rev2） | `task-reviewer.md` | 昇格済み（PR #75 / 群 C。**書き直し**） |
| 152 | 2026-08-13 | stage5 | PR #74 | review package が差分を 2 カラム描画で保存しているため hunk の旧側が途中で切れて読めず、足場が動いたかの判定に `git show <sha>:<path>` で pre-image を別途取る必要があった（Task 18 rev2） | `task-reviewer.md` | 昇格済み（PR #75 / 群 T） |
| 153 | 2026-08-13 | stage5 | PR #74 | 修正で入ったコメントが因果を逐語で主張していたが、確かめるには production とテストの 2 箇所を同時に変異させる必要があり、「変異は 1 回に 1 箇所」の例外としての扱いが手順に定義されていなかった（Task 18 rev2） | `task-reviewer.md` | 昇格済み（PR #75 / 群 V） |
| 154 | 2026-08-13 | stage5 | PR #74 | lane-controller が dispatch で逐語指定した 2 assert が、それが捕まえるはずの変異に一度も当てられないまま実装者へ渡された（Task 19 rev1） | `lane-controller.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 155 | 2026-08-13 | stage5 | PR #74 | レビュアーが渡した処方が「指摘した 3 変異を赤にすること」だけで検証され、処方が閉じる族の境界が誰にも書かれないまま実装者が逐語採用した（Task 19 rev2） | `task-reviewer.md` | 昇格済み（PR #78 / 群 γ） |
| 156 | 2026-08-13 | stage5 | PR #74 | レビュアーの処方が区間の始点だけを錨づける形で書かれ、終点の未固定は処方を書いた側も採用した側も測らないまま 1 ラウンド分の修正として出荷されかけた（Task 19 rev3） | `task-reviewer.md` | 昇格済み（PR #78 / 群 γ） |
| 157 | 2026-08-13 | stage5 | PR #74 | 実装者が新しく書いた doc の断定に自分で変異を当てる手順が実装者側に無く、team-lead が毎ラウンド名指しで発注してようやく検出される状態が 3 ラウンド続いた（Task 19 rev3〜rev5。本 PR だけで 5 例目） | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 158 | 2026-08-13 | stage5 | PR #74 | 実装者が処方を採用する際、処方に無かった「なぜ拾わないか」の説明理由をコメントへ新しく書き、その理由が偽になる変異を誰も打たないまま次ラウンドへ渡った（Task 19 rev4） | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 159 | 2026-08-13 | stage5 | PR #74 | このリポジトリは difftastic を外部 diff に設定しており `git diff <range>` が `^diff --git` を 1 行も出さないため機械解析が静かに 0 を返し、team-lead が「新しい doc の断定は増えていない」と誤判定した（Task 19 rev4。本 PR 内で 2 回目） | `SKILL.md` / `lane-controller.md` | 昇格済み（PR #75 / 群 T） |
| 160 | 2026-08-13 | stage5 | PR #74 | 実装者の report の測定表と、その実装者が production コメントへ書いた例示が食い違ったまま出荷されかけた（測定表とコメントを突き合わせる手順が無い）（Task 19 rev5） | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 161 | 2026-08-13 | stage5 | PR #74 | 指定された変異 2 本（M-K と MTAIL）が実装上まったく同じ 1 編集になり、独立した 2 観測として報告された（同一編集に潰れていないかを確認する手順が実装者側に無い）（Task 19 rev5） | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #78 / 群 α） |
| 162 | 2026-08-13 | stage5 | PR #74 | 観測を手動スモークから自動テストへ移す作業で、受け皿となる spec は作られたが、手動チェックリスト側からの項目削除と旧仕様文言の更新が同じ差分に入るのか別作業なのかが定義されていなかった | `lane-controller.md` | 昇格済み（PR #78 / 群 ι） |
| 163 | 2026-08-13 | stage5 | PR #74 | `task-reviewer.md` の「タスク単位レビューでは `定義の穴` 欄を出さない」と dispatch の指示が食い違ったまま解消されておらず、本 PR の 10 本のタスク単位レビューのうち 1 本だけが欄を空にし他 9 本は記入した（PR 28 に続き 2 回目） | `task-reviewer.md` / `lane-controller.md` | 昇格済み（PR #75 / 群 V） |
| 164 | 2026-08-13 | stage5 | PR #74 | dispatch が変異箇所を「例:」付きの箇条書きで渡したため、それが「渡された分」（必ず打つ）なのか自発設計のための材料なのかが定義に無く、PR 単位レビュアーが 1 本を推論で片付けて未実施のまま報告を書きかけた | `task-reviewer.md` / `lane-controller.md` | 昇格済み（PR #75 / **群 V には入れない**）— 例外: 1 回。**機構は「優先順位の未定義」ではなく「渡した一覧の**書式**が『必ず打つ分』と『材料』を区別しない」である。** 手当てを `lane-controller.md`（変異一覧に「必ず打つ: N 件」の件数宣言）へ書いたため閉じる。**次のゲートは本行を群 V の前例に使わないこと** |
| 165 | 2026-08-13 | stage5 | PR #74 | contract-owner が追記した裁定が、その裁定自身が名指しした適用者（lane-controller）へ届く機構を持たない（§119.7 の 6 行に対し `grep -c '§119' progress.md` = 0 のままレーンが完了報告に至りかけた。契約の受領を ledger へ記帳する手順が定義に無い） | `lane-controller.md` / `contract-owner.md` | 昇格済み（PR #78 / 群 ζ） |
| 166 | 2026-08-13 | stage5 | PR #74 | PR 単位レビューの Minor を ledger の deferred minor 行へ転記する規則が無く、全ブランチレビューへ渡る roll-up の母数が 30 件ではなく 26 件になった（「一覧」が何から作られるかが定義されていない） | `lane-controller.md` | 昇格済み（PR #78 / 群 η） |
| 167 | 2026-08-13 | stage5 | PR #74 | parked 項目の裁定文に添えられた「族として閉じる手段が現行の技法に無い」という技法の不在の主張を、誰も反証しないまま次の層へ渡す形になっている（書く側にも triage する側にも検算の手順が無い） | `lane-controller.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 168 | 2026-08-13 | stage5 | PR #74 | レビュー指摘への修正コミットが production コメントへ新しい断定を書き足すとき、その断定を実測する手順が定義に無い（`task-reviewer.md` 群 E はレビュアー側にだけ課しており、implementer が修正の理由として書く断定は射程外だった） | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #75 / 群 E。**書き直し**） |
| 169 | 2026-08-13 | stage5 | PR #74 | `git ls-files -s` を実行ビットの復元確認に使う指示が dispatch に書かれたが、これはインデックスのモードを返すので worktree の喪失を検出しない（指定された確認手順が構造的に空振りする形） | `lane-controller.md` | 昇格済み（PR #78 / 群 β） |
| 170 | 2026-08-14 | stage5 | stage5 ゲート | contract-owner が契約 §119.7 に「`:2466` は `../` が**無いので**そのまま正しい」と書いたが、`../` は直前の `:2465` に在った。検査 17 ② の grep が行をまたげず、その像をそのまま実測として読んだ形（推定）。**同章の §119.9 / §73.5 は別の理由（`CARGO_MANIFEST_DIR` 起点）で書かれており、そちらは真だった** | `contract-owner.md` | 昇格済み（PR #75 / 群 E）— 例外: 1 回。群 E の書き直しと同じ機構であり、**contract-owner だけが配置から漏れていた**ため同じ PR で置いた |
| 171 | 2026-08-14 | stage5 | stage5 ゲート | team-lead が契約変更要求に「契約は**3 箇所で**同じ誤った理由を書いている」と添えたが、実際に当たるのは 1 箇所だった。**結論が 3 箇所で同じだったので理由も同じだと読み、1 箇所しか読んでいない** | `SKILL.md`（群 F の再発） | 昇格済み（PR #75 / **群 F。書き直し**）— 例外: 1 回。**手当ては 2 箇所に置いた** —— `SKILL.md`「言明の一部だけを測って…」へ【規則】「結論が複数箇所で同じでも、理由は箇所ごとに別々に測る」（書いた側）と、`contract-owner.md`「断定は、それを偽にする入力を 1 つ探してから書く」（誤りの発生側） |
| 172 | 2026-08-14 | stage5 | PR #75 | markdown を機械検査する検査（`**` のパリティ / 引用解決の `grep`）が **code span を認識せず**、`` `**Task 11 相当: complete` `` の中の `**` を数えて偽陽性を出した。**team-lead 側とレビュアー側の 2 系統が同じ盲点を持ち、互いを検算できなかった**（raw 5 個 → code span 除去後 4 個。群 I の再発） | `SKILL.md` | 昇格済み（PR #75 / 群 I。**追記**）— 例外: 1 回。**「markdown を機械検査するときは code span を先に落とす」と「2 者が同じ道具を使っていれば独立ではない」を §6 の引用解決検査へ足したため閉じる** |
| 173 | 2026-08-14 | stage5 | PR #75 | `SKILL.md` §6 に足した引用解決の検査が「**見出しの語で引く**」向きだけで書かれており、**引用側が持つ装飾（`**`）で切れる形を構造的に検出できなかった。3 ラウンド連続で毎回 1〜2 件ずつ出た** | `SKILL.md` | 昇格済み（PR #75 / 群 I。**追記**）— 例外: 1 回。**検査を「見出し → 引用」と「引用 → 実体」の 2 方向にし、比較を正規化してから行うことを明記したため閉じる**。**#172 と同じ便で見つかった別機構**（あちらは道具の盲点、こちらは検査の向き） |
| 174 | 2026-08-14 | stage5 | PR #75 | レビュー判定が届く前に次ラウンドの修正依頼を送る形が **3 回**続き、毎回「判定は既に送信済み」の往復が 1 つ増えた（依頼側は前ラウンドの判定を待たずに次を投げ、レビュアー側は同じ内容を 2 度書いた）。**「測定時 HEAD を書く」規則は毎回どちらが古いかを切り分けたが、往復そのものは減らさなかった** | `SKILL.md` | 昇格済み（PR #75 / 群 A の近縁として `SKILL.md` の監督表へ 1 行）— 例外: 1 回（3 事象）。**「前ラウンドの判定を受け取ってから次の依頼を送る」を監督表へ書いたため閉じる** |
| 175 | 2026-08-14 | stage5 | PR #75 | レビュアーが「この N 行を直せば APPROVED」と約束した後、**その約束に付随して依頼した作業（`定義の穴` の転記）が新しい配置漏れを生み**、約束と次の判定が衝突しかけた。**約束の射程に「その修正が生む新しい記述」が含まれるかが定義に無い** | `task-reviewer.md` | 昇格済み（PR #75 / 群 E の処方側）— 例外: 1 回。**「N 行だけ」と書くときは、その修正が生む新しい記述が射程に入るかを 1 行書く、を `task-reviewer.md` へ書いたため閉じる** |
| 176 | 2026-08-14 | stage5 | PR #75 | **レビューが承認した sha と、実際にマージされた sha が違った。** `APPROVED` は `df5d06f` に対して出たが、マージされた `0934abf` には承認後の `df5d06f..95fd6fe`（3 ファイル / +8 行）が含まれる。**レビュアーが「マージ前の追記でも次のゲートでも構わない」と明示した分ではあるが、その 8 行はレビューを一度も通っていない。**PR 本文には「承認後に 1 コミット積んだ」と明記したが、**それは口頭補填であって定義側の手当てではない**（レビュアーが後追いで測って健全と確認） | `SKILL.md` / `lane-controller.md` | 昇格済み（PR #78 / 群 δ） |
| 177 | 2026-08-14 | stage5 | M2-4 dispatch | **team-lead が §73.5 の M2-4 の欄を読みながら、欄に挙がっていた `§63` を読まずに dispatch した。** 欄の 🔴 が付いた 2 章（§119 / §122）だけを拾い、印の無い章を素通りした。**§63.7 は逐語で「`resume_session` が既存の起動経路を通る」を M2-4 の dispatch 時の worklist として持っており、契約 §123 の最強の根拠だった。索引の 4 回目の空振りである** | `SKILL.md` | 昇格済み（PR #78 / 群 G） |
| 178 | 2026-08-14 | stage5 | 契約 §123 | **計画 Task 2 が、M1-4 が契約 §23 で閉じた穴を別名の関数で開け直す形になっていた。** `CliInvocation` は `program` に裸のバイナリ名を置き env フィールドを持たないため、**§18 の PATH 解決 / `KAMUX_SESSION_ID` / M3-3 のヒューリスティック装着が resume 経路だけで退行する**（3 件目は要求元も team-lead も未指摘。contract-owner が実測で発見） | 計画 `M2-4-resume.md` | 契約 §123 として採用（**作るものの話。定義側では直さない**） |
| 179 | 2026-08-14 | stage5 | M2-4 Task 3 | **team-lead が、稼働中のレーンと同じ作業ツリーで `git commit` を打ち、コミット 2 本がレーンの作業ブランチへ落ちた。** stage5 は隔離不要なので team-lead とレーンが同じチェックアウトを共有しており、**`git commit` はそのとき出ている HEAD に落ちる。** レーンが Task 3 の実装中に気づいて報告した（レビューパッケージをパス限定で作り直して実害は塞いだ） | `SKILL.md` | 昇格済み（PR #78 / 群 θ） |
| 180 | 2026-08-14 | stage5 | M2-4 Task 3 | **上の 2 本を push したつもりで `git push -q origin main` を打ったが、HEAD がレーンのブランチだったため**ローカル `main`（= 既に origin と同一）**を push して無変化で成功した。** `-q` と相まって出力は成功と区別が付かず、team-lead は「push 済み」と読んだ | `SKILL.md` | 昇格済み（PR #78 / 群 β） |
| 181 | 2026-08-14 | stage5 | M2-4 Task 3 | **`lane-controller.md` の検証 C の例外 3（「当該 PR の付帯作業のコミット（ledger / `AGENT-LESSONS.md` など）」）が、形だけでは自分の付帯作業と他エージェントの混入を区別できない。** 本件の 2 本は例外 3 に当たるため**検証 C が鳴らない** —— レーンが自力で気づかなければ検出されなかった（レーンの申告） | `lane-controller.md` | 昇格済み（PR #78 / 群 β） |
| 182 | 2026-08-14 | stage5 | M2-4 Task 3 | **レーンが混入コミットの作者を `lessons-triage-stage5` と推定して報告したが、実際は team-lead だった。** 内容（台帳への追記）から推定しており、**推定であることは明記されていなかった**。実害は無いが、誤った帰属のまま手当てを設計すると当事者に届かない | `lane-controller.md` | 昇格済み（PR #78 / 群 E） |
| 183 | 2026-08-14 | stage5 | M2-4 Task 2 | **契約 §23（`build_launch_command` / `ResumeMode` / `LaunchCommand` を所有）が §73.5 の M2-4 の欄に載っていなかった。** レーンは欄どおりに 13 章を読んだが到達せず、**実コードの grep で初めて見つけた**。**「索引は所在だけを持つ」は、章が索引に載っていなければ機能しない**（#177 とは別機構 —— あちらは**載っているのに読まれなかった**、こちらは**載っていない**） | 契約 §73.5 | 契約 §123 として採用（**contract-owner が §23 と §123 を欄へ追加済み**） |
| 184 | 2026-08-14 | stage5 | M2-4 Task 3 | **lane-controller が再レビューへ「必ず打つ」と指定した変異が、実行するとランタイムエラーで落ちる形だった。** SQL から `WHERE` 句だけを削り `params!` 側の第 2 引数を残したため `rusqlite` の「Wrong number of parameters」で落ち、**意図した全行 UPDATE の実行結果を見る前に赤になる（赤の理由が違う）**。レビュアーが是正版を自己設計して救った。**同じ dispatch に「2 件が別々の編集になることを確認した」と書きながら、変異が実行可能かは当てていなかった**（レーン自身の申告） | `lane-controller.md` | 昇格済み（PR #78 / 群 α） |
| 185 | 2026-08-14 | stage5 | M2-4 引き継ぎ | **team-lead が、引き継ぎ報告を返して終了したレーンへ `SendMessage` を 2 通送って再稼働させ、同時に後継レーンも起動したため、同じフェーズに lane-controller が 2 体になった。** 先行レーンは既に implementer を dispatch しており、後継が着手前に気づいて BLOCKED で止めなければ `cli_args.rs` に implementer が 2 体乗るところだった。**`SKILL.md` は「引き継ぎ報告が返ったら同じ形で次のレーンを起こす」と定めているが、同じレーンへ追加の便を送ると再開することを書いていない** | `SKILL.md` | 昇格済み（PR #78 / 群 ε） |
| 186 | 2026-08-14 | stage5 | M2-4 引き継ぎ | **team-lead が起動指示に「`origin/main` の台帳は 184 行」と単位を書かずに数値を渡し、受け手が測った 299 行（ファイル総行数）と食い違った。** 受け手は「差分が空であることが実測である」として数値の不一致に引きずられず正しく結論したが、**単位の無い数値は受け手に別の量を測らせる** | `SKILL.md` | 昇格済み（PR #78 / 群 Q） |
| 187 | 2026-08-14 | stage5 | M2-4 引き継ぎ | **`origin/main` が作業ブランチの祖先でないことを team-lead が見落とした**（merge-base = `ead958f`、main は `1f3d7c1`）。**契約 §101.2 の P1 でマージ手順に入る側が `git merge origin/main` を当てる必要がある。** 後継レーンが着手前スキャンで見つけて申告した | `SKILL.md` | — |
| 188 | 2026-08-14 | stage5 | PR #76 | **持ち越し `minor (deferred)` の最終判定者と判定タイミングが定義に無い。** PR 単位レビューの dispatch が個別に指示して初めて 10 件が判定された（`lane-controller.md` はフェーズ最終報告での「列挙」しか持たない） | `lane-controller.md` | 昇格済み（PR #78 / 群 ι） |
| 189 | 2026-08-14 | stage5 | PR #76 | **production の doc コメントへ絶対テスト本数（`750 passed`）が測定日もコミットも無しで焼かれ、マージ時点の実測 767 と食い違った。** 同種は同 PR の Task 4 でも起きており 2 回目 | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #78 / 群 Q） |
| 190 | 2026-08-14 | stage5 | PR #76 | **production の doc が引用する測定コミット（`424d8a2`）が、squash マージのため `origin/main` から到達不能になった**（`git merge-base --is-ancestor 424d8a2 origin/main` = 偽）。**断定そのものは正しく、壊れているのは引用の形だけである。** #189（測定日もコミットも無い絶対テスト本数を doc に焼いた）の手当てが次の穴を作った形。マージ後に advisor 指摘で発覚し、直せず Task 8 への申し送りになった | `rust-implementer.md` / `web-implementer.md` | 昇格済み（PR #78 / 群 Q） |
| 191 | 2026-08-14 | stage5 | PR #76 | **team-lead が作った `.claude/worktrees/team-lead`（main を checkout）が、レーンの `gh pr merge --delete-branch` を `fatal: 'main' is already used by worktree` で失敗させた。** GitHub 側のマージと検証 D は成立しており実害はローカルのブランチ削除だけだが、**レーンは「マージが失敗した」と読める出力を受け取る**（台帳 #118 と同型 —— worktree から `gh pr merge` を打つと成功しても失敗に見える） | `SKILL.md` | 昇格済み（PR #78 / 群 β） |
| 192 | 2026-08-14 | stage5 | 契約 §124 | **レーンが新設した手動スモーク項目のカテゴリ申告（§26.4 カテゴリ 1）が誤っていた。** カテゴリ 1 の内容は逐語で「`.app` を Finder からダブルクリックしたときの PATH 解決」だが、**項目の 3 欄に `.app` / Finder / PATH 解決が 1 つも無く、操作欄は `npm run tauri dev` だった。** **害は実在する** —— カテゴリ 1〜5 は §32.5 の PR ラベルの根拠で、M2-4 の PR は §32.5 の 7 本にも §78.3 の総目録にも無い。**そのまま読むとレーンは契約内で解けない矛盾に入る** | 計画 `M2-4-resume.md` | 契約 §124 として採用（カテゴリ 6 へ訂正） |
| 193 | 2026-08-14 | stage5 | 契約 §124 | **新設した手動スモーク項目の観測欄が、測りたい機構が死んでいても合格を出す形だった。** hooks 疎通を測るはずの後半が `BelDetected` / `SilenceTimeout` 由来の 🟡 / ⚪ に到達でき（`runtime_state.rs:149-150`。**どちらも hooks 非依存**）、**hooks が死んでいても合格する。** 🟢 も `StateInput::Spawned` で立つので操作欄 ② が陽性の対照にならない。**`SKILL.md` の 4 検査は「観測点の実在」までしか見ておらず、「その観測が、測りたい機構が死んでいても出るか」を問わない** | `SKILL.md` | 昇格済み（PR #78 / 群 α） |
| 194 | 2026-08-14 | stage5 | 契約 §124 | **契約 §66.6 の supersession 表が、行群の直前に入った空行 4 本で 5 つの表に分断されていた。** 見た目は 1 つの表に見え、**機械で全対を数えると 5 回に分かれる**。contract-owner が 1 つに戻した（行の内容は 1 バイトも変えていない。全 162 対が 3 列であることを確認）。**git 追跡外なので diff に出ず、記録しなければ帰属不明の構造変更になる** | 契約 §66.6 | 契約 §124 として採用（§124.9 に記録） |
| 195 | 2026-08-14 | stage5 | M2-4 Task 8 | **変異の適用確認に使う `git diff` のパススペックが cwd 相対のため、`cd src-tauri && … && git diff -- src-tauri/src/…` は変異が適用済みでも出力が空になる。** **`--no-ext-diff` を付けていても起きる** —— 本日 PR #75 で群 C の観測点を「変異した行が `-`/`+` に現れることを目で見る」へ書き直したが、**そのコマンド自体が cwd によって空を返す形は射程外だった。** **空の diff を「未適用」と読むと打ち直しに走る**（rust-implementer がリポジトリ直下から実行し直して目視。自分で申告した） | `rust-implementer.md` / `web-implementer.md` / `task-reviewer.md` | 昇格済み（PR #78 / 群 C） |
| 196 | 2026-08-14 | stage5 | M2-4 Task 8 | **#195 の罠を、それを記録した当人が同じ作業の次の便でもう一度踏んだ。** 実装者は report:144 に「`cd src-tauri` してから repo 相対パスで `git diff` すると空を返す」と自分で書いた後、**次のラウンドの変異の目視で同じ形を打った**（自分で申告）。**警告文では閉じない。手当ては「変異の目視は `cd` を含まない別コマンド（リポジトリ直下）で打つ」というコマンドの形でなければならない**。群 U（規則を書いた本人が同じ工程でその規則を破る）の実装者側の実例 | `rust-implementer.md` / `web-implementer.md` / `task-reviewer.md` | 昇格済み（PR #78 / 群 U） |
| 197 | 2026-08-14 | stage5 | 契約 §125 | **contract-owner が §1 のモジュールツリーの欠落を目で数えて 17 とし、1 件（`worktree/test_support.rs`）を落とした。** `comm -23` で差集合を取り直して **18** になった（自分で申告し、§125.5 に「本節の列挙を信じずに再導出せよ」と書いた）。**要求側（team-lead）は 2 件しか挙げていなかった** —— **目視の列挙は、数える対象が集合として定義できるときでも使われる** | `contract-owner.md` | 昇格済み（PR #78 / 群 μ） |
| 198 | 2026-08-14 | stage5 | 契約 §125 | **契約 §59.5 の「`sessionSlice.ts` は原理的にこの grep に当たらない」が現在の実装では偽だった**（`set((s) => …)` の updater 引数から読んでおり `:214` ほか 7 箇所）。**§122.5 の検査 5 は既に 3 ファイルの 1 つとして報告しており、契約の断定だけが古いまま残っていた。** contract-owner が別件の裁定中に副産物として発見 | 契約 §59.5 | 契約 §125 として採用（⚠️ ポインタ） |
| 199 | 2026-08-14 | stage5 | 契約 §125 | **契約 §124.10 の見出しが「8 行」と宣言しているのに表は 7 行だった**（§66.6 側は正しく 8 行受け取っている）。**件数宣言と実体の突き合わせは brief と計画については規則があるが、契約自身の節見出しには無い** | 契約 §124.10 | 昇格済み（PR #78 / 群 μ） |
| 200 | 2026-08-14 | stage5 | stage5 ゲート後 | **team-lead が台帳へ足した行の列数を 2 回落とした**（#178 / #198。どちらも `対象` 列）。**行数検算は 2 回とも合格を返す** —— 総行数は増えているため。**捕まえたのは列数検算だけである**（台帳 #106 の手当て）。**2 回とも「1 行を足す」という同じ作業で、書式のテンプレートを持たずに毎回手で書いている** | `SKILL.md` | 昇格済み（PR #78 / 群 μ） |
| 201 | 2026-08-14 | stage5 | stage5 ゲート後 | **team-lead が `git worktree remove` を worktree の中から打って cwd を消す形を 2 回踏んだ。** **1 回目の後に `TEAM-LEAD-RULINGS.md` へ「`cd -` で書かない。絶対パスで戻る」と手順を書き直したのに、次の便で同じ形を打った。** 3 回目に `git -C <絶対パス>` の形へ変えて解消。**群 U（規則を書いた本人が同じ工程でその規則を破る）の team-lead 側の実例であり、台帳 #196（実装者が自分の記録した罠を踏み直した）と同じ機構である** | `SKILL.md` | 昇格済み（PR #78 / 群 U） |
| 202 | 2026-08-14 | stage5 | M2-4 Task 11 | **lane-controller が Ruling の「理由」を『未検証の予測ではない』と断定して書いたが、実際には未検証だった。** 根拠に挙げた先行 Ruling は **Rust 側の呼び出し順しか立証しておらず、フロントが `running` を先に受け取ることは立証していなかった**。advisor の指摘で実測し直し結論は変わらなかったが、**裏取りが無ければ「PR 本文に Ruling を 1 行ずつ書く」申し送りを通じて、未検証の理由が PR 本文（`main` に残る恒久記録）へそのまま流れていた**（レーン自身の申告） | `lane-controller.md` | 昇格済み（PR #78 / 群 E） |
| 203 | 2026-08-14 | stage5 | M2-4 引き継ぎ | **team-lead の裁定中継とレーンの引き継ぎ報告が行き違い、レーンが「裁定は未着」として parked を残した。3 回目**（§123 / §124 / §125 のいずれでも起きた）。**後継の起動指示で毎回訂正しているが、その間レーンは裁定済みの件を未処理として ledger に書き、次の読み手が二重に上げうる状態が残る** | `SKILL.md` | 昇格済み（PR #78 / 群 ζ） |
| 204 | 2026-08-14 | stage5 | 契約 §125 | **群 E の「断定できないものは『未検証の予測』と明記する」が、契約への誤った着地を実際に防いだ。** レーンが CCR の提案行のうち「`resumeFailedSessionIds` の購読者」の部分を未検証と明記 → team-lead が測って §38.3 の射程外と判明 → contract-owner が当該部分を削って着地。**断定していたら誤った文言が契約に入っていた**（レーン自身が観測点として申告） | — | **観測点の実測**（`SKILL.md` §6 の「効いたと分かる signal」）。**昇格済み群 E が損害を防いだ 1 件目の記録** |
| 205 | 2026-08-14 | stage5 | M2-4 Task 12 dispatch | **team-lead が「未適用」として起動指示に書いた計画 4 箇所が、dispatch の直後に前任レーンによって適用され、後継が二重適用しうる状態になった。** team-lead が実測して即座に止めた（修正 3 は全面差し替えなので二重に当てると内容が壊れる）。**#203 と同じ「中継と引き継ぎの行き違い」だが向きが逆** —— あちらは裁定が届く前に降りた、こちらは**降りた後に作業した** | `SKILL.md` | 昇格済み（PR #78 / 群 ε） |
| 206 | 2026-08-14 | stage5 | PR #77 | **PR 単位レビューへ指定した陽性の対照 P-CLEAR の標的が、対象の純関数を直接呼ぶユニットテストで自明に赤くなる位置に在り、呼び出し側の配線（`lib.rs:107` の `validate_session_patch(patch)?;`）は測らないまま終わるところだった。** レビュアーが自発設計の M-A でその 1 行を消して初めて配線が測られた。**Task 9 の持ち越し Minor と同じ機構で 2 回目である** | `lane-controller.md` | 昇格済み（PR #78 / 群 α） |
| 207 | 2026-08-14 | stage5 | PR #77 | **dispatch が変異 P-ORDER の期待値を「赤になること」と指定したが、実行すると 783 全緑だった。** 対象の `resume_session` は契約 §96.4 の到達不能領域であり、**差分の doc 自身が「同型の変異が全緑になる」と明記していた** —— **一次資料が全緑を予告している箇所に、指定側が赤を期待していた** | `lane-controller.md` | 昇格済み（PR #78 / 群 E） |
| 208 | 2026-08-14 | stage5 | PR #77 | **dispatch が再評決の修正差分を「観測を持たない doc」と断定したが、陽性の対照 F-SEED を打つと既存テスト 2 本が赤になり、断定は偽だった。** #207 と同じ「dispatch がテストの被覆について未測定の断定をした」機構である | `lane-controller.md` | 昇格済み（PR #78 / 群 E） |

| 209 | 2026-08-14 | stage5 | 契約 §125 → §126 | **却下記録の 1 セルが、同じセルの中で前段と逆のことを書いた。** 前段「`docs/superpowers/` は git 追跡外」（真）／後段「`M2-4-resume.md` は追跡下であり」（偽 —— 同じ 1 行の exclude に入る）。**結論（計画に 1 行置く）は別の理由で生きていたので、誰も気づかないまま次章が引用しかけた。** `git ls-files docs/superpowers` = 0 行で 1 コマンドで割れる | `contract-owner.md`（「〜が無い／〜だけである」の裏取り規則は在るが、**「〜が在る／追跡下である」の肯定形の断定を裏取りする規則が無い**） | 昇格済み（PR #78 / 群 E） |
| 210 | 2026-08-14 | stage5 | PR #77 / 契約 §126 | **成果物が計画どおり作られたのに、それを装着する工程がどの計画にも無かった**（`InterruptedOverlay.tsx`）。**`Files:` の `Create` は在り `Modify` が無い形。** §25.4 のドリフト検査は逆向き（誰も Create しないまま Modify される）しか見ないので、**この向きは 12 計画 171 タスクの全期間で一度も鳴らなかった** | `lane-controller.md` / 契約 §25.4 の検査 | 昇格済み（PR #78 / 群 I）+ 契約 §128.3 として採用（検査 20。🔴 ただし計画側ではなく実装側。計画側の逆向きは母数 171 / 当たり 62 で使えない） |
| 211 | 2026-08-14 | stage5 | M2-4 完了 | **`lane-controller.md` の「フェーズ完了時」が (1) 最後の PR をマージ → (2) 全ブランチレビュー → (3) 修正 implementer を起動、の順で書かれているが、(2) の時点でブランチは `--delete-branch` で消えており (3) は構造的に実行できない。** 本フェーズでは **Critical 1 件（L-1）が手当ての経路を持たないまま main に着地した**。**`.claude/AGENT-LESSONS.md` への転記路も同時に閉じるので、レーンはこの穴を台帳行にできず報告で渡すしかない。毎フェーズの最後で必ず起きる**（レーン自身の申告） | `lane-controller.md` | 昇格済み（PR #78 / 群 δ） |
| 212 | 2026-08-14 | stage5 | M2-4 完了 | **M2-4 が `TerminalPane` を通らない起動経路を初めて作り、再開したカードを開くと `InvalidState("session … is already running")` が出て `runtimeErrors` が汚染される**（L-1。`KanbanCardResume.tsx:64` × `TerminalPane.tsx:46` × `session/mod.rs:74`）。**🔴 破れた前提の言い方を訂正した（契約 §127.5、2026-08-15）。`startSession` の production 呼び出しは今も `TerminalPane.tsx:46` の 1 本である。破れたのは「PTY を起動する production 経路が `TerminalPane` の 1 本だけ」という別の前提で、`resume_session` が 2 本目になったのにフロントの起動済み登録簿（`ptyBridge`）がそれを知らない。旧文のまま読むと `grep -rn 'startSession' src/` が 1 を返すのを見て「直った」と誤判定する。** **赤は恒久化しないが「1 瞬」でもない** —— 次の `session://state` まで残り、ターミナルバッファの notice は状態イベントでは消えない | 計画 / 契約 | 契約 §127 として採用（手当ては PR H2） |
| 213 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **team-lead がドリフト検査の依頼に個別の検査番号を並べたため、指定した集合が実質的にスコープになり、`§59.7` の登録簿 19 種のうち 12 種しか走らなかった**（検査 5〜13 / 16〜17 が未実施）。**contract-owner が報告の最後に「指定範囲に含まれないため走らせていない」と明記して返したので閉じた。明記が無ければ「12 種で全部」と読んでゲートを閉じていた** | `SKILL.md` | — |
| 214 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **team-lead の brief が `diff -u` の既定 `-U3` を前提に「変更領域が 2 であること」と期待値を書いたが、行 28 と 32 は距離 4 で 2 hunk が畳まれ 1 が返る。** 再現する者が「1 = 壊れた」と読む形だった（適用者が実測して報告） | `SKILL.md` | — |
| 215 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **稼働中のレーンが主ツリーを占有していたため、ゲートのドリフト検査が `main` ではなく `fix/h2-…` ブランチで測られかけた。** そのまま走れば**検査 19 が 0 件を返し、「H2 が着地した」と「検査が壊れた」が区別できなくなっていた**（contract-owner が走らせる前に気づき、`git archive origin/main` で別ディレクトリへ取り出して測った） | `SKILL.md` / `contract-owner.md` | — |
| 216 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **契約 §111.3 の影響表は「どのファイルの何を触ったか」を書く形式で、「§66.6 の supersession 対の表に登録したか」の欄を持たない。** §126 は自分で行を立てたので足せたが、**§127 / §128 は同じ行を書かず、検査 12 が違反 4 件を検出した。構造が強制していない** | `契約 §111.3` | — |
| 217 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **検査 12（契約自身の後方ポインタ）は §66.5 の逐語が「表の**旧章**列がすべて出力に現れることを確かめる」= 表 → 契約 の向きしか測らない。** **contract-owner が作った 4 件は逆向き（ポインタは実在し §66.6 への登録だけが欠落）で、書かれた形をそのまま走らせても 1 件も出なかった**（#210 / 契約 §128.3 と同族。§128.4 が「他の 18 検査の逆向きは裁定していない」と明記したうちの 1 件が実際に破れた） | `契約 §66.5` | — |
| 218 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **同じ名前の別の量を測って、相手の報告を疑いかけた。本日 4 回**（§66.6 の「167 対 177」= `**あり**` の数 対 対の総数 / `grep -c 'なし'` が節全体 対 ポインタ列 / 「§101.4 を引く箇所 2 対 3」= 転移集合 対 引用全部 / 「`43408c8` は 8 対 6 対 11」= 含む行数 対 相異なる主張 対 機械導出の出現数）。**4 回とも疑ったまま返す前に測り直したので往復は増えていない** | `SKILL.md` / `task-reviewer.md` | — |
| 219 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **特定の文字列の出現数を「状態」の代理にした。** team-lead は「消えたことを 0 件で測る」readback を設計したが、**訂正が旧文を引用する形と両立しない**（引用は残すべきもの）。task-reviewer は 3 回、**処方の逐語に依存した検算文字列**で「未修正」と読みかけた（`#7 の行` / `成り立たせているのは…の原理` / `43408c8 で凍結`）。**契約 §129.7 が設計した対（消すべき語 0 / 置くべき語 1）は「差し替え」には効くが「差し替え + 訂正記録を残す」には効かない** | `task-reviewer.md` / `SKILL.md` | — |
| 220 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **レビュー判定を依頼してから判定が返るまでの区間に、ブランチへコミットを積まない規律がどの定義にも無い**（群 δ 規則 1 はマージ許可の時点しか見ていない）。**PR #78 で 4 回起きた。2 回目は「`43408c8` で凍結する」と明言した便で起きている。** **4 回とも内容は正しく順序だけが誤っており、原因は不注意ではなく「レビュアーが指摘を送る → team-lead が正しく直す → 直した瞬間に宣言が古くなる」という循環である**。**🔴 恒久記録が自分でこの失敗形を実演した** —— **PR 本文の上位（`:5`）が「取り直した後にコミットを積むと本文はその瞬間にまた偽になる」と失敗形を記述している同じ文書で、下位（`:173`）が 3 版目の文言のまま残って偽になっていた**（レビュアーの記録。**害は無い。上位が正しく訂正しているため**）。**直さない判断を採った理由: `:173` を直す差分は 5 版目を生み、5 版目は必ず新しい記述を持つ。害 0 の記述を直すために、害が出うる工程をもう 1 回回す理由が無い**（群 γ） | `SKILL.md` / `lane-controller.md` | — |
| 221 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **task-reviewer が `REPORT_WRITTEN_AT` の 3 つの時刻を測らずに書いた**（`date` を打ったのは最初の 1 回だけで、2 つは未来の時刻）。**「宣言 sha と実 HEAD の時系列」を根拠に Important を挙げているレビューが、その時系列を書く欄を捏造していた**（本人の自己申告。報告の冒頭で 3 つを無効化した） | `task-reviewer.md` | — |
| 222 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **既存のブロック引用の**途中**へ新しい `###` を挿入したため、引用の末尾行が新設節の配下へ移った**（PR #78 で 2 件）。**群 U の書き直しは「他ファイルからの見出し引用」までしか射程に持たず、同一ファイル内で自分の挿入が既存ブロックを割る形を見ない。** **直前行だけを見る検査では出ない**（割れたかは `git show main:<file>` との対照でしか決まらない） | `SKILL.md` / `lane-controller.md` | — |
| 223 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **昇格 PR の必須項目「観測点」を書いた本人が、そのコマンドを 1 回も打っていなかった。** **打つと 2 群とも判定不能または不合格を返した**（群 δ の観測点はファイル全体 grep で初出行を比べており合格と不合格を区別しない / 群 ι の観測点は個別カウントを返し glob に guard が無い）。**群 U の「渡したコマンドを 1 回実行したか」は**便**に掛かっており、定義ファイルへ書く観測点には掛かっていない** | `SKILL.md` | — |
| 224 | 2026-08-15 | stage5 ゲート | PR #78 / stage5 ゲート | **台帳の列数検算の対象表が定義に無く、事象表だけに当たって棚卸し表が母数から外れた。** PR #78 は棚卸し表へ 13 行足しているのに、本文の断定「6 行目が出ていないことが唯一の証拠」は事象表しか見ていなかった（**実害 0 件。断定の射程だけが名指しした範囲ちょうどで閉じていた**） | `SKILL.md` | — |
| 225 | 2026-08-15 | — | PR #80 | **`surface` を渡す 5 箇所のうち 4 箇所に契約 §81.2 カテゴリ 3 の変異が打たれ、`ensurePtySubscription` の 1 箇所だけが 3 ラウンド・全 34 変異から漏れて 713 本全緑のまま残った**（同一スコープで同じ値を渡す箇所を列挙してから打つ手順が無い。マージ直前の PR 単位レビューが初めて発見した） | `task-reviewer.md` / `lane-controller.md` | — |
| 226 | 2026-08-15 | — | PR #80 | **スコープ付き再レビューが自分で設計した全緑変異を「範囲外の観察」へ分離して評決に算入せず、lane-controller が拾い直したため修正ラウンドとレビューが 1 往復増えた**（スコープ限定の再レビューで見つけた全緑を指摘にするか分離するかの定めが無い） | `task-reviewer.md` | — |
| 227 | 2026-08-15 | stage5 ゲート | ユーザーの指摘 | **team-lead が停止したレーンを 2 時間 14 分放置した。** `SKILL.md` §6 の「1 時間以上応答が無い → 停止を疑う」は在ったのに発火しなかった。**機構は規則の不在ではなく起床の設計である** —— **自分の起床を「通知が来ること」に依存させていた。** レーンの idle 通知を受けて実測し「CI 待ちだろう」と判断して終わり、**次に自分が動く条件を何も設定していない。** **検出すべき事象は「レーンが止まる = 通知が来なくなる」なので、通知待ちで構えている限り構造的に検出できない。時計で起きる必要があった。結果タイマー役がユーザーになった。** **併せて群 F を 1 件重ねている** —— `frontend pending` の 1 点だけを測って「CI が終わればレーンが進む」を前提として扱った（**CI はその後 3m45s で pass している**）。**手当てはコマンドの形で置いた** —— 後継レーンに `Monitor` を張り、10 分無進捗 / 25 分無進捗 / MERGED / CLOSED の 4 条件で発火させた（**沈黙を成功と読まないよう失敗の形も拾う**）。**閾値を 10 分にしたのは、規則の 1 時間が「停止と判定してよい時間」であって「気づいてよい時間」ではないため** | `SKILL.md` | 昇格済み（PR #88 / 群 ξ）— **例外: 1 回。**手当て（後継レーンへ張った `Monitor` の 4 条件）を既に書いたため開かない。**群 A（起きたら同じ応答の中で次の一手を打つ）とは機構が違う** —— 群 A は起床イベントが届くことを前提にその後の一手を定めるが、本件は**起床条件を 1 つも張っていないので、そのイベントが構造的に来ない。** 手当ては `SKILL.md` §6（正典）と `lane-controller.md`「対処 1」末尾（一般則）の 2 箇所に置いた |
| 228 | 2026-08-15 | — | PR #80 | **`lane-controller.md` のマージ手順のコードブロックは中央に `# ★ ここで一度止まる`（`:495`）を置いて分割を明示的に指示しているが、`TIP`（`:459`）と `MC`（`:507`）はその分割をまたぐシェル変数であり、Bash 呼び出しを分けると失われる。** **🔴 レーンは「検証 D が引数なしで走って偽の合格（空）を出す」と報告したが、team-lead が再現したところ理由が偽だった** —— **実物の `:508` は `git diff \"$MC\" \"$TIP\" --stat` とクォート付きで、空文字 2 つを渡すと `fatal: ambiguous argument ''` / exit 128 で落ちる（大きな音を立てる安全な失敗）。** **偽の合格になるのはクォートを外した場合だけ（出力 0 行 / exit 0）。結論（分割で変数が失われる危険は実在する）は生きる。** **同ファイル `:740` は「`TIP` はシェル変数なので、途中で降りると後任は検証 D・E・F を実行できない」と既に書いており、欠けているのは「同一レーン内で Bash 呼び出しを分けても失われる」の一点である。** **契約側の原本（§32.2 / §101.2）に `ここで一度止まる` は 0 件で、分割指示は定義ファイル側にしか無い。** **PR #80 では 1 回の呼び出しにまとめて回避したので実害 0 件** | `lane-controller.md` | — |
| 229 | 2026-08-15 | — | ユーザーの指摘（stage6 前の残タスク棚卸し） | **team-lead が stage6 の残タスク一覧に「契約は削除 UI を M3-4 Task 12 の担当と書くが、計画の Task 12 は作成も削除もしないと書いている。dispatch 前に解消が要る」と載せ、ユーザーに裁定を求めた。実際は §49.7 が 2026-08-02 に確定させ、§49.8 の worklist も計画側へ適用済みだった**（`M3-4-ops-ux.md:2696`-`:2704` に追記ブロックが実在）。**「計画は作成も削除もしない」の出典は計画ではなく契約 §44.4 の 2026-08-01 の散文であり、同じ論点に答えた後続章 §49.7 を読んでいない。** **機構は #123 系（充足済みの裁定が「未処理」と読める形で残る）の再演だが、場面が新しい —— 今回はレーンの dispatch ではなく残タスクの棚卸しで、しかも読み手は契約の書き手ではなく team-lead 自身である。** **🔴 章番号順に読んだことが原因である** —— **申し送りは章番号の小さい側に在り、それに答えた裁定は必ず末尾側に在る。実測 1 件（§44.4 → §49.7、章 5 つ後）** | `SKILL.md` / `lane-controller.md` | — |
| 230 | 2026-08-15 | — | PR #88（群 ξ の昇格便。自己適用） | **昇格させた 群 ξ を自分の便へ当てて `task-reviewer` の待ちに `Monitor` を張ったが、待ち条件を「報告ファイルの mtime」1 点にしたため、レビュアーが報告を書き始める前の 10 分で `STALL:` が誤爆した。レビュアーは健全で、同じ scratchpad に検証用フィクスチャを 2 本書いていた。** **機構は 群 ξ ではなく 群 α である**（r2b のレビュアーが判定。**team-lead は当初 群 ξ と断定し、群 α / 群 β を検討した形跡を書いていなかった** —— 群 E の射程）。**能動的な機構は張ってある。破れたのは張った機構の判別力であり、「まだ書き始めていない」と「死んだ」が同じ観測になる。** **群 α が自ら「群 P と同じ工程が欠けているが宛先が違ったため 7 行が射程外に落ちた」と書いているのと同じ形が、宛先を変えてもう一度起きた** —— 群 α の宛先は「変異 / 陽性の対照 / スモークの観測欄」で、**待ち条件・起床条件が射程外だった。** **`lane-controller.md` の「レポートファイルの存在を条件にしない」は存在の側だけを警告しており、mtime の側が抜けていた** | `SKILL.md` / `lane-controller.md` | 昇格済み（PR #88 / **群 α。射程を広げる書き直し。8 行目**）— **例外: 1 回。**同じ PR の成果物の変更で直した —— **群 α の宛先に「待ち条件・起床条件」を足し（`SKILL.md` 人間ゲートの検査 5）、待ち条件の判定基準（系統数 / 生存の陽性証拠の由来 / 終端状態の 3 条件）を `lane-controller.md`「対処 1 の続き」(3) へ**正典として 1 箇所だけ**置いた**（`SKILL.md` は指すだけで写さない —— **当初 2 箇所へ写したところ 2 版が食い違い、同じ入力に別の評決を出す形になった。群 λ**）（**追記ではなく宛先の書き直しである。**昇格済みの規則の再発は追記で足さない） |
| 231 | 2026-08-16 | — | PR #88（マージ直前の CI 待ち） | **CI 緑を待つ `Monitor` の状態パース（`python3` のワンライナー）がクォートの入れ子で壊れて空文字を返し、その空文字が `case` の「進行中」の腕に落ちた。** **`PROGRESS:` の行に状態が 1 つも出ていないことに team-lead が気づいて直接 `gh pr checks` を打ったところ、実際には既に `frontend=SUCCESS rust=SUCCESS` で緑だった。** **機構は 群 α である** —— **自分で設計した観測の、パースが失敗したときの出力を検討しておらず、失敗が「まだ待っている」とまったく同じ観測になった。** **同じ便で昇格させた 3 条件（系統数 / 生存の陽性証拠の由来 / 終端状態）は「どの信号を見るか」しか定めず、「各信号の取得が失敗したとき何を出すか」を問わない** —— **3 条件をすべて満たしたまま、この形は通る。** **併せて 群 I（解析コマンドを、その解析が曖昧になる入力に当てて検算していない）を 1 件重ねている** —— パーサを一度も空入力・壊れた入力に当てていない | `SKILL.md` / `lane-controller.md` | — |

**`ステージ` 列は棄却判定に要る。** 「初出からいくつステージを跨いだか」は、日付や PR 番号からは求まらない（ステージは日付でも PR 番号でも区切られない）。**レーンの外で行った作業は `—` と書く**（次のステージ完了ゲートを 1 つ目として数える）。

## 昇格済みの規則（棚卸し用）

| 規則 | 昇格 PR | 所在 | 観測点（効いたと分かる signal） |
|---|---|---|---|
| 起きたら**同じ応答の中で**次の一手を打つ（区切りで応答を終えない） | PR #57 | `lane-controller.md`「対処 1 の続き」 | **タスク境界での 1 時間以上の無応答が 0 回**（M3-1 は 3 回・最大 82 分） |
| 渡された変異を打ったあと、**自分で設計した変異を必ず足す** | PR #57 | `task-reviewer.md`「変異は、渡された分を打ってから…」 | レビュー報告の `変異検証:` 行に**指定分以外の変異が 1 件以上**現れる |
| 人間ゲートは **観測点の実在 / 契約との向き / 不合格の形**の 3 点を渡す前に検算する | PR #57 | `SKILL.md`「人間ゲートの手順は、渡す前に実装で検算する」 | 人間ゲートの各項目が**「どう見えたら不合格か」を 1 行持っている**。実施後に「手順が空振りだった」が 0 件 |
| **報告項目そのものを落とさない** —— 完了報告テンプレの全項目を埋める（`npm run lint` / `fmt:check` / §26.5-3 の仕分け） | PR #57 | `lane-controller.md`「フェーズ完了時」の報告フォーマット | 完了報告に**空欄の項目が 0 件**。lint / fmt:check が team-lead の PR 作成時ではなくレーンの報告に現れる |
| 報告と PR 本文の数値は**最終コミットに対して取り直す** | PR #57 | `lane-controller.md`「報告と PR 本文に書く数値は…」 | PR 本文のテスト件数が、**マージ直前に team-lead が実行した実測と一致**する |
| テストの**恒真 2 形**（`> 0` の述語 / 置き換え後も恒真）を疑う | PR #57 | `task-reviewer.md`「テストが恒真になっていないか」 | レビュー報告に**恒真 assert への変異が 1 件以上**含まれる |
| **「同じ穴」は結果ではなく機構で判定する。**群にした根拠を昇格 PR に書く | PR #57 | `kamux-promotion/SKILL.md`「5. 同じ穴の判定」 | 昇格 PR に**群ごとの「どの機構が同じか」が書かれている**。レビューで寄せ方を検算できる |
| **昇格 PR の本文と台帳の語彙は、最後のコミットの後に取り直す**（本文・タイトル・群の表・語彙表） | PR #57 | `kamux-promotion/SKILL.md`「8. 昇格 PR の手続き」 | マージ直前の PR 本文の件数・群数・状態が、**その時点の台帳を数えた値と一致**する |
| 裁定と実施記録は**リポジトリに残る形**で渡す。予告だけの工程は未実施として扱う | PR #57 | `SKILL.md`「裁定と実施記録は…」/ `lane-controller.md`「後で実施すると書いたら…」 | ledger に**予告だけで結果の無い行が 0 件**。task-reviewer が裁定を grep で確認できる |
> **PR #69 の群は A〜R の 21 群**（`-2` / `-1` の枝を含む）。**PR #57 の群 A〜I とは別系列である。** 対応は `昇格` 列の `PR #<n> / 群 <X>` で引くこと。
> **枝番（`J-1` / `J-2` / `Q-2` / `E-2` / `O`）は「機構が違うので分けたが、手当てを既に書いたので閉じた 1 回」である。** **どの行にも `例外` の語が入っている。前例として使わないこと**（判定はゲート §6 の閾値表）。

| **群 A**（**2 つの下位機構。共通の根は「完了を意味しない信号を完了として扱った」**）<br>**A-1 信号の意味**: マーカ・コミット・clean は「作業の完了」を示すが「エージェントの停止」を示さない。**権威は完了通知**（#51 / #64 / #92 / #93 / #65 / #88）。**#65 は A-1 のうち破壊的操作の側**（稼働中のツリーへ `git restore`）、**#88 は標本化の側**（振動する信号を 1 回読んだ）<br>**A-2 語彙**: 中継が実装完了とレビュー通過を同じ言葉で書いた（#105）。**#45 は A-1 の帰結**であり、書いた側の機構は群 F | PR #69 | `lane-controller.md`「対処 1 の続き」(1)(2)(3) / `SKILL.md`「中継の言葉は…」 | **不可逆な工程（PR 作成 / 次の implementer / 差分の受け渡し / マージ）の直前に、完了通知の中継を引用した行があること。** **中継に「進んでよい」が実装完了へ付いていないこと** |
| **群 B** 破壊的操作は事後の検査では守れない。**陽性の観測点は操作の前にしか置けない**（#29 / #43 / #104 / #44） | PR #69 | `web-implementer.md` / `rust-implementer.md` / `task-reviewer.md` の「順序」節 | **変異検証を含む報告に「変異を打つ前に作業ツリーが clean だった」の 1 行があること。** その行が無い報告は、消失していても分からない |
| **群 C** 変異が**実際には測られていない**のに、測ったのと同じ出力になる（適用されていない / 古い成果物を叩いた / 型検査が走っていない。#39 / #70 / #76 / #90）<br>**🔴 観測点は PR #75 で書き直した**（旧「`git diff` 非空」は #138 / #136 / #141 / #151 / #137 に破られた。**2 箇所同時なら片方が無言で不適用でも非空になる**） | PR #69 → **#75（書き直し）** → **#78（書き直し）** | `rust-implementer.md`「打てていない変異は…」/ **`web-implementer.md`「変異が当たったことの確認」** / **`task-reviewer.md` の変異手順ブロック** → **#78（書き直し）** | **変異報告に「変異した行の `-`/`+` を目視した」の 1 行が現れる。アンカー文字列の一意性（`grep -c` = 1）が確認されている。****複数行の変異が「`perl` で書けない」を理由に落ちていない**（`--no-ext-diff` そのものの観測点は群 T が持つ） |
| **群 O**（例外・1 回） 読めない結果（ハング）を**測定不能として捨てた**。`timeout` の exit 124 が赤である（#57） | PR #69 | 同上 | **「ハングするので打たなかった」と書かれた変異が、`timeout` つきで打ち直された記録があること** |
| **群 N** 変異の赤 / 緑を**件数**で読んだ。正しくは失敗テスト名（#41 / #71） | PR #69 | `rust-implementer.md`（同節） | 変異報告の赤に**失敗テスト名が書かれている** |
| **群 D** 修正ラウンドの差分が、**元の指摘が閉じたか**の焦点の外側に落ちる（#59。**行の中に 4 件。4 回目は安全性論証の全前提（`kill(-pid,…)` の符号）で、符号を落としても 424 全緑だった**） | PR #69 | `task-reviewer.md`「修正ラウンドの差分は…」 | **修正ラウンドのレビュー報告に「修正が持ち込んだ挙動」への変異が 1 件以上**あること |
| **群 S** 純関数へ切り出すと、**呼ぶ側と継ぎ目**が無検査のまま残る（#17 の 2 件 / #46。**#17 は `kamux-promotion/SKILL.md`「5. 同じ穴の判定」が「機構で判定する」の正典の例として名指ししている行であり、2 回に達したまま 2 ステージ放置されていた**） | PR #69 | `task-reviewer.md`「純関数へ切り出すと…」/ `rust-implementer.md` / `web-implementer.md`「純関数へ切り出したら…」 | **純関数を新設・切り出しした差分のレビュー報告に、呼び出し側を壊す変異が 1 件以上**あること |
| **群 E** 「閉じる / 守られている / 効く」と書いた側が実測していない（#47 / #52 / #74 / #75）<br>**🔴 PR #75 で書き直した** —— **義務は役割ではなく**行為**に付く。射程が「レビュアーが書く処方と報告」だけだったため、implementer が production の `doc` / コメント / テスト名へ書く断定が誰の射程にも入らず 10 回再発した**（#116 / #133 / #145 / #146 / #154 / #157 / #158 / #160 / #167 / #168 / **#170**。**11 行**） | PR #69 → **#75（書き直し）** | `task-reviewer.md`「…実測していない」+ 同節の書き直しブロック / **`rust-implementer.md`「断定は、宛先を問わず…」** / **`web-implementer.md`「断定は、宛先を問わず…」** / **`lane-controller.md`「断定は、宛先を問わず、偽になる観測を 1 つ示してから書く」** / **`contract-owner.md`「断定は、それを偽にする入力を 1 つ探してから書く」** | レビューの処方が**渡す前に自分で当てられている**。「後続で担保される」に**未検証の予測**の明記があること。**加えて変異検証の表に `doc` / テスト名 / コメントの断定を宛先とする変異が 1 件以上現れる** —— **新しい断定が増えた差分で 0 件なら測っていない**。<br>**🟢 効いた実測（台帳 #204、2026-08-14）: 本規則が、契約への誤った着地を実際に防いだ 1 件目。** 断定できないものを「未検証の予測」と明記したことで、contract-owner が測り直して裁定が変わった。**本表で `signal` が鳴った記録の 1 件目である**。<br>**🔴 PR #78 で 3 度目の書き直し（#81 / #182 / #202 / #207 / #208 / #209。6 行）** —— 射程が否定形の断定に留まり、**肯定形**（「〜が在る」「〜は追跡下である」。#209 は `git ls-files docs/superpowers` = 0 行の 1 コマンドで割れた）、**dispatch がテストの被覆について書く断定**（#207 / #208）、**中継の断定**（#182 / #202）、**テスト名の主張**（#81）が射程外だった |
| **群 E-2**（例外・1 回） 値の妥当性を**測る手順が定義に無い**（固定 `sleep`。#95。**行の文言が「承認されうる形だった」で、実現した事象ではない**） | PR #69 | `task-reviewer.md`（同節末） | **固定 `sleep` を入れた手当てに、猶予を下げてしきい値を探るスイープの記録があること** |
| **群 F** **言明の一部だけを測り、残りを前提として扱った**（理由 / 観測経路 / すぐ確かめられる証拠 / 原因・分類 / 比較の片側 / **記録した実測の鮮度**。#82 / #100 / #102 / #103 / #108 / #109）<br>**🔴 PR #75 で書き直した** —— **7 件目（#171）は「結論が複数箇所で同じだったので理由も同じだと読み、1 箇所しか読まなかった」形。手当ては書いた側（`SKILL.md`）と誤りの発生側（`contract-owner.md`）の両方に置いた** | PR #69 → **#75（書き直し）** | `SKILL.md`「言明の一部だけを測って…」 | **他者の報告を追認した中継に「理由は未確認」または実測の出所**があること。再発防止の記述に**実測か推定かの明記**があること。**申し送りの実測に sha か日付が併記されていること**。**🔴「N 箇所が同じ誤りを持つ」と書いた便に、N 箇所を 1 つずつ引いた逐語があること** |
| **群 G** 一覧・索引は、**書いた時刻より後**と、**載っていない外側**を構造的に落とす（#53 / #79 / #97 / #101 / #13） | PR #69 → **#78（書き直し）** | `kamux-promotion/SKILL.md`「4. 集計 —— 母数を先に取る」 / `lane-controller.md`「RULINGS に…dispatch へ転記」 → **#78（書き直し）** | 転記の直前に**「この一覧の後に起きた事象」を問うた記録**があること。**RULINGS の逐語項目が dispatch プロンプトに現れる**（要約されていない） |
| **群 H** brief / 計画のコード片が、内容と一緒に**欠陥と置き場所**を持ち込む（#55 / #66 / #86 / #89 / #98） | PR #69 → **#78（書き直し）** | `lane-controller.md`「brief のコード片は…」 → **#78（書き直し）** | dispatch 前に**brief のコード片が呼ぶ関数名を grep で当てた記録**があること。brief 由来の欠陥が**同じ PR で直っている** |
| **群 I** 出力を解析する検査は、**その解析が曖昧になる入力**に当てて検算する（#83 / #84 / #99 / #106。**契約 §109 の検査 2 を入れて 5 例。道具は 5 種すべて別で、機構だけが同じ**）<br>**🔴 PR #75 で 2 例追記**（#172 markdown の機械検査が code span を認識しない / #173 引用解決の検査が片方向だった）。**#172 は「2 系統の独立検査が同じ盲点を持ち、互いを検算できなかった」形である** | PR #69 → **#75（追記）** → **#78（書き直し）** | `lane-controller.md`「【一般則】出力を解析する検査は…」/ `kamux-promotion/SKILL.md`「4. 集計 —— 母数を先に取る」 + **`kamux-lane/SKILL.md` §6 の引用解決検査**/ `contract-owner.md` のドリフト検査ブロック（**「空」の 2 つの意味**） → **#78（書き直し）** | **検査を直したコミットに、罠を持つ入力での検算結果が併記**されていること。**「0 行 = 合格」と読める型の検査は、0 行を不合格と明文化しているか**。**🔴 markdown を機械検査するなら code span を落としてから測っていること。「2 者が独立に測って一致した」と書くとき、2 者が別の道具を使っていること** |
| **群 J-1**（例外・1 回） ledger の完了行の**書式が割れて探せない**（#96） | PR #69 | `lane-controller.md`「完了行の書式は…」【規則 1】 | `grep -c '^Task .*: complete'` の件数が**完了タスク数と一致**する |
| **群 J-2**（例外・1 回） ledger の完了行**そのものが失われた**（セッション中断。#85。**#96 とは原因が違う**） | PR #69 | 同【規則 2】 | レーン再開の最初の応答に**最終完了行と最終コミットの対応確認**があること |
| **群 K** 規則を**片側にだけ**置くと、もう片側は同じ誤りを書き続ける（打つ側の Rust に規則が無い #38 / #61 / #63、**指定する側に「陽性の対照を測る変異」が無い #94**）<br>**🔴 PR #75 で書き直した** —— **stage5 で 7 行に再発した**（#137 / #154 / #157 / #158 / #160 / #161 / #168）。**昇格 PR の必須項目に「配置先の全ロール」を足した**（`kamux-promotion/SKILL.md`「7. 昇格 PR に必ず書く 3 つ」の 3 つ目）。**証拠の行は各 primary 群の下で数えており二重計上していない** | PR #69 → **#75（書き直し）** | `rust-implementer.md`「変異検証 —— あなたが打つ」/ `lane-controller.md`「変異を指定するときは…」 | **`rust-implementer` の完了報告に変異検証の節がある**（昇格前は 0 件）。**レビューへ渡す変異の指定が「心配している箇所」と「陽性の対照」の 2 種に分かれている**。**🔴 昇格 PR の本文に「置いたロール」と「置かなかったロールとその理由」が書かれている** |
| **群 L** 人間ゲートの 3 検査はすべて**出口**を見ており、**入口**を誰も見ていない（#32 / #78 / #80。#77 の `SKILL.md` 側もここで閉じた） | PR #69 | `SKILL.md`「渡す前に 4 つを行う」検査 4 / 「規則を当てる工程」/ 「測定を指示するときは…」 | 各項目に**「どの変数／状態を、どの経路で変えるか」の 1 行**があること。**測定を指示する行に修飾語（どのスレッドから / どのハンドルに対して）が書かれていること。平均値を根拠にした判断が無いこと** |
| **群 M** `.claude/` の定義ファイル変更もレビューを通す（#2 / #34） | PR #69 | `SKILL.md` §7 | 定義ファイルを触った PR に**`task-reviewer` の差分レビュー記録**があること |
| **群 P** 診断プローブそれ自体が判別力を持つかを、仕掛ける前に確かめる（#48 / #60） | PR #69 | `task-reviewer.md` / `rust-implementer.md`「診断プローブそれ自体が…」 | プローブの記述に**「仮説が偽ならこのプローブは何を出すか」の 1 行**があること |
| **群 Q** 報告に**測定の来歴**（どのツリーで / いつ書いたか）が無く、事後に切り分けられない（#58 / #67） | PR #69 → **#78（書き直し）** | `task-reviewer.md` 報告の形 / `rust-implementer.md` / `web-implementer.md`「報告に必ず書く」 → **#78（書き直し）** | 変異報告に**測定時 HEAD** と `REPORT_WRITTEN_AT:` があること |
| **群 Q-2**（例外・1 回） **実行していない検証の出力**を報告に書いた（#56。**`REPORT_WRITTEN_AT` は本件に効かない** —— 書いた時点で既に偽だった） | PR #69 | 同上（3 ファイルの「実行していない検証の出力を書かない」） | 報告の検証欄が、**実際に走らせた出力か「未実施」のどちらか**になっていること |
| **群 R** 手当ては**受け手の実物**で書く。ツール名で書かない（#42 / #73） | PR #69 | `task-reviewer.md`「手当ては、受け手の実物で書く」 | 渡した手順が、**受け手の `tools` 行と突き合わせて**書かれていること（ツール名ではなく満たすべき性質） |
| **2 タスク進んだら引き継いで終了する**（起動時の ledger 最終完了行を起点に数える。降りる条件は「タスク境界」ではなく `git status --short` が空であること） | PR #71 | `lane-controller.md`「2 タスク進んだら引き継いで終了する」 | **タスクあたり文脈総和が 23.72M を下回る**（1 レーン = 1 PR で走った M2-2 PR 16 分の実測値。`agent-*.jsonl` の `usage` を `message.id` で重複除去して合算し、完了タスク数で割る）。併せて ledger の `grep -c '^Handoff:'` で引き継ぎ回数、同じ行の `マージ許可` の有無で受け渡しを確認する（どちらも遵守の確認であって効果ではない） |
| **群 T** `diff.external=difft` により、差分を読む／機械解析するあらゆる手段が静かに 0 を返す（#127 / #131 / #152 / #159。**この設定は 6 本の定義ファイルのどこにも書かれていなかった** —— `grep -n 'diff.external\|no-ext-diff\|difft'` = 0 件） | PR #75 | `task-reviewer.md`「差分を機械解析するなら `--no-ext-diff`…」/ `lane-controller.md` 同名節 / `rust-implementer.md`「打てていない変異は…」の確認表（`--no-ext-diff` の行）/ `web-implementer.md`「変異が当たったことの確認」 | **差分を解析するコマンドに `--no-ext-diff` が付いている。** `^diff --git` が 0 行のとき「差分なし」ではなく**不合格**として扱った記録が在る |
| **群 V** dispatch と定義ファイルが正面から食い違い、優先順位がどちらの定義からも導けない（#135 / #139 / #153 / #163。**同じ食い違いが 2 回、10 本のレビューのうち 1 本だけ結果が割れた**）<br>**🔴 ラベルは `群 V`。`群 O` は #57（ハング／`timeout` exit 124、PR #69）が既に使っている** | PR #75 → **#78（書き直し）** | `task-reviewer.md` / `rust-implementer.md` / `web-implementer.md`「dispatch と本ファイルが食い違ったら、本ファイルが勝つ」（**4 形の閾値表つき**）/ `lane-controller.md`「dispatch は定義ファイルと食い違ってはならない」 → **#78（書き直し）** | **食い違いが BLOCKED として上がっていること**（`定義の穴` 欄の有無は問わない —— **タスク単位レビューはその欄を出さないので、そこを観測点にすると構造的に測れない**）。**同じ工程で結果が割れている形跡が 0 件** |
| **群 U** 規則・指摘を書いた本人が、同じ文書・同じ工程でその規則を破る（#124 / #125 / #128 / #142 / #144。**規則を書く行為が「もう効いている」という感覚を作る**）<br>**🔴 #164（変異一覧の書式が「必ず打つ分」と「材料」を区別しない）も PR #75 で閉じた。群は別だが棚卸しの入口を本行が持つ** —— 所在は `lane-controller.md`「必ず打つ: N 件」の件数宣言、観測点は**dispatch の変異一覧に件数宣言が在り、レビュー報告の件数と突き合わせられること**<br>**🔴 PR #78 で書き直した（#196 / #201。5 ファイル全部に在るのに同日に両側で再発した）** —— **手当ては「気をつける」ではなくコマンドの形にする。併せて `contract-owner.md` へ新規に置いた（それまで 0 行）** | PR #75 → **#78（書き直し）** | `lane-controller.md`「規則を書いた便には、その規則を自分で当てる」/ `task-reviewer.md`「規則・指摘を書いた便には…」/ `rust-implementer.md`「規則・処方を書いた便には…」/ `web-implementer.md`「規則・コメントを書いた便には…」/ `SKILL.md`「規則を書いた便には、その規則をその便自身へ当てる」/ **`contract-owner.md`「規則を書いた便には、その規則をその便自身へ当てる」** | **規則を書いた便に「その規則を自分の出力へ当てた結果」が 1 行ある**（「読んだ」ではなく「当てた」）。**渡したコマンドを 1 回実行した記録が在る**。**🔴「注意する」「気をつける」で終わっている手当てが 0 件である** |
| **群 α** 自分が設計した観測（変異 / 陽性の対照 / スモークの観測欄）の**判別力を、使う前に測らない**（#91 / #129 / #130 / #161 / #184 / #193 / #206 / **#230**。**8 行**）<br>**🔴 #230 で宛先を広げて書き直した（2026-08-15。PR #88）** —— **宛先を「変異 / 陽性の対照 / スモークの観測欄」と書いていたため、`Monitor` / 待ちループの**待ち条件**が射程外に落ちた。「自分で設計した観測」であれば、検証のためか起床のためかは関係ない**<br>**群 P（診断プローブ）と同じ工程が欠けているが、宛先が違ったため 7 行が射程外に落ちた。`lane-controller.md` の群 K は「対照を入れたか」しか問わず「対照が正しい位置に在るか」を問わない** | PR #78 / **PR #88（宛先の書き直し）** | `lane-controller.md` 群 K 節の書き直し / `task-reviewer.md` 群 P 節の拡張 / `rust-implementer.md` 群 P 節の拡張 / `web-implementer.md`（新規） / `SKILL.md` 人間ゲートの**検査 5** / **`SKILL.md` 検査 5 の「待ち条件・起床条件にも当てる」+ `lane-controller.md`「対処 1 の続き」(3) の判定基準（PR #88）** | **変異報告の各変異に「この変異が緑になる条件」の 1 行がある**。dispatch の変異一覧に**production 編集 / テスト編集の別**が書かれている。**手動スモークの各項目に「この観測は、測りたい機構が死んでいても出るか」への答えが 1 行ある**。**🔴 張った `Monitor` / 待ちループが、`lane-controller.md`「対処 1 の続き」(3) の 3 条件を満たす**（系統が 2 つ以上 —— **同一アーティファクトへの複数条件は 1 系統と数える** / うち 1 系統は成果物とは別のアーティファクト由来の生存の陽性証拠 / 終端状態を成功と失敗の両方で含む） |
| **群 β** **借用したコマンド出力・既存の指標が 2 状態を区別しない**（#28 / #40 / #54 / #113 / #114 / #115 / #118 / #169 / #180 / #181 / #191。**11 行**）<br>**群 α と機構が違う** —— あちらは自分で設計した観測、こちらは**借りてきた出力**。手当ても設計時ではなく**使用時**に「不合格の形」を書く | PR #78 | `lane-controller.md` / `task-reviewer.md` / `SKILL.md`「証拠に借りたコマンドの出力が、2 状態を区別するか」/ **`rust-implementer.md` / `web-implementer.md`「検証コマンドをパイプしない」**（**節名が 2 通りある。役割ごとに主症状が違うため。`群 β` のラベルで引くと 5 本とも当たる**） | **証拠に使った各コマンドについて「不合格ならこう見える」の 1 行が在る**。**検証コマンドが `tail` にパイプされておらず、各行に `exit <n>` が書かれている**。`cargo test --workspace` に `--no-fail-fast` が付いている |
| **群 γ** 処方・訂正・値の差し替えが**名指しした範囲ちょうどで閉じ、同じ族の残りが誰の射程にも入らない**（#1 / #3 / #69 / #72 / #117 / #132 / #140 / #143 / #155 / #156。**10 行**） | PR #78 | `task-reviewer.md` / `lane-controller.md` / `rust-implementer.md` / `web-implementer.md` / `contract-owner.md` の各「族の境界」節 | 処方・訂正に**「この処方が閉じる族」「同型の残り」の 1 行**が在る。**「1 語だけ」「N 行だけ」と書いた便に、その語での `grep -c` の出力が併記されている** |
| **群 δ** **検査と是正の順序が保証されていない** —— 検査の時点で、結果を反映できる状態が既に失われている（#16 / #176 / #211） | PR #78 | `lane-controller.md`「フェーズ完了時」の順序 / `SKILL.md`「検査と是正の順序」 | マージ許可の記録に **`git diff --no-ext-diff --stat <承認 sha>..HEAD` の出力**が引かれている。**`lane-controller.md` で全ブランチレビューが `gh pr merge` より前の番号に在る** |
| **群 ε** **同じ作業に実行主体が二重に就く** —— 割り当てる側が、既に就いている実体を測らずに新しい実体を立てる（#7 / #185 / #205） | PR #78 | `SKILL.md`「同じ作業に実体は 1 体だけ」/ `lane-controller.md` 同名節 | 起動指示に**先行実体の停止を確かめた出力**が 1 行ある。**引き継ぎ後に前任へ送った `SendMessage` が 0 件**。「未適用」として渡した箇所に、渡す直前の実測が併記されている |
| **群 ζ** **裁定が適用者へ届く機構が無い** —— 着地する場所（契約ファイル）と適用者が見る場所（`SendMessage` / ledger）が別で、繋ぐ工程がどの定義にも無い（#147 / #165 / #203） | PR #78 | `lane-controller.md`「裁定は `SendMessage` を待たず、契約ファイルの末尾を**自分で**読む」/ `contract-owner.md`「裁定には適用者を名指しし、届く経路を書く」/ `SKILL.md`「裁定は `SendMessage` を待たず、契約ファイルの末尾を読む」（中継の側）（**節名が 3 通りある。`群 ζ` のラベルで引くと 3 本とも当たる**） | 契約の各裁定節に**「適用者: 〜」の 1 行**が在る。`grep -c '§[0-9]*' <ledger>` が、そのフェーズで着地した章の数以上を返す。**裁定を中継した便に章番号が書かれている** |
| **群 η** レビュアー・中継者の**「留保」や規則の不在が、数える対象そのものを減らす**（#11 / #134 / #166。**#134 が自ら「4 例目」と記録している**） | PR #78 | `task-reviewer.md` / `lane-controller.md` の各「一覧に載せないと判断したものは、件数と理由を書く」 | `grep -n '対象外\|射程外\|なし' <レビュー報告>` の各行に**件数が併記されている**。**ledger の deferred minor 件数と PR 単位レビューの Minor 件数の和が一致する** |
| **群 θ** **稼働中の他者が使っている状態へ書く**（#112 / #179。**どちらも `git status --porcelain` が構造的にこの状態を見ない**） | PR #78 | `SKILL.md`「稼働中の他者が使っている状態へ書かない」/ `task-reviewer.md` 変異前確認の逆向き / `lane-controller.md` 同名節 | team-lead が `git commit` を打つ記録に**直前の `git -C <絶対パス> rev-parse --abbrev-ref HEAD` の出力**が併記されている。成果物を上書きする変異の報告に `lsof` か `pgrep` の出力が 1 行ある |
| **群 ι** **工程の産物の書き先・書き手・時点が定義に無い** —— 工程は定義されているが、産物が予告のまま消えるか誰も判定しない（#33 / #162 / #188） | PR #78 | `SKILL.md`「工程を定義したら、産物の書き先・書き手・時点を同じ場所に書く」/ `lane-controller.md` 同名節 | `grep -c '後で実施\|次の PR で\|別作業' <ledger>` の各行に、**同じファイル内の結果の行が対で存在する**。**ゲートの実施記録が `progress.md`「## 裁定」の §<n> として毎回同じ形で在る** |
| **群 κ** **参照先の本文を照合せずに転記する**（#149 / #150。**同一タスクの連続する 2 ラウンドで、rev1 の手当てが rev2 の穴を作った**） | PR #78 | `rust-implementer.md` / `web-implementer.md` / `task-reviewer.md` / `lane-controller.md` / `contract-owner.md` の各「参照は本文照合してから書く」 | 参照を書いた差分の報告に **`grep -n '<引いた逐語>' 00-contracts.md` の出力が 1 参照につき 1 行**ある。**参照の置換を「旧文字列 0 件」だけで確認した記録が 0 件** |
| **群 λ** **原本が実在する値・規則を手で写し、複製側が原本の変化に追随しない**（#4 / #110。**#110 は 4 検査が 4 つとも合格し、壊れたのは手順の「転送」だけだった**） | PR #78 | `SKILL.md`「原本が在る値を手で写さない」/ `lane-controller.md` 同名節 / `contract-owner.md` 同名節 | **人間ゲートの手順に、値を手で書いた行が 0 行**（実コードを実行して出力させる形）。写した値の行に `<ファイル>:<行>` が併記されている |
| **群 μ** **手で作った列挙・件数を、機械導出と突き合わせない**（#197 / #199 / #200。**#200 は行数検算が 2 回とも合格を返し、捕まえたのは列数検算だけだった**） | PR #78 | `contract-owner.md` / `SKILL.md` / `lane-controller.md` の各「列挙・件数は機械で導出して突き合わせる」 | 列挙を含む節に**導出コマンドの出力**が併記されている。**「N 件」と書いた見出しの直後の表について `grep -cE '^\| '` が N と一致する**。台帳へ行を足した便に列数の分布が在る |
| **群 ν** **一般則から自分を除外する論証をし、その論証が誤っていた**（#68。**行内 3 回。§98.10 が逐語で「§97.9 の理由は本件に転移しない」と書いていた**）<br>**規則の不在ではなく規則の無効化なので、既存のどの群の網にも掛からない。前例は #59 = 群 D（行内 4 回）と #48 = 群 P（3 回）** | PR #78 | `contract-owner.md` / `SKILL.md` の各「この規則は本件には当たらない」と書くとき | `grep -n '転移しない\|適用外\|本件には当たらない'` の各行の**直後 3 行以内に、台帳行番号か章番号の引用がある** |
| **群 ξ**（例外・1 回） **不在（無応答・停止）を検知する規則を、受動的な通知待ちのまま置いた** —— 検出すべき事象が「通知が来なくなること」なので、通知待ちで構えている限り構造的に発火しない（#227）<br>**⚠️ #230（張った機構の観測対象が 1 点だった）は本群ではなく 群 α である。** **機構を張ったか**と**張った機構が 2 状態を区別するか**は別の軸で、**本群は前者しか見ない**<br>**群 A とは機構が違う** —— あちらは**起床イベントが届く**ことを前提にその後の一手を定める。**機構自体は `lane-controller.md`「対処 1」に既に在り、team-lead 側だけが持っていなかった**（無名のまま片側にしか無かった機構に名前を付けた） | PR #88 | `SKILL.md`「事象が起きないことを検知する規則は、能動的な起床機構を同時に置く」/ `lane-controller.md`「対処 1」末尾の一般則 | **レーン・`Agent` を起動した便、および「待つ」「様子を見る」と判断した便に、`Monitor` の登録結果（`task-id`）か `Bash`（`run_in_background: true`）の待ちループの投入が 1 行あること。****見るのは語の存在ではなく登録が成立した出力**で、「監視する」と書いただけの便は不合格。**張った機構の判別力は 群 α の側で測る**（本群の観測点は機構の実在までしか見ない） |

**規則が増え続けると読まれなくなる。ただし「一定期間発火しなかったから消す」という自動の引き金は置かない** —— **規則が発火したことを観測する主体が、この鎖のどこにも居ないからである。** 観測されない指標を列に持つと、全ての規則が「発火なし」に見えて削除候補に落ち続ける。

> **棚卸しは、定義ファイルが読みにくくなったと感じたときに手で行う。**
> **消す前に、その規則を外したら何が起きるかを 1 行書く。書けないなら残す。**

**発火しないのは、予防できているからかもしれない。** 守っているものを壊してみて初めて守っていたかが分かる —— 変異検証と同じ思想を規則自身に適用する。
