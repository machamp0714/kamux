# エージェント定義の学び（AGENT-LESSONS）

**このファイルは「事象」を溜める場所であって、規則を書く場所ではない。** 規則の形にするのは昇格時である。

設計は `docs/superpowers/specs/2026-08-05-agent-self-improvement-design.md`（**git 管理外**。設計書は git で管理しない方針なので、clone しただけの環境には無い）。

## 正典の分界

> **作るものの話（仕様・型・命名・順序・値）は契約** `docs/superpowers/plans/2026-08-01-kamux/00-contracts.md`。
> **作り方の話（誰がいつ何を確認するか）が本ファイルと `.claude/agents/*.md` / `.claude/skills/kamux-lane/SKILL.md`。**

**契約に書けば防げたものを本ファイルに書かない。** それは契約変更要求であり、contract-owner の専管である。

## 書き方

| 規則 | 内容 |
|---|---|
| **事象で書く** | 「◯◯すべき」ではなく「◯◯が起きた」。規則の形で溜めると微妙に違う規則が並び、**2 件目が同じ穴かどうかを判定できなくなる** |
| **1 行に収める** | **表のセルは改行を含められない。** 改行した瞬間に表が壊れる |
| **0 件なら何も書かない** | 「特になし」の行を残さない。**0 件は正常な結果である。** 残すとファイルが「なし」で埋まり、実在の事象が埋もれる |
| **棄却も行として残す** | 一度考えて捨てた判断を消すと、**同じ議論が再燃する**（契約が全章に却下記録を持つのと同じ理由） |

## 入力源

| 源 | `出典` 列に書く値 | 誰が書くか | どこへコミットするか |
|---|---|---|---|
| PR レビューの `定義の穴:` 欄 | `PR #<n>` | lane-controller | **その PR のブランチ。** 時点は `lane-controller.md` が持つ |
| **ユーザーからの指摘** | `user` | team-lead | **ブランチを切って PR にする。** ローカル `main` に直接コミットしない（契約 §43.3） |

**ゲートでの棄却・昇格の記入も PR 経由で行う。** 未コミットのまま残すと、次にこのリポジトリで走るレーンの検証 A（`git status --porcelain` が空）に他人が引っかかる。

## 昇格

**判定の手順・閾値・昇格 PR の要件は `.claude/skills/kamux-lane/SKILL.md` の「ステージ完了ゲート」が持つ。本ファイルには書き写さない** —— 書き写した瞬間に二重の正典になり、片方だけ直せば静かに乖離する。

**本ファイルが持つのは `昇格` 列の語彙だけである。**

| 値 | 意味 | 誰が・いつ書くか |
|---|---|---|
| `—` | 未昇格。ゲートが数える対象 | 転記した時点の初期値 |
| `昇格済み（PR #<n>）` | 定義ファイルへ反映済み | **昇格 PR 自身がこの列を書き換える**（同じ PR に含める） |
| `棄却（<理由>）` | 昇格しないと判断した。**行は消さない** | ゲートで判断した team-lead |

> **`昇格済み` はマージ後に書くのでは遅い。** マージコミットの sha はマージするまで存在しないので、それを待つと**行は `—` のまま残り、次のステージのゲートが同じ行を数えて同じ昇格 PR をもう一度作る。** だから PR 番号を使い、**昇格 PR の中で書き換える。**

## 事象

| # | 日付 | ステージ | 出典 | 事象 | 穴のあった定義 | 昇格 |
|---|---|---|---|---|---|---|
| 1 | 2026-08-06 | — | PR #54 | 計画の逐語ブロックを適用するとき、置換行の前後を読まずにコミットし、同一節内に矛盾が残った | `rust-implementer.md` / `web-implementer.md` | — |
| 2 | 2026-08-06 | — | PR #54 | `.claude/` の定義ファイル変更を team-lead がインラインで実装し、implementer もタスク単位レビューも経由しなかった（`SKILL.md` §7 の禁止は「コードを直す」としか書かれておらず、定義ファイルが該当するか読み取れない） | `SKILL.md` | — |
| 3 | 2026-08-06 | — | PR #54 | レビュー指摘の文言だけを直して記述位置（runbook の実行順）を直さず、上から実行する読み手には同じ失敗経路が残った | `lane-controller.md` | — |
| 4 | 2026-08-06 | — | PR #54 | 二重の正典を 1 ファイルから削った同じコミットで、同じ閾値が別ファイルに新設された | `SKILL.md` / `CLAUDE.md` | — |
| 5 | 2026-08-06 | stage3 | PR #55 | rust-implementer が報告に貼ったドリフト検査 10 の grep 逐語が、コミット済みの最終コードで実行した結果と一致しなかった（実装途中の結果を貼っており、最終形では 2 ファイルで不合格だった） | `rust-implementer.md` | — |
| 6 | 2026-08-06 | stage3 | PR #55 | lane-controller が implementer の報告の逐語を検証せずに team-lead へ転記し、ドリフト検査の不合格を緑として報告した（検査を自分では 1 度も実行していなかった） | `lane-controller.md` | — |
| 7 | 2026-08-06 | stage3 | PR #55 | team-lead が繰り越し項目の受け手を名指ししたが、その項目に既に着手していた実行中の別エージェント（contract-owner）が居るかを確認しておらず、受け手が 2 人になりうる状態を作った | `SKILL.md` | — |
| 8 | 2026-08-06 | stage3 | PR #55 | 監視ループの完了通知は届いていたが、レーンが次の一手を打たずに 82 分停止した（`lane-controller.md` の起床手段の節は機構を張ることは求めているが、起床後に次の一手を打つことは求めていない） | `lane-controller.md` | — |
| 9 | 2026-08-06 | stage3 | PR #55 | 前回の停止の直後、レーンが自ら再発防止を宣言した次の区切り（Task 4 クローズ後）で同じ停止が再発した。宣言では閉じない性質の穴である | `lane-controller.md` | — |
| 10 | 2026-08-06 | stage3 | PR #55 | タスク境界（実装完了 → レビュー dispatch）で 3 度目の停止。3 回とも同じ遷移点であり、レーンが再発防止を宣言した後も同じ場所で起きた | `lane-controller.md` | — |
| 11 | 2026-08-06 | stage3 | PR #55 | task-reviewer が契約 §77.3 を「`keyRouting.test.ts` への追加も禁止」と読み、是正可能な指摘を「修正禁止・記録のみ」に分類した（同節が禁じているのは `registry.ts` への変更範囲と `registry.test.ts` の既存テスト削除のみで、`keyRouting.test.ts` への追加は裁量として明示的に許されている） | `task-reviewer.md` | — |
| 12 | 2026-08-06 | stage3 | PR #55 | team-lead が `SendMessage` で渡した裁定がリポジトリ上に残らず、task-reviewer が `TEAM-LEAD-RULINGS.md` を grep しても確認できないため「承認条件が満たされていない可能性」として検証不能扱いになった | `SKILL.md` | — |
| 13 | 2026-08-06 | stage3 | PR #55 | lane-controller が team-lead の裁定を報告へ要約した際に意味が反転した（「契約の表に無い 4 つ目の変更」→「契約の射程内」）。反転後の記述はドリフト検査を素通りさせるため、申告を課した目的そのものが消える | `lane-controller.md` | — |
| 14 | 2026-08-06 | stage3 | PR #55 | フェーズを通じて `npm run lint` / `npm run fmt:check` が一度も実行・報告されず、team-lead が PR 作成直前に初めて走らせた（レーンの報告は vitest / tsc / e2e / cargo のみだった） | `lane-controller.md` | — |
| 15 | 2026-08-06 | stage3 | PR #55 | PR 本文が「すべて最終コミットに対して実行」と宣言しながら、`vitest`（392 / 実測 397）と `cargo test`（361 / 実測 363）の 2 つが数コミット前の値だった。PR 本文はマージ後も残る恒久的な記録であり、報告や ledger と違って訂正の機会が乏しい | `lane-controller.md` | — |
| 16 | 2026-08-06 | stage3 | PR #55 | 全差分レビューのパッケージ生成後に team-lead の指摘でブランチへコミットが積まれ、レビュー対象と実物がずれた（レビュアーが新 tip で再検証して吸収したが、そうする取り決めは無かった） | `task-reviewer.md` / `SKILL.md` | — |
| 17 | 2026-08-06 | stage3 | PR #56 | 純粋関数は変異検証で守られていたが、それを呼ぶ側（Rust の `resolve_cwd` 配線 / TS のストア action）が実際に呼んでいることを守るテストが無く、呼び出しを差し替える変異が全テスト緑を生き延びた（Task 4 / Task 6 の 2 件） | `rust-implementer.md` / `web-implementer.md` | — |
| 18 | 2026-08-06 | stage3 | PR #56 | lane-controller がレビューへ指定した変異は全部赤だった一方、レビュアーが自分で設計した変異が Important 4 件すべてを暴いた（指定分は既に心配されている箇所しか見ず、自発分が誰も気づいていない箇所を発見した） | `task-reviewer.md` / `lane-controller.md` | — |
| 19 | 2026-08-06 | stage3 | PR #56 | E2E の共有モックが実 API の呼ぶ内部フックを持たず、`unlisten` が必ず TypeError になる状態が M1-2 の導入以来 4 フェーズ分放置されていた（その経路を踏む spec が書かれるまで発現しなかった） | `SKILL.md` | — |
| 20 | 2026-08-06 | stage3 | PR #56 | レイアウトを守るはずの E2E assert が `> 0` で書かれており、実測で高さが 665 から 288 へ退化しても緑のままだった（`> 0` は「何でも通る境界」であり assert の形をした空振りだった） | `web-implementer.md` | — |
| 21 | 2026-08-06 | stage3 | PR #56 | team-lead が「クラス名を新しいものに置き換えたので assert の守りが戻る」として承認したが、置き換え後の assert も恒真であり、変異を打つまで誰も気づかなかった | `SKILL.md` | — |
| 22 | 2026-08-06 | stage3 | PR #56 | 手動スモークの項目が契約と逆の期待値を持っており（`Cmd+J`/`Cmd+K` が editor 画面で効く、と記載）、そのまま実施していれば契約どおりの正しい実装が不合格と判定されるところだった | `lane-controller.md` | — |
| 23 | 2026-08-06 | stage3 | PR #56 | 手動スモークの手順（PATH から nvim を外す）が実装のどの分岐にも届かず必ず空振りする状態だった（探査がログインシェルから PATH を組み直し、フォールバック 4 ディレクトリも無条件に追加するため） | `lane-controller.md` | — |

| 24 | 2026-08-07 | stage3 | user | 手動スモークの期待結果が「nvim が開く」までしか書かれておらず、PTY が 80×24 のまま描画領域が画面の一部しか埋めない状態でも合格として通る状態だった（ユーザーが目視で気づかなければ見逃していた） | `SKILL.md` | — |
| 25 | 2026-08-07 | stage3 | user | brief が指定した変異 3 件は全部赤だったが、レビュアーが自分で設計した変異が修正の核心行（`resizePty` の引数）の無防備を暴いた。**#18 と同じ穴の 2 回目** | `task-reviewer.md` / `lane-controller.md` | — |

**`ステージ` 列は棄却判定に要る。** 「初出からいくつステージを跨いだか」は、日付や PR 番号からは求まらない（ステージは日付でも PR 番号でも区切られない）。**レーンの外で行った作業は `—` と書く**（次のステージ完了ゲートを 1 つ目として数える）。

## 昇格済みの規則（棚卸し用）

| 規則 | 導入日 | 所在 |
|---|---|---|

（0 件）

**規則が増え続けると読まれなくなる。ただし「一定期間発火しなかったから消す」という自動の引き金は置かない** —— **規則が発火したことを観測する主体が、この鎖のどこにも居ないからである。** 観測されない指標を列に持つと、全ての規則が「発火なし」に見えて削除候補に落ち続ける。

> **棚卸しは、定義ファイルが読みにくくなったと感じたときに手で行う。**
> **消す前に、その規則を外したら何が起きるかを 1 行書く。書けないなら残す。**

**発火しないのは、予防できているからかもしれない。** 守っているものを壊してみて初めて守っていたかが分かる —— 変異検証と同じ思想を規則自身に適用する。
