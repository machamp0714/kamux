---
name: kamux-lane
description: kamux の実装フェーズ（M1-1〜M3-4）を lane-controller エージェントとして起動する。「ステージ0 を起動」「M1-1 を始めて」「stage4」「M2-2 のレーンを立てて」などで起動する。フェーズ ID かステージ番号を引数に取る。
argument-hint: "<M1-1 | M1-2 | … | M3-4 | stage0 | … | stage6>"
---

# kamux レーン起動

`docs/superpowers/plans/2026-08-01-kamux/` の実装フェーズを `lane-controller` エージェントとして起動する。**あなた（team-lead）はレーンを起動して監督するだけで、コードは書かない。**

引数の形は 2 つ。

| 引数 | 動作 |
|---|---|
| `M1-1` 〜 `M3-4` | そのフェーズを 1 本だけ起動する |
| `stage0` 〜 `stage6` | そのステージのレーンを**1 メッセージ内でまとめて**起動する（= 並列実行） |

引数が無い場合は、ledger（`.superpowers/sdd/*/progress.md`）と `git log` から現在地を推定し、次に来るステージを提案してから確認を取る。

---

## 1. 起動前チェック（毎回、必ず）

```bash
# 1. エージェント定義が認識されているか（5 種）
ls .claude/agents/

# 2. 作業ツリーが汚れていないか
git status --short

# 3. 直前のステージの成果がマージ済みか
git log --oneline -5
```

`lane-controller` が利用可能エージェント一覧に出ていない場合は**セッション再起動が必要**。フォールバックとして `subagent_type: "claude"` で起動し、`.claude/agents/lane-controller.md` の本文をプロンプト冒頭に貼る。

---

## 2. ステージの定義

依存グラフから導出済み（`TEAM.md` §1）。**並列度は最大 3。** クリティカルパスは 7 フェーズ直列なので、レーンを増やしても縮まない。

| ステージ | レーン | isolation | マージ順 | 備考 |
|---|---|---|---|---|
| stage0 | M1-1 | 不要 | — | 全フェーズをブロックする土台 |
| stage1 | M1-2 ∥ M1-3 | **両方 worktree** | **M1-2 → M1-3** | `App.tsx` / `store/index.ts` / `useKeymap.ts` / `package.json` が重衝突 |
| stage2 | M1-4 | 不要 | — | M1-1〜M1-3 すべてが前提 |
| stage3 | M2-1 ∥ M3-1 | 両方 worktree | **M2-1 → M3-1** | 衝突は `main.rs` / `App.tsx` の追記のみ。M3-1 の前提は M1-3 だけなのでここに前倒し |
| stage4 | M2-2 ∥ M2-3 ∥ M3-2 | **M2-2 / M2-3 のみ** | **M2-2 → M2-3 → M3-2** | M3-2 は純フロントで衝突ゼロ（隔離不要） |
| stage5 | M3-3 → M2-4 | 不要 | 直列 | **並列にしない。** Rust/フロント両側で 10 ファイル衝突する |
| stage6 | M3-4 | 不要 | — | M1-1〜M3-3 すべてが前提 |

**stage5 は 1 メッセージ 1 起動（直列）。** M3-3 の完了を待ってから M2-4 を起動する。

**isolation 列が「worktree」の行は、`isolation: "worktree"` ではなく `git worktree add` で手動作成する**（§5.1。ハーネスの隔離 worktree は自動削除される）。

---

## 3. フェーズ表

| ID | 計画ファイル | タスク | ブランチ | ゴール | 前提 | 主な実装者 |
|---|---|---|---|---|---|---|
| M1-1 | `M1-1-foundation.md` | 20 | `feat/m1-1-foundation` | アプリ再起動後もプロジェクトとセッションが復元される | なし | rust + web |
| M1-2 | `M1-2-kanban.md` | 17 | `feat/m1-2-kanban` | セッションをカンバン上で管理し、手動で状態を動かせる | M1-1 | web |
| M1-3 | `M1-3-pty-terminal.md` | 16 | `feat/m1-3-pty-terminal` | shell セッションを起動し、ターミナルで対話できる | M1-1 | rust + web |
| M1-4 | `M1-4-worktree-cli.md` | 12 | `feat/m1-4-worktree-cli` | カードから claude が選択した作業ツリーで起動し、該当ペインに直行できる | M1-1〜M1-3 | rust + web |
| M2-1 | `M2-1-runtime-state.md` | 10 | `feat/m2-1-runtime-state` | カード/タブを見るだけで各セッションの実行状態が分かる | M1-1〜M1-4 | rust + web |
| M2-2 | `M2-2-hooks-relay.md` | 19 | `feat/m2-2-hooks-relay` | Claude Code の入力待ち・完了・session_id を決定的に捕捉できる | M1-4, M2-1 | rust |
| M2-3 | `M2-3-macos-notification.md` | 16 | `feat/m2-3-notification` | アプリを見ていなくても要対応セッションに戻れる | M2-1 | rust + web |
| M2-4 | `M2-4-resume.md` | 12 | `feat/m2-4-resume` | アプリ終了後も同じ作業ツリーと会話に戻れる | M1-4, M2-1, M2-2 | rust + web |
| M3-1 | `M3-1-nvim-editor.md` | 8 | `feat/m3-1-nvim-editor` | フォーカス中セッションの作業ツリーを nvim で閲覧/編集できる | M1-3 | rust + web |
| M3-2 | `M3-2-split-layout.md` | 11 | `feat/m3-2-split-layout` | 2 つのエージェントを同時に監視し、片方へ即座に入力できる | M1-3 | web |
| M3-3 | `M3-3-generic-cli.md` | 19 | `feat/m3-3-generic-cli` | Claude Code 以外の CLI でも最低限の状態把握ができる | M1-3, M2-1, M2-2 | rust + web |
| M3-4 | `M3-4-ops-ux.md` | 21 | `feat/m3-4-ops-ux` | 要件1〜10 を満たし、日常運用で破綻しない | M1-1〜M3-3 | rust + web |

計画ファイルはすべて `docs/superpowers/plans/2026-08-01-kamux/` 配下。

**1 フェーズ = 複数 PR。** 区切りとブランチ名は契約 **§27.4**（全 33 本）が正典。**lane-controller が PR の作成から CI 緑・マージまで担当する**（契約 §32 の 7 条件）。あなたはマージ許可を出し、人間検証が必要な PR に対応する。

**実行順が特殊なフェーズが 2 つある。**

- **M1-1**（契約 §27.3）: Task 19（lint）→ Task 20（CI）→ Task 1 → … → Task 18。lint と CI は他の全 PR が乗る土台なので先に入れる
- **M3-4**（契約 §27.4 の例外 3）: Task 1〜14 → 16〜21 → **15**。Task 15 は要件 1〜10 の最終スモークなので、スクラッチと shim の後に置く

---

## 4. 人間ゲート（該当フェーズを起動するときに必ず伝える）

契約 **§26** により、フロント単体 E2E（Playwright + IPC モック）を採用している。基盤は **M1-2 Task 17** が作り、M1-3 以降は各フェーズが自分のスモーク項目を仕分ける（DOM で検証できるものは `e2e/*.spec.ts` へ）。

下に残るのは **§26.4 の 5 カテゴリ = エージェントには物理的に実行できない検証**だけである。**該当フェーズの dispatch に「BLOCKED で上げること」と明記する。**

| フェーズ | 内容 | 根拠 |
|---|---|---|
| M1-4 | ビルドした `.app` を **Finder からダブルクリック**して PATH 解決を確認（Task 6–11）／**shim ディレクトリが rc 通過後も PATH に残るか**（Task 12、§30.3） | 契約 §18 / §26.4-1・5 |
| M2-2 | 実 PTY の**対話モード**で `Notification` / `PermissionRequest` の payload をキャプチャ。hook の stdout がターミナル表示を汚さないか実測 | 契約 §12.4 は未確認事実と明記 |
| M2-3 | 通知の**クリック応答**を実機で確認 | 契約 §21（`notify-rust` を選んだ唯一の理由） |
| M3-2 | `Cmd+D` / `Cmd+[` / `Cmd+]` が WebView に奪われないか実機確認 | 奪われる場合は `Cmd+Shift+D` / `Cmd+Alt+←→` |
| M3-4 | 起動 1 秒未満 / セッション 5 個で 300MB 未満 / アイドル CPU の実測（Task 15）／**`Cmd+W` が macOS 既定メニューに奪われないか**（Task 20、§29.8） | 契約 §0 / §26.4-3・4 |

---

## 5. dispatch テンプレート

### 5.0 dispatch の前に、契約 §73.5 の該当フェーズの行を読む（**毎回。省略不可**）

**契約 §73.5 は「フェーズ別の着手前 worklist 索引」である。** そのフェーズが着手前に読むべき章がフェーズごとに 1 行にまとまっている。

```bash
grep -n -A22 '### 73.5' docs/superpowers/plans/2026-08-01-kamux/00-contracts.md
```

**`-A` の値は表の末尾まで届いていることを目で確認する。** 短く切ると **M3-3 / M3-4 の行が落ちる** —— 「末尾が落ちても出力は正常に見える」のがこの種の欠陥の形である（契約 §68.1）。

- **索引の行を読み、そこに挙がった章を読み、必要なものを brief と dispatch プロンプトに反映する**
- **索引は所在だけを持つ。指示の中身は各章にある**（§73.5 の規律）。**ここに表を写さない** —— 写した瞬間に二重の正典になり、片方が腐る
- **新しい章が増えるので、記憶で代用しない。** 訂正は常に契約ファイルの末尾に増える

> **なぜ手順に組み込むか: 「dispatch 時に適用する」worklist は 38 行・7 フェーズあり、2026-08-05 に 3 回連続で落とした。** 3 回とも形は「契約に書いてあったが読まれなかった」である。**索引は手数を「7 章を探す」から「1 行を見る」に減らすだけで、読む人がいなければ落ちる。**

### 5.1 呼び出しの形

`Agent` ツールを次の形で呼ぶ。**`model` は必ず明示する**（省略すると親を継承する）。

```
subagent_type: "lane-controller"
name:          "lane-<フェーズ ID 小文字>"     ← SendMessage の宛先になる
model:         "opus"
description:   "<フェーズ ID> lane"
run_in_background: true
```

**`isolation: "worktree"` を使わない。** ハーネスの隔離 worktree は「レーンのエージェントが終了し、かつ変更が無い」と自動削除される。**`Agent` は `run_in_background: false` でも非同期で返るため、レーンは implementer を起動した直後に必ず一度終了する** —— コミットが 1 つも無い初回タスクの最中は削除条件に必ず当たり、**M1-2 と M2-1 で実際に worktree が消えた。**

§2 の表で隔離が必要なステージでは、**あなたが手で作ってからパスを渡す**（ハーネス管理外なので消えない）。

```bash
git fetch origin
git worktree add .claude/worktrees/<フェーズ ID 小文字> -b <ブランチ> origin/main
(cd .claude/worktrees/<フェーズ ID 小文字> && npm ci)   # node_modules は worktree に無い

# レーンを張り直すときはブランチが既にあるので -b を外す（-b 付きは fatal で落ちる）
git worktree add .claude/worktrees/<フェーズ ID 小文字> <ブランチ>
```

**`docs/superpowers/` と `.superpowers/` は `.git/info/exclude` により git 管理外なので、worktree 側に存在しない。** 計画・契約・ledger は**メイン作業ツリーの絶対パス**で渡す（`/Users/ooidetatsuya/repo/kamux/docs/superpowers/...`）。相対パスで渡すとレーンは「ファイルが無い」と言って止まる。

プロンプトの中身（`<>` を表の値で埋める）:

```
あなたは kamux の <ID> レーンを担当する。

計画: docs/superpowers/plans/2026-08-01-kamux/<計画ファイル>（<タスク数> タスク）
契約: docs/superpowers/plans/2026-08-01-kamux/00-contracts.md
ゴール: <ゴール>
作業ブランチ: <ブランチ>（main から切ること。main で直接作業しない）
作業ディレクトリ: <worktree の絶対パス>（隔離が要るステージのみ。全コマンドを cd <絶対パス> && ... の形にすること）

<このフェーズが全体のどこに位置するか / 後続が何に依存するか を 2〜3 行>

実装は src-tauri/ を rust-implementer、src/ を web-implementer に投げること。
model は sonnet を明示する（設計判断を伴うタスクのみ opus）。

<人間ゲートがあれば: 「<内容> は実機確認が必要なので、着手せず BLOCKED で返すこと」>

マージ許可: あり / なし（契約 §32.3。なしの場合は PR をスタックさせ、マージしない）

契約の不足に気づいたら、自分で 00-contracts.md を編集せず、契約変更要求として上げること。

完了したら完了報告フォーマットで返す。
```

**過去タスクの要約や、このセッションの会話履歴を貼らない。** サブエージェントは会話を一切継承しないが、だからといって履歴を貼り付けるのは逆効果（実セッションで dispatch が 42k 文字に膨れ、99% が貼り付けた履歴だった事例がある）。渡すのは計画ファイルのパスと、上の枠内の情報だけでよい。

並列起動するステージでは、**1 回の応答の中に `Agent` 呼び出しを複数書く**。1 メッセージ 1 呼び出しは直列になる。

---

## 6. 起動後の監督

| 状況 | 対応 |
|---|---|
| 進捗を聞かれた | `TaskList` で一覧、`TaskOutput` で途中経過を覗いて要約する |
| **1 時間以上応答が無い** | **停止を疑う。** `git -C <worktree> log --oneline -3` と `git status --short` を見る。動いていなければ `SendMessage` ではなく **`Agent` の再起動**で再開させる（`SendMessage` はつねにバックグラウンド実行になり、完了イベントが 1〜2 時間遅れる事象がある）。2026-08-05 はこれで 4 回・計 3 時間半以上を失った |
| BLOCKED が返った | 人間ゲートならユーザーに提示し、結果を `SendMessage` でレーンへ返す |
| 契約変更要求が返った | `contract-owner` を起動して裁定させる。**同時に走らせるのは 1 体だけ**（下記）。裁定結果は**並列中の他レーンにも周知する** |
| PR が出た | **lane-controller がマージまで行う**（契約 §32 の 7 条件）。あなたは介入しない |
| `needs-human-verification` の PR で BLOCKED が返った | 検証手順をユーザーに提示 → 結果を受けてラベルを外す → `SendMessage` でレーンへ通知 |
| レーンが完了した | 下のゲートを実行 → 次のステージへ |

### 🔴 言明の**一部だけ**を測って、残りを前提として扱わない（群 F。stage4 で 4 回）

**あなたの断定は指示に変換され、レーンの dispatch を経て implementer まで届く。理由は結論より速く、そして遠くまで伝播する。**

| 測らなかったもの | 実測 |
|---|---|
| **理由**（結論は正しい） | 「`Cmd+Alt+J` が発火するのは合成イベントのテスト由来」→ **そのテストは 1 本も存在しない。** 結論（発火しない）は正しかったが理由が偽（台帳 #82）。「push すれば `ci.yml` が走る」を追認 → **`ci.yml` は `branches: [main]` にしか反応せず、結果は永久に来ない。実在しない待機を 1 つ作った** |
| **観測経路**（不在を根拠にした） | 「rust-implementer が変異を打たなかった」→ **レーンが 2 条件を dispatch へ入れ忘れていた。** 「報告に変異検証が無い」→ **レポート `:83` に実在し、要約の書式に出ないだけだった**（台帳 #102）。**不在は「対象が失敗した」証拠にも「観測経路が届いていない」証拠にもなる。2 回とも後者** |
| **すぐ確かめられる証拠** | 「brief の describe は射程外」→ **ファイルを開けば 6 箇所すべてを覆っていた。** 「brief の数え方に系統誤差」→ **系列の頭（Task 1・2）は完全一致で、原因は別だった**（台帳 #103） |
| **原因 / 分類の根拠** | 並列違反の原因を推定して再発防止を書いた → **実際の原因は「マーカで起床して次を出した」だった。推定に当てた手当ては、当事者が「守っていた」と言える形になり、穴が開いたまま閉じたように見える** |
| **比較の片側** | コード側の符号位置は測ったが、**契約側は測らず「U+00B7 のはずだ」と前提した**（contract-owner が測って一致を確認） |

> **【規則】他者の報告を追認するときは、**結論と理由を分けて**、理由のほうに実測の出所が書かれているかを見る。** 書かれていなければ「**結論を採る。理由は未確認**」と明示して返す。
> **【規則】再発防止を書くときは、その原因が**実測**か**推定**かを明記する。推定なら「推定である」と書き、確かめる手順を添える。**
> **【規則】規則が守られたのに悪い結果が出たら、疑う先は 2 つある** —— **規則の書き方（入口）と、規則を満たした後の行き先（出口）。** stage4 で 2 回とも入口を疑い、1 回は出口だった。**先に「規則どおりやった結果どこへ行くか」を辿る。**

### 中継の言葉は、対象ごとに書き分ける（群 A）

**「実装完了」を中継するときに「次のタスクへ進んでよい」と書き、レビューゲートを免除したように読める指示を 2 レーンへ出した**（台帳 #105）。**実装完了はタスク完了ではなく、レビューが別工程として残る。** M2-3 が解釈を確認してきて発覚した —— **止めなければ 2 レーンでレビューが飛んでいた。**

| 中継の対象 | 書く言葉 |
|---|---|
| implementer の完了 | **「レビューへ dispatch すること」** |
| レビューの `APPROVED` / `PASS` | **「次のタスクへ進んでよい」** |
| 修正ラウンドの完了 | **「再レビューへ進むこと」** |

> **「進んでよい」を実装完了の中継に付けない。**
> **⚠️ あなたの完了通知の中継は、レーンにとって唯一の権威である**（マーカは代替でしかない。`lane-controller.md`「対処 1 の続き」(2)）。**中継を怠ると、レーンはマーカに頼るしかなくなる。**
> **⚠️ 時間依存の指示（「◯◯の前に △△ すること」）には、**実行してよい条件**を必ず添える。** 「Task 9 を出す前にブランチを戻せ」には「**その worktree で稼働中のエージェントが 0 体であること**」が要った。**条件の無い時間依存の指示は、受け手がいつ実行しても正しいように見え、実際には正しい瞬間が限られている。**

### 契約の書き手は同時に 1 体だけ

**`contract-owner` を並列に起動しない。** 複数の裁定が同時に走ると、両方がファイル末尾に新章を足して**章番号が衝突する。追記では直せない**（先に書いたほうの番号を後から動かすと、12 計画の相互参照が一斉に陳腐化する）。

- 契約変更要求が複数同時に返ってきたら、**1 体に直列で queue する**（1 回の起動で複数の要求を裁定させてよい）
- 裁定が終わるまで、その論点に触れるタスクは止める

### 人間ゲートの手順は、渡す前に実装で検算する

**手順が空振りしても、ユーザーには「不合格」と見分けがつかない。** 5 つの失敗形を実測している（うち 3 つは 2026-08-05、2 つは 2026-08-06〜07 の M3-1）。

| 失敗形 | 例 |
|---|---|
| **自動テストの緑を根拠にする** | 上流ライブラリのバグには自動テストは無情報である。緑でも症状は残る |
| **実在しない観測を指している** | 手順が出力・ログ・UI 要素を指しているが、実装にその出力経路が無い。`grep` で確かめれば渡す前に分かる |
| **手順の粒度が粗く、症状が素通りする** | 1 文字ずつ打てば現れる現象を「文字列を入力する」と書いた。契約 §71.4 / §71.5 が正典 |
| **期待値が契約と逆を向いている** | M3-1 の項目 22 は `Cmd+J`/`Cmd+K` が editor 画面で効くと書いていたが、契約 §11.4.2 は逆を定めていた。**そのまま実施すれば契約どおりの正しい実装が不合格になる** |
| **期待結果が狭く、不合格の形を含まない** | M3-1 の項目 7 は「nvim が開く」までしか書いておらず、**PTY が 80×24 のまま画面の一部しか埋めない状態でも合格として通った**（ユーザーの目視で偶然見つかった） |

**渡す前に 4 つを行う。**

1. **観測点の実在確認** —— 手順が触れる出力・ログ・UI 要素を実装側で `grep` する
2. **契約との突き合わせ** —— 期待値がその機能を定める契約章と同じ向きか。**手順を書いた人と契約を書いた人が違うので、逆を向いていても誰も気づかない**
3. **不合格の形を書く** —— 期待結果は「何が見えれば合格か」だけでなく、**「どう見えたら不合格か」を必ず 1 行持つ**。合格の形しか書かないと、**実施者は「それらしく動いた」を合格と読む**
4. **🔴 前提条件の到達可能性** —— **手順が作ろうとしている状態に、実装から見て本当に到達できるか**を確かめる

> **⚠️ 検査 1〜3 はすべて「出口」を見ている**（信号が出るか / 向きが正しいか / 読み方）。**「入口」を誰も見ていなかった。**
>
> **実測（2026-08-10 の stage4 ゲートで、台帳 #23 を 3 検査に逐語で通した結果）: 3 つとも合格するのに、手順は必ず空振りする。**
> `PATH` から nvim を外す手順は**実施者の対話シェルの `PATH` を変えるだけ**で、アプリはログインシェルから組み直した `PATH` に 4 つのフォールバックを**無条件に足していた**（`launch_env.rs:34-35,150-155`）。**macOS で nvim が入っているのはまさに `/opt/homebrew/bin` である。**
> **検査 1 で grep すると「ガイド文の経路はある」と出て合格になる。出力経路は在り、作れないのは入力のほうだった。**
>
> **確かめ方: 手順が変える対象を実装側で `grep` し、実装がその値を読んでいることまで辿る。**「その名前の変数がある」で止めない —— **同名の別経路であることがこの穴の正体である。**
> **観測点: 手動スモークの項目それぞれについて「どの変数／状態を、どの経路で変えるか」が 1 行書かれていること。** 書けない項目は、前提条件を作れていない疑いがある。

> **⚠️ 台帳 #23 の帰属を上の表の 2 行目（実在しない観測を指している）から外し、本検査 4 へ移した。** **機構が違う** —— 2 行目は「出力経路が無い」、#23 は「出力経路はあるが前提条件を作れない」。**結果（手順が空振りする）が同じなので同じ行に寄せたのが、規則の側の誤りだった。**

> **【管轄の分界。二重の正典にしないため必ず読むこと】**
>
> | 誰の義務か | 正典 |
> |---|---|
> | **人間ゲート項目を**書く**者**（契約自身 / 計画 / レーン）—— 項目に現れる**名詞 / 操作 / 観測**のそれぞれにリポジトリ内の一次証拠を 1 つ挙げる | **契約 §105.5**（+ §107.4 が「観測」欄に問いを 1 つ足した） |
> | **項目を実施者へ**渡す**前に team-lead が走らせる検算** —— 上の 4 検査 | **本節。契約 §105.5.2 が「契約の管轄外。転写するかどうかは team-lead が決める」と明記している** |
>
> **本節は §105.5 を写していない。写すと二重の正典になる**（台帳 #4 の形）。**3 欄は「項目に現れる具体物を数える」、4 検査は「その項目が信号を生むか」を見る。軸が違う。**
>
> **🔴 §107.4 の問いを、検査 1（観測点の実在確認）に必ず併せて当てること** ——「**その値の読み手は人間の目だけか。機械が読める出力が別に在るなら、そちらを正典にする。探した結果『無かった』なら、探したことと探し方を項目に書く**」。**人間の目だけを読み手にした項目は「見えなかった」と「出ていなかった」が同じ報告になる。**

### 🔴 規則を**当てる**工程を、渡す前に 1 回入れる

**契約 §105.5（名詞 / 操作 / 観測の 3 欄に一次証拠を 1 つずつ挙げる）は 2026-08-09 21:00 に成立した。その当日 21:14 に、それが捕まえるはずの 6 件目を素通りさせた。**

| 欄 | 挙げるべきだった一次証拠 | 実際 |
|---|---|---|
| 名詞（Dock の kamux アイコン） | `lsappinfo … ApplicationType="Foreground"` | 挙げた |
| 操作（見る） | —— | —— |
| **🔴 観測（バッジが見える）** | **`defaults read com.apple.dock autohide` = 0** | **挙げなかった。この機体は `1`（自動的に隠れる）だった** |

**バッジは 3 回とも正しく出ていた。Dock が画面に存在しなかっただけである。** 窓を 3 秒 → 20 秒に広げても、合図を `tail -f` にしても、この原因には一切効かない。

> **【規則】人間ゲートの項目を渡す前に、**その項目に規則を逐語で当てた結果**を 1 行書く。** 「規則を読んだ」ではなく「当てた」である。**規則が在ることは、当てたことを意味しない。**
> **実施者が 6 回止まっている**（台帳 #77 / 本件）。**team-lead の検査は項目の「主張」を見ており、実施者が触る「具体物」を見ていない。**

### 測定を指示するときは、**測る対象を名指しする**（台帳 #78）

**同じ穴が 2 回。** (1) 測定 2 で「**どのスレッドから**測るか」を落とした（メインから測ると `is_waiting` はほぼ常に false になり、別の量を測る） (2) 修正ラウンド 1 で「`wait_for_response` を**どのハンドルに対して**行うか」を落とし、**項目 3〜6 の観測が全部「捨てる経路」にぶら下がった。**

> **「◯◯を測れ」ではなく「◯◯を、△△から、□□に対して測れ」と書く。** 落ちるのはいつも修飾語のほうである。
> **⚠️ 平均値を根拠に設計判断をしない**（台帳 #80）。`is_waiting` の `ratio` が 0.73 の回で試行 A が通り、**0.82 の回で落ちた。** 効くのは着地した瞬間の値だけで、**平均はそれを平滑化して隠す。1 回しか回していなければ「A で十分」と結論していた。**

### 裁定と実施記録は、リポジトリに残る形で渡す

**`SendMessage` で渡した内容はリポジトリのどこにも残らない。** M3-1 では team-lead の裁定を task-reviewer が `TEAM-LEAD-RULINGS.md` に grep しても見つけられず、「承認条件が満たされていない可能性」として検証不能扱いになった（台帳 #12）。

同じ形で、**やると宣言した工程の実施記録が残らない**事故も起きている。PR 24 では契約 §32 条件 3（PR 単位レビュー）が ledger に「Task 8 のレビュー後・PR 24 作成前に実施する。いま dispatch しない」と**予告だけ**書かれ、実施されないままレーンが終了した。**実施済みか未実施かを後から判別する手段が無く、マージ直前に team-lead が気づかなければそのまま入っていた**（台帳 #26）。フェーズ完了報告そのものが出ず、契約 §26.5-3 の仕分け記録が残らなかった事例もある（台帳 #30）。

> **裁定は `SendMessage` と同時に `TEAM-LEAD-RULINGS.md` へ書く。**
> **「後で実施する」と書いたら、実施したときに同じ場所へ結果を書く。** 予告だけが残っている工程は、**未実施として扱う。**

### マージ許可（契約 §32.3）—— あなたが出す

| ステージ | 出し方 |
|---|---|
| 単独レーン（stage0 / 2 / 5 / 6） | **起動時のプロンプトに「マージ許可あり」と書く。** PR は毎回 `main` から切られ、緑になり次第マージされる |
| 並列レーン（stage1 / 3 / 4） | **§2 のマージ順で先頭のレーンにだけ「マージ許可あり」と書く。** 後発レーンには「マージ許可なし」と書き、PR をスタックさせる。先頭レーンが全 PR をマージし終えたら、`SendMessage` で次のレーンに許可を出す |

**🔴 マージ許可を出す便には、その時点で条件が閉じているかを明記する。**

> **「マージ許可: あり。**追加条件なし**」または「マージ許可: あり。**残る条件は N 件（列挙）。それ以外は足さない**」。**
> **許可を出した後に条件を足す必要が生じたら、条件を送るのではなく**許可を一旦取り下げる**便を先に出す。取り下げが届かないうちに進んでも、レーンに責任は無い。**
>
> **なぜ: 2026-08-08、許可を出したあと 3 通に分けて条件を足し、3 通が届いた時点で PR は既にマージ済みだった。** レーンが止まらずに進めたのは正しい —— **許可が下りている状態で、後から来るかもしれない条件を待つ理由が無い。逆順（条件を待って止まる）だと 1 時間単位で止まる。**
> **観測点: マージ許可の便に「追加条件なし」または条件の件数が書かれていること。**

**🔴 番号外の PR（どのタスクにも属さない変更）は、PR 単位レビューが唯一の網になる。**

> **通常の PR はタスク単位レビューも通っているので二重の網がある。番号外の PR はタスク単位レビューに一度も掛からない。**
> **2026-08-09、`[PR H2]`（#62）について「CI 緑になり次第マージしてよい」と 2 度書いた。中身は `task-reviewer` が一度も見ておらず、`pty/surface.rs` の production 挙動を変える修正だった。M2-2 レーンが止めた。**
> **観測点: 番号外 PR のマージ許可に、PR 単位レビューの承認が明示的に引用されていること。**

**プロンプトに許可の有無を書き忘れると、lane-controller は「許可なし」として扱い確認を求めてくる。** 並列ステージで両方に許可を出してはならない —— stage1（M1-2 ∥ M1-3）は `App.tsx` / `store/index.ts` / `useKeymap.ts` が重衝突するため、交互にマージすると後発レーンが毎回コンフリクトする。

### ステージ完了ゲート

```bash
npm run lint && npm run fmt:check && npx tsc --noEmit && npx vitest run
npm run e2e            # M1-2 Task 17 以降
cd src-tauri && cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cd ..
```

**マージ前に、そのステージの PR がすべて CI 緑であること**を `gh pr checks` で確認する（契約 §27.5）。

加えて次の 3 つを確認してから次のステージに進む。

1. `contract-owner` にドリフト検査（契約 §0 の防護節 + §25.4）を走らせ、**出力が空であること**
2. lane-controller の完了報告に **「E2E に移した項目 / 手動に残した項目」** が含まれていること（契約 §26.5）。手動に残した項目は §26.4 のどのカテゴリに当たるかが書かれていること
3. **`.claude/AGENT-LESSONS.md` の未昇格の行（`昇格` 列が `—`）を数え、下記のとおり処理する。** 本ゲートが昇格の唯一の判定点であり、**閾値と昇格 PR の要件はここが正典である。台帳には書き写さない**（`CLAUDE.md` §3 だけは、ここへのポインタ付きで要約を持つ）

   | 状況 | 動き |
   |---|---|
   | **同じ穴が 2 回以上** | **定義ファイルへ昇格させる PR を作る。** 1 回は偶然、2 回は構造 |
   | 1 回だけ | そのまま残す（消さない） |
   | **初出から 3 ステージ跨いで 1 回のまま** | `昇格` 列に `棄却（<理由>）` と書いて行を閉じる。**行は消さない** |
   | **その PR のレビューで出た穴を、同じ PR で**成果物の変更**で直した** | **1 回でも `昇格済み（PR #<n>）` と書いて閉じてよい。** 閾値「2 回」は「まだ直していない事象を定義へ上げるか」の判定であり、**既に直したものを開いたままにする意味は無い**（次のゲートが同じ修正をもう一度作る） |
   | **🔴 その PR の dispatch プロンプトで**口頭補填**しただけ** | **閉じてはならない。`—` のまま残す。** **dispatch はタスク 1 本で消えるので、次のタスクには何も残っていない**（`--no-fail-fast` / 復元後の再ビルド / `git checkout --` の順序 / 変異の適用手段が実例）。**口頭補填を「直った」に含めると、定義ファイルへ上がるべきものだけが消える —— 昇格の必要性が最も高い層が、最も閉じられやすい**<br>**判定: `昇格済み` と書いた行が、成果物の差分を指せること。「dispatch に書いた」しか根拠が無い行は閉じない** |
   | **作り方ではなく作るものの話だった** | `契約 §<n> として採用（PR #<m> …）` と書く。**定義側で直さず contract-owner へ契約変更要求として上げる** |

   **数える単位はステージであってフェーズではない**（stage3 = M2-1 ∥ M3-1 のように 1 ステージが複数フェーズを含む）。台帳の `ステージ` 列がこの判定のためにある。

   > **⚠️「同じ穴」は結果ではなく**機構**で判定する。**
   > **失敗の機構が同じなら同じ穴、結果だけが同じなら別の穴である。** 例: 「呼ぶ側の配線が守られていない」と「`> 0` の恒真述語」は**どちらも結果は『テストが何も守っていない』だが機構が違う**ので別に数える。「起きたあと次の一手を打たなかった」3 件は機構が同じなので 1 群である。
   > **寄せ方は閾値の判定を直接変える。** 別機構を 1 群に寄せれば 1 回のものが 3 回になり、逆も起きる。**群にした根拠（どの機構が同じか）を昇格 PR に必ず書くこと。** 書けないなら、それは同じ穴ではない。

   **昇格 PR には次の 2 つを必ず書く。**

   - **なぜ**（実例と日付）—— **理由の無い規則は次のレーンが消す**（契約 §68.8 の実測）
   - **観測点**（効いたと分かる signal）—— 無いと効いたかを測れない。**issue #51 の受け入れ条件 5 がその形だった**

   **昇格 PR は通常の PR レビューを通す。** PR #52 では追記が既存記述と矛盾しかけ、レビューで検出された。**定義の変更こそ差分レビューが要る。**

   **昇格 PR は、対象行の `昇格` 列を `昇格済み（PR #<n>）` に書き換えるところまでを含む。** マージ後に書こうとすると行は `—` のまま残り、**次のステージのゲートが同じ行を数えて同じ昇格 PR をもう一度作る。**

   **PR 本文・タイトル・群の表は、最後のコミットを積んだ後に取り直す。** 昇格作業は自分の後続コミットで件数と群構成が動くため、**書いた瞬間に偽になる**（PR #57 で 3 回起きた）。**PR 本文はマージ後も残る恒久記録であり、訂正の機会が乏しい。**

   **昇格した規則は台帳の `## 昇格済みの規則（棚卸し用）` にも 1 行足す。** その表は `観測点` 列を持つ —— **上で必須と定めた観測点は、PR 本文ではなくそこに書く**（PR 本文はマージ後に探しにくい）。**昇格済みの規則があるのに同じ穴が 3 回目に出たら、規則の書き方が悪い。** 追記で足さず、**その規則を書き直す** —— 既存記述と矛盾したまま足すと、次のレーンが「矛盾している」として両方消す。

   > **⚠️ 判定基準そのものが壊れていないかを、同じ場で確かめる。**
   > **そのステージでマージした PR の本数**（`gh pr list --state merged --search "merged:>=<ステージ開始日>"`）**と、台帳に増えた行数を比べる。PR が 3 本以上あって増加が 0 行なら、基準を見直す。**
   > 「うまく動いていて出ない」と「基準が壊れていて出ない」は区別が要る。**後者は空振りであり、不合格と見分けがつかない。**
   > **`定義の穴: なし` の連続回数では測れない** —— レビュー報告は短命な作業ファイルにしか無く、台帳は 0 件を記録しない設計だからである。**永続的に数えられるのは「PR 本数」と「台帳の行数」の 2 つだけである。**

   > **🔴 集計コマンド自体を、罠を持つ行に当てて検算する（群 I。2026-08-10 に実際に外した）。**
   > **`awk -F'|'` で未昇格を数えると、セル内の `\|` でも分割して列がずれ、行を静かに落とす。** 実測で **72 と出たが正解は 75**。落ちた 3 行（#58 / #84 / #99）のうち **2 行は、そのゲートが昇格させる群の構成要素そのものだった。**
   > **落ちた行は出力に現れないので、少なく出たことに気づく手段が無い。** 数える前に `grep -cE '^\| [0-9]+ \|'` で総行数を独立に取り、内訳の和と一致することを確かめる。
   > **これは検証 E（3 版連続で壊れた squash 判定）と同じ機構である** —— **解析コマンドを、その解析が曖昧になる入力に当てて検算していない**（正典は `lane-controller.md`「出力を解析する検査は…」）。

   > **🔴 転記の直前に「この一覧を書いた後に起きた事象が無いか」を 1 度だけ問う（群 G）。**
   > **転記は「レビュアーの `定義の穴` 一覧を写す」運用なので、一覧を書いた時刻より後に起きたことを構造的に落とす。** stage4 で 3 回起きた —— 予告だけ書かれて実施記録が残らない（台帳 #26）/ レビューファイルが `定義の穴: なし` のまま更新されない / **レビュー完了後に起きた 2 件がどの一覧にも載らなかった**（台帳 #53）。
   > **同じ形が索引でも起きる** —— **worklist に載った章は全項目照合されたが、同じタスクを縛る `§30.2` の env 表は worklist に無かったため、誰も踏まなかった**（台帳 #97）。**訂正を 1 箇所に入れたら、同じ誤りを持つ他の手順書へ後方転記する**（台帳 #79。5 時間後に実施者へ渡す直前まで誤った手順が残っていた）。
   > **【規則】一覧・索引を正典にする運用では、「一覧の外に何があるか」を問う工程を 1 つ必ず持つ。**

---

## 7. やってはいけないこと

- **実装 implementer を並列に起動する**（レーン内は必ず直列。**1 レーン = 1 worktree。分ければ並列にできる、ではない**）。2026-08-05、M2-1 の Task 4 と Task 5 の並行編集で**作業が実際に消失した**（復旧に一時 worktree + `git update-ref` を要した）。消失しなくてもレビュー範囲が汚染される
- **`contract-owner` を並列に起動する**（章番号が衝突し、追記では直せない）
- **team-lead 自身がコードを直す**（レビューを迂回し、あなたのコンテキストが汚れる）
- **`.claude/agents/*.md` と本 `SKILL.md` の変更を、レビューを通さずに入れる**（群 M）。**「コードを直す」としか書かれていなかったため、定義ファイルが該当するか読み取れず、PR #52 / #54 / #57 と 3 度続いた。台帳 #2 は `—` のまま据え置かれ、閾値 2 回が発火しなかった**（台帳 #34）。**定義ファイルはすべてのレーンの振る舞いを決めるので、実装コードより差分レビューが要る。** **team-lead が書いてよいが、`task-reviewer` を PR の差分に 1 回かけてからマージする**（昇格 PR も同じ。下のゲート §6 が既に要求している）
- **`00-contracts.md` を lane-controller や implementer に編集させる**（章番号がずれると 12 計画の参照が一斉に陳腐化する）
- **人間ゲートを「たぶん通る」で ✅ にする**（headless で再現しない項目を通ったことにするのが、この計画で最も危険な失敗）
- **stage5 を並列にする**（10 ファイル衝突。得られる時間より統合コストが上回る）
