dist/
# 無視パターンが特定の場所を焼き込んでいると、その場所が動いたときに黙って死ぬ
# （Cargo ワークスペース化で target/ が src-tauri/target/ から repo root へ移った）。
# .prettierignore は gitignore 構文なので、先頭に / の無い target/ は任意の階層に
# 一致し、src-tauri/target/ も既にこの 1 行が覆う（後者は冗長だった）。
target/
playwright-report/
test-results/
package-lock.json
docs/
.claude/
.agents/
.remember/
CLAUDE.md
AGENTS.md
skills-lock.json
# task brief / report / 進捗台帳（.superpowers/sdd/*/.gitignore で git 管理外）。
# 他タスクの brief や共有 progress.md を Prettier が書き換えるのを防ぐ。
.superpowers/
# 契約 §53.2: .claude/skills/kamux-design-system/tokens.css の逐語コピー。
# Prettier のコメント字送り整形すら許さない（1 文字も編集しない）。
src/styles/tokens.css
