# kamux エージェント運用ガイド

このファイルは Claude Code / Codex など、このリポジトリで作業するすべてのコーディングエージェント向けの共通指示書です。
（`AGENTS.md` はこのファイルへのシンボリックリンクです。編集する場合は `CLAUDE.md` 側を編集してください。）

## 言語設定

- ユーザーへの回答（地の文・説明・要約）は**常に日本語**で書く。
- コード・コマンド・ファイル名・識別子・エラーメッセージの引用などはそのまま（原語）で残す。

## 適用範囲

本ガイドの「ワークフローの進め方」は、**ユーザーと直接対話するメインセッション**への指示である。`.claude/agents/` で定義されたサブエージェントには適用しない。サブエージェントは、渡された task brief と `superpowers:subagent-driven-development` の手順に従う。

サブエージェントはユーザーに直接確認を取れない。判断が要る場合は自分で止めずに **BLOCKED として呼び出し元へ返す**こと。

進捗の記録先は `superpowers:subagent-driven-development` の ledger（`.superpowers/sdd/<plan>/progress.md`）とする。**レーンごとに独立したファイルになるため、並列実行しても衝突しない。**

## ワークフローの進め方

### 1. まず計画（Plan First）

- 非自明なタスク（3 ステップ以上、または設計判断を伴うもの）は必ずプランモードに入る。
- 想定と違う方向に進み始めたら、押し切らずに**即座に止まって計画し直す**。
- 検証（verification）の手順もプランモードで設計する。実装のためだけのものではない。
- 曖昧さを減らすため、詳細な仕様を先に書き下す。

### 2. サブエージェントの活用

- メインのコンテキストウィンドウを汚さないよう、サブエージェントを積極的に使う。
- 調査・探索・並列分析はサブエージェントに任せる。
- 難しい問題にはサブエージェントで計算量を投入する。
- 1 サブエージェント = 1 タスク。焦点を絞って実行させる。

### 3. 自己改善ループ

- ユーザーから指摘を受けたら、それを `.claude/AGENT-LESSONS.md` に必ず追記する。**規則ではなく事象として書く**（「◯◯すべき」ではなく「◯◯が起きた」）。
- **規則の形にするのは昇格時である。** 同じ穴が 2 回目に出たら、`.claude/agents/*.md` か `.claude/skills/kamux-lane/SKILL.md` へ規則として昇格させる（判定はステージ完了ゲート。**手順・閾値・人間承認ゲートの正典は `.claude/skills/kamux-promotion/SKILL.md`**）。
- **昇格させた規則には、なぜそうするかと、効いたと分かる観測点を必ず併記する。** 理由の無い規則は次のレーンが消す。
- セッション開始時に、そのプロジェクトに関連する `.claude/AGENT-LESSONS.md` を読み返す。

### 4. 完了前の検証

- 動作を証明せずにタスクを完了扱いにしない。
- 関連する場合は main と自分の変更との挙動差分を確認する。
- 「スタッフエンジニアはこれを承認するか？」と自問する。
- テストを走らせ、ログを確認し、正しさを実証する。

### 5. エレガンスの追求（バランスを取って）

- 非自明な変更では、一度立ち止まって「もっとエレガントなやり方はないか？」と問う。
- その場しのぎに感じる修正なら「今わかっていることを全部踏まえて、エレガントな解を実装し直す」。
- 単純で自明な修正ではこの工程を飛ばす。過剰設計しない。
- ユーザーに提示する前に、自分の成果物に自分でツッコミを入れる。

### 6. 自律的なバグ修正

- バグ報告を受けたら、手取り足取りの指示を求めずにまず直す。
- ログ・エラー・失敗したテストを指し示されたら、そこから自力で解決する。
- ユーザー側にコンテキストスイッチを強いない。
- CI の失敗テストは、直し方を教わらなくても直しに行く。

## 基本原則

- **シンプルさ優先**: あらゆる変更を可能な限り単純に保つ。影響するコードは最小限に。
- **手抜き禁止**: 根本原因を突き止める。一時しのぎの修正はしない。シニア開発者の基準で。
- **影響範囲の最小化**: 必要な箇所だけに触れる。新たなバグを持ち込まない。
